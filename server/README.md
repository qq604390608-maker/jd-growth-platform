# server/ · 服务端工程结构（枝杈索引）

## 文档卡

| 项 | 内容 |
| ---- | ---- |
| 上游 | `AGENTS.md`（宪法索引：server/ 五模块名不得改）｜ `../docs/03-locks/tech-stack.md`（§2.2 服务端模块 / §6 工程结构 / §7.3 部署形态 `TS-20` 待确认）｜ `../docs/04-plan/dev-plan.md`（阶段0 工程骨架；阶段1 · M2 F-07~F-12）｜ `../wrangler.toml`（D1 绑定配置）｜ `../db/`（迁移与种子） |
| 职责 | 五个服务端 Worker 模块的实现目录；唯一的数据出口与凭证持有方（tech-stack §1.2） |
| 硬红线 | 生产环境零写操作；真实返回（禁模型预期替代）；证据四要素；失败不否定结论（BRD §5.3 / §7） |

## 工程状态（2026-09-19，阶段0-工程骨架 → 阶段1 · M2 → 阶段2 · M5 F-23/F-24/F-25/F-26 → 阶段3 · M1 F-01）

- ✅ **工程骨架已立**：单一 Worker 入口 `server/api/index.js`（`wrangler.toml` 的 `main`），D1 绑定 `DB`（`migrations_dir = ./db/migrations`）。`wrangler dev` 起本地 D1 + `wrangler d1 migrations apply --local` 成功（见下方验证）。
- 🟡 **模块进度**：`shared-context` 六个功能点 **F-07 业务背景管理**、**F-08 可用来源与工具登记**、**F-09 证据管理**、**F-10 机会记录管理**、**F-11 研究结果与历史管理**、**F-12 上下文按任务组织注入** 已落地（2026-09-19，用例全绿，见 `./shared-context/README.md`）——**M2 阶段1 全部功能点收口**；`tool-executor` **F-23 工具注册与权限检查**（38 断言全绿）、**F-24 查询执行与真实返回**（74 断言全绿，含 DS-06 自建 MCP 客户端五项协议面）、**F-25 查询记录保存**（53 断言全绿，`EXT-01 query_record` 落痕：失败也留痕、记录可回查）、**F-26 失败重试与受限返回**（100 断言全绿：重试是代码逻辑、上限取 `CFG-04` 封顶 100、成功不算失败、受限不重试、用尽→受阻/再犯→停止且停止不自动重启、已停止/已完成任务在执行前即被前置守卫拒；任务态写入面单列 `task-state.js` 以便静态验证，见 `./tool-executor/README.md`），**M5 阶段2 四点全收口**；`task-runner` **F-01 研究目标登记与口径管理**（88 断言全绿：目标身份 + 六要素**定版式版本化**（历史快照逐字节只读、`is_applied` 同 goal 恒至多一行 1）、材料逻辑删除、`CFG-05` 规则驱动口径检查 → `PD-04` 待补项（别名归一显式可见）、补充并入六要素并 bump 新版本、`goal_check` 2 步任务）、**F-02 机会发现任务调度**（86 断言全绿：`CFG-04` 策略登记与选取、运行频率四式 → Cron Triggers（最小粒度 1 分钟 / 250 条上限）、`retry_limit` 与 `max_duration_min` 写入侧从严、`createDiscoveryTask` 建任务→`LNK-04`→五步→`CFG-06` 上下文现读→发 Queue 消息（恰两键、<1KB）、`delegateToAgent` 守卫）、**F-03 研究建议管理（人工节点）**（82 断言全绿：`MD-12` 建议登记（研究问题必需、可选列按 `varchar(300)` 口径拒超长）、**幂等键 = 机会+问题+假设+限制 的 SHA-256 摘要**（`UK` 落地「同键不新建、不重复启动相同任务」；用 JSON 序列化而非分隔符拼接，避免撞键与爆列长）、**建议与机会版本绑定**（版本号从机会现读、入参不符即拒）、研究问题不明确**只提示不阻断**（「无需重填机会材料」）、机会 `candidate→submitted` 留一行 `PD-05`（改行经 F-10 单一写入面）、`markProposalTriggered` 守住二次启动），**F-04 HVA 研究任务调度**（45 断言全绿：建议提交后建 `hva_research` 任务→`LNK-04` 锚点→五步→按 `CFG-06` hva_research 模板落 `PD-06` 二阶段上下文（selected_opp 关联机会、product_question 关联建议）→取 `CFG-03` 按 `hva-agent` 工具权限→幂等守卫同一建议不重复启动→发 Queue 消息恰两键+`delegateToAgent` 守卫；**第二阶段启动时点＝建议提交**、**版本冲突以机会为准并提示**、`hva_followup` 创建归 F-05），**F-05 追问与版本管理**（57 断言全绿：在已有研究上提新问题→关联原研究建 `hva_followup` 任务（`parent_task_id` 挂原任务）→建新 `MD-07` 研究壳（`parent_research_no` 指原研究、`start_task_id` 指新任务、七要素从原研究承接）→`LNK-04` 锚点→五步→按 `CFG-06 hva_followup` 模板落 `PD-06` 二阶段上下文（含 `related_history`）→取 `CFG-03` 按 `hva-agent` 工具权限→写 `PD-07` 追问消息（双外键库级强制）→发 Queue 消息恰两键+`delegateToAgent` 守卫；原研究/原任务维持启动版本不变、版本可显式升版不混旧、零外部调用），**F-06 任务记录与异常恢复**（47 断言全绿：复用 F-26 任务态单一写入面，`retry_limit` 封顶 100 显式报错、受阻矩阵保留 `done_part`、停止不自动重启）；`agent-orchestrator` **F-13 角色指令配置**（30 断言全绿：MD-13/MD-14 唯一写入面、版本单行推进）+ **F-14 围绕目标寻找线索**（33 断言全绿：纯编排零写库、意向分归一、S-A1 确定性查证计划＋停止条件、S-A3 线索须有归属、复用 shared-context 读面）+ **F-15 基础查证**（87 断言全绿：S-A2 一次闭环**经 M5 真实查询**——`result_summary` 原样透传、**只有真实 ok 才 `verified=true`、模型预期被显式忽略（哨兵串证明）**；五项检查＝来源/适用范围/信息时点/已有机会/信息缺口（缺口口径读 `CFG-05`）；倒挂隔离仅该条失效非整包失败；缺字段退回请补不编造；证据草稿复用 F-09 `createEvidence` 落 `EXT-02`；**零自有写语句**）+ **F-16 机会形成与去重**（73 断言全绿：S-A4 整理——依据足够→组装六要素（对应目标/涉及对象/观察现象/初步依据/研究理由+未知项二态）落 `MD-06`、判重命中落 `LNK-03` 相同问题关联更新（方向对齐种子 `LK-OR-002`）；**依据不足→缺口记录且不写任何表**（「本轮未产生新机会」是合法产出）；`unknown_item` 二态（未评估→不齐、确无→齐全、纯空白串**应用层显式拒**）；**零自有写语句、零裸 SQL**——MD-06 归 F-10 `createOpportunity`、LNK-03 归 F-10 `linkOpportunityRelation`、取号走读面）已建；F-17~F-22 待建（受 A-1 LLM 门禁）。
- ⚠️ `TS-20` 待确认：五模块拆多 Worker 还是合并单 Worker。**当前为单 Worker 入口**，拆分时调整 `wrangler.toml` 的 `main` 与 `modules` 配置，**不改变模块名与职责边界**。

## 模块清单（职责边界以 tech-stack §2.2 为准，名不得改）

| 模块 | 目录/文件 | 状态 | 服务功能点 |
| ---- | ---- | ---- | ---- |
| `api` | `server/api/index.js` | 🟡 骨架 + F-07~F-12 + **F-23/F-24/F-25/F-26** + **F-01/F-02/F-03/F-04/F-05** 路由（健康检查 / D1 探测 / 业务背景库接口 / 来源登记接口 / 证据接口 / 机会记录接口 / 研究结果与历史接口 / 上下文注入接口 / 工具注册与权限判定接口 / 工具描述与真实查询接口 / 查询记录落痕与回查接口 / 失败重试与受限返回接口 + 运行策略与受阻记录回查 / **目标登记与口径管理接口（含口径检查、待补项补充）/ 发现任务调度接口（运行策略管理 + 生效策略 + 到点建发现任务 + 派发回查 + Agent 调用契约入口）/ 研究建议接口（提交/列表/读模型/触发登记/问题即时提示）/ HVA 研究任务接口（建任务 + 回查）/ 追问任务接口（建任务 + 回查）/ 任务记录与异常恢复接口（block/stop/resume）/ 角色指令配置接口（读角色指令 + 版本推进 + 注册 Skill）/ 线索寻找接口（S-A1 查证计划 + 一阶段注入清单 + S-A3 线索归纳 + 意向分归一）/ 基础查证接口（S-A2 经 M5 真实查询 + 五项检查 + 倒挂隔离 + 缺字段退回请补 + 证据草稿与落库 + 单查询排查）/ 机会形成与去重接口（S-A4 依据足够→机会记录+判重关联；不足→缺口记录且不写库；六要素组装 + 依据判定 + 缺口记录 + 取号）**） | F-07~F-12 **F-23 F-24 F-25 F-26** **F-01 F-02 F-03 F-04 F-05 F-06 F-13 F-14 F-15 F-16**、F-27~F-32（后续实现） |
| `task-runner` | `server/task-runner/` | 🟡 部分（**F-01 / F-02 / F-03 / F-04 / F-05 / F-06 已建** 2026-09-19。**与 F-26 共用状态机口径**：任务态写入面已在 `tool-executor/task-state.js` 落地，本目录 `import` 复用而非重写；**机会状态改行复用 `shared-context` 的 F-10 写入面**） | **F-01 F-02 F-03 F-04 F-05 F-06** |
| `shared-context` | `server/shared-context/` | 🟡 部分（**F-07 / F-08 / F-09 / F-10 / F-11 / F-12 已建** 2026-09-19；**M2 阶段1 收口**） | F-07~F-12 |
| `agent-orchestrator` | `server/agent-orchestrator/` | 🟡 部分（**F-13 角色指令配置 / F-14 围绕目标寻找线索 / F-15 基础查证 / F-16 机会形成与去重已建** 2026-09-20；F-17~F-22 待建。**MD-13/MD-14 唯一写入面**在 `profile.js`；**F-14 为纯编排层零写库**（`discovery.js`）、**F-15 零自有写语句**（`verification.js`：EXT-01 经 M5、EXT-02 经 F-09，外部访问只经 M5）、**F-16 零自有写语句且零裸 SQL**（`opportunity.js`：MD-06/LNK-03 全经 F-10 `shared-context` 写入面，取号/判重走读面），零外部调用、不调 LLM（A-1 门禁只挡推理部分）） | **F-13 F-14 F-15 F-16** F-17~F-22 |
| `tool-executor` | `server/tool-executor/` | 🟡 部分（**F-23 / F-24 / F-25 / F-26 已建** 2026-09-19；M5 阶段2 全收口） | **F-23 F-24 F-25 F-26** |

## 部署与本地

- 本地启动：`wrangler dev`（默认 local D1）
- 迁移应用：`wrangler d1 migrations apply jd-growth-platform --local`
- 研发期种子：`wrangler d1 execute jd-growth-platform --local --file=db/seed/0001_mock.sql`（**一次性**，重灌会撞字典 UK）
- 出口形态：前端经 `server/api` 取数；前端不直连库、不持凭证（tech-stack §1.2）

## 反向清单

- **上游（我来自哪）**：`AGENTS.md` 索引（`server/` 行）｜ `tech-stack.md` §2.2 / §6 / §7.3 ｜ `dev-plan.md` 阶段0 / 阶段1 ｜ `../wrangler.toml` ｜ `../db/`
- **下游（我被谁引用）**：各 `server/` 模块（后续阶段）｜ 阶段0-CI（`.github/workflows`，部署本 Worker）｜ `frontend/`（阶段5 经本 `api` 取数）
- **登记**：`AGENTS.md` 索引 `server/` 行（状态已由「待建」翻为「🟡 部分已建 2026-09-19」）
