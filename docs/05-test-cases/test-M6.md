# 测试用例 · M6 运营端工作台（F-27~F-32）

> **文档卡**
> | 项 | 内容 |
> | ---- | ---- |
> | 文档编号 | TC-M6 |
> | 上游约束 | `AGENTS.md`（白盒原则｜双向引用）｜`BRD.md` §4 F-27~F-32、§5.3 硬红线、§7 验收总则｜`PRD-M6-运营端工作台.md`（F-27~F-32 验收要点）｜`schema.md`（MD-01/02/03、MD-06、MD-07~11、MD-12、PD-01/02/03/05、PD-07、LNK-01、EXT-02）｜`tech-stack.md`（DS-05 零构建前端、§1.2 前后端分离）｜`prototype/`（可交互原型，已钉死需求）｜`dev-plan.md`（阶段 5 · M6）｜`ADR-003`（六要素二态） |
> | 范围 | 前端（前后端分离的前端）：六页面（目标/机会/建议/结果/追问/任务），只读消费 `server/api` |
> | 分层覆盖 | 契约(L2) / 约束(L3) / 平台集成(L5)。**L1（纯计算）本模块无评分逻辑**（价格展示约束属域外，见注）**；L4（Agent Skill）不适用**。 |

## 1. 契约 / 结构（L2）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-C-M6-001 | L2 | F-27~F-32 | 前端调 `server/api` | 前端消费 `server/api` 返回的 JSON；**前端不直连数据库、不持有任何外部系统凭证**；动态数据一律经 API（tech-stack §1.2）。硬红线：前端不发起任何生产写接口调用。 | 见 `tech-stack.md` §1.2（前后端分离落点）｜`BRD` §5.3｜`PRD-M6` 上游约束 | — |

## 2. 约束 / 数据（L3）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-D-M6-001 | L3 | F-27~F-32 | 前端任何写操作 | **前端不写库**：所有写请求经 `server/api` 转发至后端；前端代码库不得出现直接 D1 binding 或外部系统凭证。 | 见 `tech-stack.md` §1.2（前端不直连、不持凭证）｜`BRD` §5.3｜`PRD-M6` 上游约束 | — |

## 3. 平台 / 集成（L5 · 六页面逐页）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-I-M6-001 | L5 | F-27 | 目标配置页 | 业务方登记/关联目标六要素；**不替业务方定指标**（口径不清时列待补项）；目标更新形成新版本并提示影响范围（版本可见，不混期）。 | 见 `PRD-M6` F-27 验收要点｜`M1` TC-I-M1-005｜`BRD` F-01（Agent/PM 不另定指标） | — |
| TC-I-M6-002 | L5 | F-28 | 机会列表与详情页 | 按目标浏览机会（状态：候选/暂不研究/已提交研究）；详情呈现**六要素齐全**＋证据链（来源/条件/时点可点开回查）＋未知项；证据四要素缺一不可回查。 | 见 `PRD-M6` F-28 验收要点（六要素齐全、证据可回查）｜`ADR-003`｜`M2` TC-I-M2-001（四要素） | — |
| TC-I-M6-003 | L5 | F-29 | 研究建议提交页 | PM 选机会→填研究问题（必需）→可选补候选行为假设/人群限制；提交后触发 F-04；**重复提交幂等**。 | 见 `PRD-M6` F-29 验收要点｜`M1` TC-I-M1-002（幂等）｜`BRD` F-03 | — |
| TC-I-M6-004 | L5 | F-30 | 研究结果页 | 呈现结果七要素；每项关键发现挂证据来源与适用范围；候选 HVA 支持/不支持并列；**已查明与仍受限内容视觉可分**。 | 见 `PRD-M6` F-30 验收要点｜`M4` TC-A-M4-004（七要素）｜`BRD` F-21 | — |
| TC-I-M6-005 | L5 | F-31 | 追问对话页 | 基于通用 Agent 场景化追问：拿到上下文→对话再调度/查没发现处/注入新信息再跑；追问建新任务并关联原研究（F-05）。 | 见 `PRD-M6` F-31 验收要点｜`M1` TC-I-M1-003｜`M4` TC-I-M4-001 | — |
| TC-I-M6-006 | L5 | F-32 | 任务与状态页 | 展示任务运行状态/受阻原因（等待必要信息/等待接口恢复/已停止）/已完成部分；机会未被选中、任务未完成、研究未支持候选行为三类事实分别可见；状态机口径与 M1 F-06 一致。 | 见 `PRD-M6` F-32 验收要点｜`M1` TC-I-M1-004（状态机）｜`BRD` F-06 | — |
| TC-I-M6-007 | L5 | F-27~F-32 | 前后端分离判断标准 | **删掉 `frontend/` 整个目录**，`server/api` 与 `db` 仍能独立存在并被任意客户端调用（白盒逐点）。 | 见 `tech-stack.md` §1.2（判断标准）｜`dev-plan.md` 阶段 5 验收 | — |

## 4. 本模块不适用层级说明

- **L1（纯计算）**：前端无评分/归一逻辑；用户示例「活动价≤标价」属交易域，**本平台无价格字段**（BRD §5.2 out of scope），不适用，其「单实体不变量＋失败隔离」模式已在 `M5` TC-U-M5-001 以域内涵代演示。
- **L4（Agent Skill 六步）**：前端非 Agent，不适用；Skill 见 M3/M4。
