# 测试用例 · M5 工具执行程序（F-23~F-26）

> **文档卡**
> | 项 | 内容 |
> | ---- | ---- |
> | 文档编号 | TC-M5 |
> | 上游约束 | `AGENTS.md`（白盒原则｜双向引用）｜`BRD.md` §4 F-23~F-26、§5.3 硬红线、§7 验收总则｜`PRD-M5-工具执行程序.md`（F-23~F-26 验收要点）｜`schema.md`（CFG-02/03/04/05、EXT-01/03）｜`external-deps.md`（§2 五系统、§5 工具 TOL-01~12、§6 mock 契约、§7）｜`tech-stack.md`（DS-06 自建 MCP、§2.6 失败语义映射）｜`dev-plan.md`（阶段 2 · M5） |
> | 范围 | 查询执行与留痕：工具注册与权限、真实查询返回、查询记录保存、失败重试与受限返回 |
> | 分层覆盖 | 单元(L1) / 契约(L2) / 约束(L3) / 平台集成(L5)。**L4（Agent Skill 六步）不适用**——M5 是工具执行程序，行为（F-23~26）归入 L5；Skill 见 M3/M4。 |

## 1. 单元 / 纯计算（L1）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-U-M5-001 | L1 | F-24 | **单实体不变量＋失败隔离（价格约束模式）** | 用户示例「活动价 69 > 标价 59 → 商品级隔离、非整包失败」：本仓库为研究平台、**无交易价格字段**（BRD §5.2 out of scope 含「收益测算」），价格约束不在本域适用。故以**域内涵代**演示同模式：**证据信息时点 ≤ 取数时刻**（`EXT-02.info_time_point ≤ EXT-01.query_time`）。倒挂（时点晚于取数）→ **仅该证据失效（实体级隔离），非整包查询失败**；其余证据照常。 | 见 `BRD` §5.2（收益测算 out of scope）｜`schema.md` EXT-01/02 字段｜`ADR-003` 二态精神 | ⚠️域外模板 |

## 2. 契约 / 结构（L2）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-C-M5-001 | L2 | F-24 | 工具一次返回 | 返回体可映射到 `EXT-01`：`result_status` / `result_summary` / `returned_rows` / `fail_reason` / `retry_count` / `restricted_flag`；**硬红线**：返回内容保留查询条件、来源、时点、限制（证据四要素），不得用模型预期代替。 | 见 `external-deps.md` §6（统一约定）｜`schema.md` EXT-01｜`PRD-M5` F-24 验收要点 | ⚠️待确认(§7-T01/T02/T05) |

## 3. 约束 / 数据（L3）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-D-M5-001 | L3 | F-23 | `CFG-02 tool_registry`：重复 `tool_id` / `tool_code` | 反①：PK 拒绝 NULL/重复；反②：UNIQUE(tool_code) 拒绝重复。正：12 工具（TOL-01~12）种子齐全。 | 见 `schema.md` CFG-02（PK/UNIQUE）｜DDL L50/57｜`db/seed/0001_mock.sql`（12 工具已种） | — |
| TC-D-M5-002 | L3 | F-23 | `CFG-03 tool_permission.tool_id='TOL-NOPE'` | 反：FK 失败（权限须指向已注册工具）。 | 见 `schema.md` CFG-03（FK tool_id）｜DDL L65 | — |
| TC-D-M5-003 | L3 | F-23 | `CFG-04 run_policy.goal_id='GOAL-NOPE'`（非平台级） | 反：FK 失败；正：平台级 `goal_id=NULL` 允许（DDL 定义可空）。 | 见 `schema.md` CFG-04（goal_id 可空 FK）｜DDL L76｜`tech-stack.md` §5 | — |
| TC-D-M5-004 | L3 | F-23 | `CFG-05 gap_rule` 重复 `rule_id` | 反：PK 拒绝。正：4 口径规则（GAP-1~4）种子齐全。 | 见 `schema.md` CFG-05｜DDL L86｜`db/seed` GAP 已种 | — |
| TC-D-M5-005 | L3 | F-25 | `EXT-01 query_record`：`task_id`/`source_id` 不存在 | 反：FK 失败（每次查询须归属真实任务与来源）。 | 见 `schema.md` EXT-01（FK）｜DDL L431-433 | — |

## 4. 平台 / 集成（L5 · 工具执行行为 F-23~F-26）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-I-M5-001 | L5 | F-23 / F-24 | 每次请求先查权限 | **权限分支互斥**：允许 → 执行；不允许 → 保存 `restrict_reason` 并返回受限原因（`restricted_flag=1`）。不静默放行。 | 见 `PRD-M5` F-23 验收要点（权限分支互斥）｜`schema.md` CFG-03/EXT-01 | ⚠️待确认(§7-T04) |
| TC-I-M5-002 | L5 | F-24 | 真实查询返回 | **不用模型预期内容代替查询结果**；返回保留来源/条件/时点/限制（四要素）。失败返回具体原因。 | 见 `PRD-M5` F-24 验收要点｜`BRD` §7.3｜`external-deps.md` §6 | ⚠️待确认(§7-T01/T02/T05) |
| TC-I-M5-003 | L5 | F-25 | 执行落库 | 每次执行把条件/来源/时点/结果或失败原因写入 `EXT-01`；**失败也留痕**（可回查）。 | 见 `PRD-M5` F-25 验收要点｜`schema.md` EXT-01｜`BRD` §7（记录可回查） | — |
| TC-I-M5-004 | L5 | F-26 | 调用失败 | **重试是代码逻辑不是 AI 决策**（Queues `max_retries`，≤100）；持续失败 → 暂停受影响研究、保留已完成部分（`task_status=blocked`，见 `M1` TC-I-M1-004）；实在卡死保留状态停止。 | 见 `PRD-M5` F-26 验收要点｜`tech-stack.md` §4.2（重试≤100）｜`BRD` §7.4 | ⚠️待确认(§7-T06) |
| TC-I-M5-005 | L5 | F-23 / F-24 | 外部接口调用 | **生产零写**：所有查询为只读（`F-24`）；任何面向生产环境会改线上数据的接口**不实现、不调用**（CI 不配置生产写 Token）。 | 见 `BRD` §5.3 硬红线｜`tech-stack.md` §7.2（零写落实）｜`PRD-M5` F-24 | — |

> ⚠️ **待桩说明**：F-23~F-26 的真实工具名/查询条件/返回结构/失败语义均依赖 `external-deps.md` §7（T-01/T-02/T-04/T-05/T-06）未关闭项；本步用例以 **stub/mock** 打桩（契约见 `external-deps.md` §6 七类行为），**不设为发布门禁**。

## 5. 本模块不适用层级说明

- **L4（Agent Skill 六步）**：M5 无 Skill；工具执行行为归入 L5。Skill 见 M3（S-A1~S-A4）/ M4（S-B1~S-B4）。
