# 测试用例 · M3 机会发现 Agent（F-13~F-17）

> **文档卡**
> | 项 | 内容 |
> | ---- | ---- |
> | 文档编号 | TC-M3 |
> | 上游约束 | `AGENTS.md`（白盒原则｜术语口径「候选行为」｜人工节点不自动交接）｜`BRD.md` §4 F-13~F-17、§5.3 硬红线、§7 验收总则｜`PRD-M3-机会发现Agent.md`（F-13~F-17 验收要点＋§1.1 领域能力定义：agent.md / business-rules.md / S-A1~S-A4）｜`schema.md`（MD-06、MD-13、MD-14、LNK-03、CFG-05）｜`external-deps.md`（§2 五系统、§5 工具 TOL-01~12、§7）｜`tech-stack.md`（DS-02 Workers AI、DS-06 自建 MCP、§2.5 json_schema）｜`dev-plan.md`（阶段 4 · M3）｜`ADR-003`（六要素二态） |
> | 范围 | 第一阶段研究：围绕目标找线索、基础查证、机会形成与去重，止于「机会＋初步依据」 |
> | 分层覆盖 | 单元(L1) / 契约(L2) / 约束(L3) / Agent 行为(L4) / 平台集成(L5) 全覆盖 |

## 1. 单元 / 纯计算（L1）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-U-M3-001 | L1 | F-14 / F-15 | 意向分归一：`intention_raw=1.3`、`threshold=1.0` → `min(raw/threshold, 1)` | 输出 `1.0`（封顶 1）；`intention_raw=0.5` → `0.5`。确定性、无随机。 | 见 `PRD-M3` F-14/F-15（线索评分、基础查证）｜`BRD` §4 F-14 验收：线索对人群差异停留在发现现象 | — |

## 2. 契约 / 结构（L2）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-C-M3-001 | L2 | F-13 | Agent 一次六步产出 | 输出为**结构化 JSON**（经 `response_format: json_schema` 强制）；`strict` 校验**只卡外层可解析性，不钻 `data` 子 schema**（tech-stack §2.5 仅规定「json_schema 结构化输出」，未定义外层字段名与六步名——见下方 ⚠️）。**硬红线断言**：输出中**不得含**活动配置 / 权益组合 / 预算 / 排期 / `creative`/`review`/`deliver` 类内容（BRD §5.3 不输出生产动作）。 | 见 `tech-stack.md` §2.5（结构化输出 json_schema）｜`BRD` §5.3 硬红线｜`PRD-M3` F-13 | ⚠️待确认(契约名) |

> ⚠️ **待确认（契约名来源说明）**：用户指令要求本层对齐「8 字段外层契约（schema_version/run_id/step/status/source_files/data/warnings/next_step）＋标准六步名（input/audience/product_match/creative/review/deliver）」，并复用 `scripts/validate_output.py` 与数据集 `cases/`。**账实核对结果**：该契约名在白名单文件（tech-stack §2.5 / schema / BRD）中**均未命中**；`scripts/`、`cases/` 两目录**仓库当前不存在**；且 `creative/review/deliver` 步骤与 BRD §5.3 硬红线（不输出活动配置/权益/排期）**直接冲突**。故本层仅断言「可解析 JSON + 硬红线禁输出」，具体外层字段名与六步名**待【输出契约文档】锁定后回填**，不臆造。

## 3. 约束 / 数据（L3）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-D-M3-001 | L3 | F-16 | `MD-13 agent_profile` 重复 `agent_code='discovery-agent'` | 反：UNIQUE(agent_code) 拒绝（该 UK 承载 MD-14 外键）。 | 见 `schema.md` MD-13（agent_code UNIQUE）｜DDL L120 | — |
| TC-D-M3-002 | L3 | F-13 / F-16 | `MD-14 skill_registry`：`skill_no='S-A1'`、`skill_code='clue-scan'`、`bound_agent_code='discovery-agent'` | 正：插入成功（**Q-07 已关闭**，S-A1↔clue-scan↔AGP-DISC 映射确认）。反①：重复 `skill_no` → PK 拒绝；反②：重复 `skill_code` → UK 拒绝；反③：`bound_agent_code` 错 → FK 失败。 | 见 `schema.md` §12 Q-07（已决 2026-09-19）｜MD-14（PK/UNIQUE/FK）｜DDL L307-313｜`external-deps.md` T-24 | — |

## 4. Agent 行为（L4 · S-A1~S-A4 六步实跑）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-A-M3-001 | L4 | F-14 | S-A1 发现任务调度：一阶段注入清单（目标/范围/背景/历史/能力说明） | 输出查证计划（先查什么、后查什么）＋逐项结果；**停止条件**=计划内查证完成或依据已足够。 | 见 `PRD-M3` §1.1 S-A1｜`BRD` F-14 | — |
| TC-A-M3-002 | L4 | F-15 | S-A2 指标查证：待查指标＋范围 | 经**工具执行程序**（M5）真实查询 → 分析返回 → 记录任务；输出带来源与时点的查询事实；**禁止以模型预期内容代替查询结果**。 | 见 `PRD-M3` §1.1 S-A2｜`BRD` F-15｜`M5` TC-I-M5-002 | — |
| TC-A-M3-003 | L4 | F-14 | S-A3 旅程线索归纳：查证事实集合 | 按「谁在什么环节遇到什么现象」组织线索；每条线索均有人群或环节归属（不罗列裸变化）。 | 见 `PRD-M3` §1.1 S-A3｜`BRD` F-14 验收（关联到具体人群/旅程） | — |
| TC-A-M3-004 | L4 | F-16 | S-A4 机会整理与去重：结构化线索＋已有机会摘要 | 对照已有机会判重；组装六要素；标未知项（二态，见 `TC-D-M2-005`）；输出机会记录或缺口记录；**没有足够依据也是合法产出**。 | 见 `PRD-M3` §1.1 S-A4｜`ADR-003`｜`BRD` F-16 | — |
| TC-A-M3-005 | L4 | F-15 / F-16 | **边界·missing-field**：输出缺某字段 | 缺字段按**只松不严不硬映射**——缺失键 / 查不到已过审素材时**退回**请补，而非静默回退或编造。 | 见 `PRD-M3` §1.1 公共业务指令（引用证据须带来源/条件/时点/适用范围）｜`BRD` §7.3 | ⚠️待确认(数据集) |
| TC-A-M3-006 | L4 | F-15 | **边界·price-conflict（倒挂隔离模式）**：证据信息时点晚于取数时刻 | 视为倒挂 → **商品/实体级隔离**（仅该证据失效），**非整包失败**；其余证据照常参与。 | 见 `ADR-003` 二态精神（不污染全量）｜`BRD` §7.4（失败不否定结论） | ⚠️待确认(数据集) |

## 5. 平台 / 集成（L5）

| 用例ID | 层级 | 验收(F-xx) | 输入 | 期望输出 / 断言 | 引用(见…) | 标记 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| TC-I-M3-001 | L5 | F-17 | 机会发现 → HVA 分析衔接 | 经**人工节点**（M1 F-03/F-04，PM 选机会、提研究问题）转交；**不自动交接**；无人工节点时 M4 不被调用。 | 见 `PRD-M3` F-17 验收（衔接经人工节点）｜`BRD` §6（人工节点）｜`dev-plan.md` 阶段 4 | — |
| TC-I-M3-002 | L5 | F-15 | 查询链路 | 所有外部查询经 M5 工具执行程序，**只读**、不调生产写接口（BRD §5.3）。 | 见 `BRD` §5.3｜`PRD-M5` F-24｜`M5` TC-I-M5-001 | — |
| TC-I-M3-003 | L5 | F-13 | 五查口径（来源/适用范围/时点/已有机会/缺口） | 对现象的解释一律标明「尚未验证」；支持「值得进一步研究」，不给完整人群对照与 HVA 结论。 | 见 `PRD-M3` §1.1 角色指令（结束条件）＋公共业务指令（初步解释标明性质）｜`BRD` F-15 | ⚠️待确认(§7-T21) |
