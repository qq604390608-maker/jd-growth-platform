# 2026-09-19 · 工作日志

> 承接 `2026-09-18.md`（同日跨零点，schema / 三项锁定 / D1 探针的工作都在那份里）。

## 任务：为 schema.md §12 的 Q-03 / Q-04 / Q-05 出决策请示包

**产出**：`docs/07-decisions/DEC-PACK-001.md`（412 行）＋ `docs/07-decisions/README.md`（枝杈）＋ 登记进 `docs/README.md`。

**明确边界（用户原话）**：只出请示包**不建 ADR**；**不自行选定方案**；**不要先改任何文件**；禁止改 `schema.md` / `tech-stack.md`；禁止用"建议采纳"代替"待我裁决"。→ 已全部遵守，**上游三份锁定 mtime 未变**（23:18–23:19）。

### 关键事实（本任务新查证的，可复用）

**Q-03（business_context 与 goal_version 联动）**
- **"目标更新后研究保留启动时目标与范围"由 `MD-02` + `MD-07` 保证，与 `business_context` 无关**——证据：`schema.md` L176（MD-02「目标更新不改旧行」是 F-01 的存储基础）+ L280–L281（`research.goal_id` / `goal_version_no` 启动时快照）。而"目标和范围"落在 MD-02 的 `business_goal`(①)/`business_scope`(③)/`focus_period`(④)，**背景不在六要素内**。
- 真缺口：`MD-04` 无版本（只有 `created_at` + `is_active`）；近似手段 `PD-06 context_injection`（`CONTEXT_TYPE` 字典确有 `background`）但**只指回行、指不回内容快照**。
- 结构性问题：`MD-04.goal_id` **可为空**（平台级背景）→ 任何"挂目标版本"的方案都给平台级背景留了例外口。
- **原型零覆盖**：`data.js` 的 `goal` 只有 `fields`/`materials`/`versions`，无背景结构；全库检索"背景/context"在 prototype 零命中。

**Q-04（§8.3 四条命名）—— 四条候选名全部 0 实例**
- `research_proposal` 3 处（全在 schema.md：L112/L341/L806）｜备选 `research_request` 0
- `followup_message` 4 处（L126/L494/L807/L892）｜备选 `research_dialogue` 0
- `goal_check` 2 处（L808/L825）｜备选 `metric_check` 0
- `dict_type`/`dict_item` **18 处 / 5 文件**（schema 11 ｜ tech-stack L234 ｜ external-deps L395+L426 ｜ db/probes/type/README L427/428/463 ｜ db/probes/collation/schema.sql L7）｜备选 `code_table`/`code_value` 0
- **判据**：零实例 → 改名都不断链；成本差异在"当前名被谁引用"。**只有第 4 条跨文件**，且波及 2 份**已落盘的实测证据**（探针文件不能回改，只能标注）。
- 连带发现：`OBJECT_TYPE` 字典值含 `proposal`（L833）→ 第 1 条若改表名而不同改字典值，就违反"同一概念唯一命名"。**第 3 条风险最高**：`metric_check` 会把语义收窄（CFG-05 校验的是六要素多字段，不止指标）。

**Q-05（机会六要素必填 / unknown_item）**
- **现状：MD-06 六要素字段全部标 `N`，但 `NOT NULL` 不拦空串（D1 实测）→ 实际一条都没真正强制非空**，"必填"要落地**必须加 CHECK**。
- **原型 8 条机会（OPP-014/013/012/011/010/009/006/005）的 `unknown` 全部非空**，无空串 → "该不该允许空"**原型未给反例**。
- 课程口径两句话**本库查无原文**（我按转述处理并标注来源）；最接近落点：**BRD L195（F-15「必须标明尚未验证的部分」）** 与 **BRD L292（F-28「六要素齐全」）**。
- **结论（供参考的核心判断）**：两条口径**对象不同、本身不冲突**；冲突只在"把未知项强制成必须有实义未知"时发生（= 把"允许不全"改写成"必须编造不全"）。**写法三（`NULL`=未评估 / 空串=已评估且确无）是唯一同时满足两条的写法**，且其技术前提已被 D1 实测证实（空串与 NULL 可区分）。

### 查出的上游缺陷（未修，已写进请示包 §5）

1. **`schema.md` §12 Q-05 措辞与 MD-06 表定义不自洽**：§12 写「当前 `unknown_item` 可为空说明」，表里标 `N`。
2. **`external-deps.md` L177 / L425 引用不存在的 `OPP-015`**（原型只到 OPP-014 且集合含 005/006/009）——该处实指 **OPP-011**（`data.js` L252 的 `unknown` 原文一致）。属**悬空引用**，F-09/F-15 证据回查会查无所指。
3. **仓库仍有并发会话**：`db/probes/collation/`（6 份 raw + schema.sql + worker）为本会话之外新建，其 schema.sql L7 引用了 `dict_item` → 影响 Q-04 第 4 条的实例数时效。

### 用户偏好（本轮新增）

- 要**"请示包"而不是"结论"**：每个待决项必须给「选项 + 推荐 + 置信度 + 缺口」，但**不替他选**、`【待我裁决】` 留空。
- 要**"为什么现在必须定"具体到 migration 文件 / 引用卡 / 原型页面字段**——不接受"影响面较大"这类空话。
- 要**「反向验证」**（选了这个方案哪条功能点/口径会做不成），**找不到必须说"无"，不许编**。
- 要**零实例项单独标出**（改名成本最低）。
- 明确区分 **DEC-PACK（裁决前，不改文件）** 与 **ADR（裁决后，才改上游）**。

### 环境坑（沿用 09-18 的记录，重点重述）

- 本环境 shell 的 `grep` 对含**全角字符或复合转义**的 pattern **静默失败**（返回空 + exit 1），极易误判成"改动没生效"；`zsh` 下 `--include=*.md` 会报 `no matches found`。→ **复核一律用 `awk`（`index()` / `print NR`）或 Grep 工具**。
- 本次用 `awk` 复核了全部行号与实例数，未再踩坑。

## 追加：按 PM 指令把三处【待我裁决】填入推荐值

**用户指令**：「待我裁决值，给我填入推荐值」——**授权填入**，但填入的是"预填"不是"已裁决"。

**填入结果**（DEC-PACK-001 v1.0 → **v1.1**，412 → **464 行**）：

| 事项 | 预填取值 | 置信度 |
| ---- | ---- | ---- |
| Q-03 | **B · 不联动**（责任归属写进 `CFG-06`/`PD-06` 口径；`MD-04` 零结构变更） | 中 |
| Q-04 | **四条全部维持现名**（`research_proposal` / `followup_message` / `goal_check` / `dict_type`+`dict_item`） | 中（第 3 条**低**） |
| Q-05 | **C · `NULL`/空串二态**；「齐全」＝六项都被评估过 | 中 |

**处理"预填 ≠ 裁决"的手法（可复用）**：
1. 状态统一标 **`⬜ 预填待落章`**（不写"已决"），并明确"PM 改动任一行即为最终裁决"。
2. 每条预填**带上§x.4/§x.5 列出的前置核对条件**——核对结论若与推荐相反，**该预填作废**。这样填了值也不把 PM 绑住（Q-03 与 Q-04 第 3 条尤其重要，后者置信度只有低）。
3. 文档卡 / §0 说明 / 反向清单 **三处措辞必须同步改写**，否则文档自相矛盾（原文写着"【待我裁决】一律留空"）。
4. 新增 **§0.1 预填速览表**（三项 × 预填值 / 置信度 / 落章后触发的上游改动），便于落章时一眼核对。
5. **上游一字未改**——`schema.md` §12 三行仍为「未决」，因为**落章才触发**"改上游 + 升 ADR + 回填 §12"三步。反向清单里加了一段「预填与裁决的边界」说明这是有意留痕。

**连带同步**：`07-decisions/README.md`（目录表状态 + 「谁拍板」行 + 反向清单）、`docs/README.md`（目录表状态 + 反向清单行）。

**顺手修的缺陷**：DEC-PACK-001 原有两处 HTML 实体转义残留（L208 `&#x5219;`→"则"、L224 `&#x5176;`/`&#x8FD9;`→"其"/"这"）——是上一轮 Write 时产生的，已清除（复核 `&#x` 计数 = 0）。

**复核数字**：六段结构各 3 处齐全；`⬜ 预填待落章` 5 处；禁用措辞（已采纳/建议采纳/即生效/已定）全 **0**；`&#x` 残留 **0**；上游三锁定 mtime 仍为 23:18–23:19（未动）。

**教训（新）**：Write 重写中文长文档时会产生 `&#xNNNN;` 形式的实体转义残留（出现在中文词内部，肉眼易漏）。**重写后必须跑一次 `awk 'index($0,"&#x")'` 自检**。

## 追加：修复 `external-deps.md` 的 `OPP-015` 悬空引用

**用户指令**：「external-deps.md L177 / L425 引用了不存在的 OPP-015…实际指的是 OPP-011——F-09/F-15 的证据回查会因此查无所指。修复」

**做了什么**：
- `external-deps.md`：两处 `OPP-015` → `OPP-011`（用 `replace_all` 一次改完，避免同文件多 Edit 静默丢改）；文档卡版本 **v1.0 → v1.1**，版本行写明修正内容 + 依据指向 `DEC-PACK-001.md` §5.2。
- `docs/03-locks/README.md`：目录表版本标注 `v1.0 草稿` → `v1.1 草稿`。
- `DEC-PACK-001.md`：把"该缺陷存在"的**4 处过期断言**全部翻成已修状态——§4.1 行 2b（`需要独立修` → `✅ 已修 2026-09-19`）、§5 标题（`（未修，供你决定）`→`（供你决定）` + 加状态更新块）、§5.2 小节标题与正文（追加 `✅ 已修` 记录）、§3.6 与 §4.3 的现在时表述改为"曾引用…已更正"、反向清单上游表补更正状态。

**审计（可复用的脚本思路）**：写了 `/tmp/id_audit.py` —— 把 `data.js` 里出现过的 ID 当"已定义"集合，再扫 `docs/**` + `prototype/**`（除 data.js、除 .trash）统计各类编号的**悬空引用**。

**审计结论**：修复后**活引用零悬空**。OPP/EV/ST/R/T\*/Q\* 全绿；唯一另一处告警 `GOAL-2026Q3 ← goal.html` 是**正则误报**（L549 是运行时拼接 `"GOAL-2026Q3-" + pad(seq)` 的前缀模板）。

**命名空间提示（潜在，未处理）**：`T-xx` 被两套体系共用——**技术文档的「待确认条目」编号**（`T-01`~`T-32`，2 位）与**原型任务 ID**（`T-1008` 等，4 位）。位宽可区分，但同前缀会在 grep 审计时互相污染，值得留意。

## 追加：按 `DEC-PACK-001` 裁决回写锁定文件并收口

**用户指令**：6 条硬要求（逐条落改 / 每项裁决一份 ADR / 双向引用三件套 / 四项一致性逐条核对 / 两文件 bump 版本并归档 / 输出【DDL 开工检查单】）；禁止顺手补业务 migration、禁止把 ADR 写成流水账、禁止跳过逐条核对。

**改了什么（按文件）**：

- `docs/03-locks/schema.md` **v1.1 → v1.2**，旧版归档 `.trash/schema.md-v1.1.md`（归档前比对 SHA 一致）。改动点：`MD-06.unknown_item` `N`→`Y`（二态 NULL/`''`）+ 六要素加 `CHECK (length(trim(x)) > 0)`；CFG-06 / PD-06 写 Q-03 口径；§8.1 加 `unknown_item` 行；§8.3 四条标「未采纳」并保留第 3 条前置条件；§0.6 加 `opportunities[].unknown → unknown_item` 映射；§12 Q-03/04/05 转 `✅ 已决 2026-09-19`、Q-07 标注「开放…不阻塞建表但阻塞 MD-14 种子」；尾部上游加 `probes/type` + `probes/fk`、反向加 `ADR-001~003` 与两份探针。
- `docs/03-locks/tech-stack.md` **v1.0 → v1.1**，旧版归档 `.trash/tech-stack.md-v1.0.md`。改动点：§3.1 回填类型/长度实测 + `boolean`/`real` 补测行 + `datetime` 改判 **NUMERIC 亲和**（含两条静默出错写入方式）；§3.2 中文排序升级「已实测证实」+ 新增「无拼音排序规则」行；§3.3 外键（T-14）与 `CHECK` NULL 行为；§8 的 T-11/T-12/T-14 状态更新；头部/尾部引用卡同步。
- **新建** `docs/07-decisions/ADR-001-业务背景不与目标版本联动.md` / `ADR-002-次级命名维持现名.md` / `ADR-003-机会六要素必填与未知项二态.md`，统一六段结构（问题/备选/最终/权衡/影响面/回滚代价），并各引 `DEC-PACK-001` 对应小节。
- `docs/07-decisions/DEC-PACK-001.md` **v1.1 → v1.2**：11 处「预填待落章」翻成「已裁决/已升 ADR/已回写」。
- `docs/07-decisions/README.md`（重写）、`docs/03-locks/README.md`、`docs/README.md`：版本标注与枝杈表更新。
- `db/probes/README.md` / `db/probes/type/README.md` / `db/probes/fk/README.md` / `db/probes/collation/README.md`：**反向清单里过期的「⏳ 待回填」翻成实际状态**（type/fk = ✅ 已回填；collation = 🟡 部分，其 §6「字典 `ORDER BY order_no` 零改动规避」尚未被设计文件收录）。顺手修掉「两份探针」→「三份探针」、`db/README.md` 的过期缺口条目。
- `db/README.md`：新增 **【DDL 开工检查单】**（5 项走查 + 放行判定表），修过期的「T-11/T-14 尚未回填」条，补 `collation/` 与 `ADR-001~003` 上游。
- `external-deps.md`：**经核查不受牵连**（只在 L395/L426 提到 `dict_item`，Q-04 维持现名，无需改）。

**四项一致性核对结论（逐条）**：
- **4a** §8.1 命名唯一性对照表**无失同步**——Q-04 的四条现名本就在表内；Q-05 新增的 `unknown_item` 行已补。CHECK 是约束不是命名，不产生新行。
- **4b** §7「不建表清单」**未被突破**——Q-03 给 MD-04 加的是 **0 个字段**；Q-05 加的是 CHECK 不是外部数据列。`§7` 一字未动。
- **4c** §0.6 原型映射表**需补一条**（已补）：`opportunities[].unknown` → `unknown_item`，并注明**原型只有「有未知项」一态，落库按 ADR-003 补「未评估 NULL / 已评估且确无 `''`」**。**原型本身未改**（宪法：prototype 是冻结证据）。
- **4d** 残留扫描：**`TBD` = 0 处**；`待确认` = 40 处、`待定` = 6 处，**逐条列位置后判定全部合法**（`tech-stack.md` §8 待确认清单 11、`external-deps.md` §7 17、`ADR-003` 未闭合项 2、两份 README 的版本注 3、工作日志 6、`schema.md` §12 Q-07 1）。**未擅自清理**，留用户决定。

**踩到的坑（新）**：`grep -n "A\|B"` 与 `--include=*.csv` 这类写法在本机 zsh 下**静默失败/报 no matches found**——本轮回合又踩一次（核对 tech-stack 三探针行时 grep 返回空，实际行存在）。**结论：一律改用 Grep 工具，或 `awk`/`for f in ...; do grep ...; done`。**

**给未来自己的提醒**：探针 README 的「反向清单」是**最容易过期**的地方——上游锁文件一改，探针那侧的 ⏳ 就成假话。收口时**必须反向扫一遍 `db/` 里所有 `⏳/⬜ 待回填`**，不能只看自己改过的两个锁文件。

## 追加：修宪法索引状态失实（宪法升 v2）＋ 纠正我自己的一个误报

**用户指令**：「缺陷按照你推荐的执行」。

**⚠️ 先纠正我上一轮的错误**：我上轮说「宪法标题写『两条硬红线』正文只有 1 条，仍未处理」——**这是错的**。打开文件一看，`AGENTS.md` 磁盘上是 **v1**（mtime 2026-09-18 21:52），第 29 行已是 `## 一条硬红线`，第 7 行 v1 变更记录第 ② 条明确写了「硬红线标题由「两条」收敛为「一条」」。**根因：我读的是系统预注入的 `<project_guidance>` 缓存副本（那是 v0 的旧内容），没有用工具打开真文件核对。** 这正是我 USER 记忆里「预注入的上下文 ≠ 已核对」那条的又一次复发，而且这次是**在已经知道这条规则的前提下**犯的。以后凡是要对某文件「提缺陷」，**必须先 Read 该文件**，禁止凭注入块下结论。

**真正的缺陷（本轮修的）**：`AGENTS.md` 索引表「状态」列失实——`docs/` 与 `db/` 都标「待建」，实际都已有产物。该缺陷在**两处**被显式登记为「未擅自改」：`DEC-PACK-001.md` §5.4 第 1 条、`db/README.md` 已知缺口 1。

**改了什么**：
- **`AGENTS.md` v1 → v2**（旧版归档 `.trash/AGENTS.md-v1.md`，SHA 已比对一致）：`docs/` 行 → `🟡 部分：BRD／三项锁定／决策留档 已有；业务背景、主子 PRD、开发计划、测试用例、runbook 待建`；`db/` 行 → `🟡 部分：probes/ 已有；migrations/、seed/ 待建`；新增**状态取值说明**一行 + **工具层目录（`.workbuddy/`、`.kilo/`）不列入索引**的说明一行。
- **`docs/03-locks/schema.md` v1.2 → v1.3**（旧版归档 `.trash/schema.md-v1.2.md`）：因 §0.7 里「`AGENTS.md` 现为 v1」是**现在时断言**，宪法升 v2 后它必然失实，必须改；改了就要有版本戳，故 bump。**v1.3 明确标注「纯交叉引用校正，表数量/字段/类型/约束一律未动，下游按 v1.2 建表无需返工」**。同批同步 6 处「钉版本」引用（两份 README + 三份探针 README + tech-stack 反向表）。
- **`DEC-PACK-001.md` v1.2 → v1.3**：§5.4 第 1 条翻为「✅ 已于 2026-09-19 修」；§5.4 标题去掉「但本次未处理」并加状态行（第 2 条 BRD 路径/编号目录体系**仍未决**，属 T-30/T-31）。
- **`db/README.md`**：上游卡「注意：宪法索引里 db/ 仍标待建」→ 改为「v2 已校正」；已知缺口 1 翻为已修。
- **`docs/README.md` / `docs/03-locks/README.md` / `docs/07-decisions/README.md`**：版本戳同步（schema v1.3、DEC-PACK v1.3）；07-decisions 的上游补上遗漏的 `db/probes/collation/`。
- **`docs/07-decisions/README.md` 新增一条维护规则**：「上游版本钉版不回溯」——ADR 里写的「宪法 v1」是撰写时点的钉版，**不回溯改**；上游漂移由上游自己的变更记录承担；判据是「新版是否动了 ADR 依赖的口径」。

**我立的两条判据（供用户否决）**：
1. **什么时候该 bump 版本**：① 规范性内容变了；或 ② 文件里**对上游当前版本的现在时断言**变了。纯粹的反向清单/状态标签翻转**不 bump**（与上一轮回填探针状态时未 bump 探针版本的做法一致）。
2. **ADR 的上游版本钉版不回溯**（理由如上）。故 8 处「宪法 v1」（ADR-001/002/003 各 2 处 + DEC-PACK 2 处）**我没动**。

**又一次踩坑**：`grep -rn "A\|B"` 在本机 zsh 下**静默返回空**（这次骗到我了——我以为「宪法 v1」已无残留，换 Grep 工具才发现有 8 处）。**结论再强化：审计类检索一律用 Grep 工具，不用 shell grep 的多分支模式。**

## 追加：待确认/待定 全量清点（回答用户「在哪？」）＋ 新发现一处编号撞号

**清点结果（2026-09-19，按文件+行号）**：
- **实体待确认条目 = 37 条**，集中在三处：`external-deps.md` §7 共 **21 条**（§7.1 数据源类 T-01~T-10；§7.2 Agent 能力类 T-20~T-24；§7.3 交付基础设施类 T-25~T-27；§7.4 文档与路径类 T-30~T-32）、`tech-stack.md` §8 共 **13 条**（T-10~T-22）、`ADR-003` §5 未编号 **2 项**、`schema.md` §12 **Q-07** 1 项。→ 21+13+2+1 = 37。
- **字符串行数 ≠ 条目数**：`待确认` 57 行（活跃文件 37 + `.workbuddy/memory` 7 + `.trash/` 13）；`待定` 9 行（活跃 4 + memory 3 + `.trash/` 2）。**`.trash/` 里的 13+2 行是归档冻结证据，绝不能改**；`待定` 活跃 4 处 = `tech-stack.md` L293(T-16 截断策略) L364(mock/ 归属) + `external-deps.md` L460(T-23 agent.md 真实形态) L473(T-27 归属)。
- 其余 `待确认` 行是**说明性提及**（"某文件含待确认清单"）或**正文里指向清单的 ⬜ 标记**，不是空位。

**🔴 新发现的缺陷（未擅自改，已当面报告）**：**`T-xx` 编号在两份锁定文件里不是同一个命名空间，且已撞号 4 处**——
- `external-deps.md` 的 T-10/T-20/T-21/T-22 与 `tech-stack.md` §8 的 T-10/T-20/T-21/T-22 **编号相同、事项完全不同**（例：T-21 在 external-deps 是「LLM 推理服务：模型/上下文窗口/并发上限」，在 tech-stack 是「平台限制数字的复核」）。
- 更刺眼的是 `tech-stack.md` L417 刚说完「`external-deps.md` §7 的 T-01~T-10 是本文件的前置」，紧接着它自己的清单就从 **T-10** 开始——直接与前一句点名的区间撞上。
- 现有缓解：所有跨文件引用都**带了文件名前缀**（如 `tech-stack.md` §8 T-10），故当前**不构成实际误引**，属**易错点/潜在缺陷**而非既成错误。
- 加剧因素：`T-xx`、`DS-xx`、`Q-xx` **都不在宪法「编号体系」那一行里**（宪法只列了 M/F/REQ/R/S/PRD/ADR）。宪法明说「编号是主键」，而 `T-xx` 跨两份文件却不共用空间。
- 值得注意：`tech-stack.md` §6 把 `scripts/` 定位为「引用自检脚本（孤儿 / 悬空 / **重号**）」——**本次这 4 处正是该脚本该抓的「重号」**，而 `scripts/` 尚「待建」。→ 说明这条自检的必要性已被项目自己预见。
- **修复选项（待用户裁决）**：(A) `tech-stack.md` 的 13 条改 `TT-xx` 或 `TS-xx` 前缀；(B) 接续到 `T-33` 起（保留单一空间）；(C) 维持现状，只把「引用须带文件名前缀」写成硬规矩。**我倾向 (B)**（保住"一个空间一个编号"，改动面最小且不引入第三套前缀）。

**给未来自己的提醒（本轮新增）**：清点残留时，**必须把 `.trash/` 和 `.workbuddy/memory/` 从"待处理"里显式剔除并说明**——否则报出的数字（57/9）会吓人，而其中 15 行根本不该动。

## 追加：执行编号撞号修复（用户选 A）→ `T-xx` 改 `TS-xx`

**用户指令**：「A」（即上轮待裁决的修复选项 A：`tech-stack.md` §8 的 13 条 `T-10`~`T-22` 改 `TS-10`~`TS-22` 前缀）。

**做了什么**：
- **`docs/03-locks/tech-stack.md` v1.1 → v1.2**，旧版**重建为真 v1.1 归档**（`.trash/tech-stack.md-v1.1.md`）：因为已在位改完才发现没先归档，于是 `cp` 当前文件后用 `perl` 反向还原 `TS-xx`→`T-xx` 13 处，得到与「改前」逐字一致的 v1.1（校验 `TS-` 残留 0、`T-10` 在）。**教训：先归档再改，别反着来。** 版本行写明「v1.2 引入 `TS-` 前缀区分 `external-deps.md` §7 的 `T-xx`，消除 4 处撞号 T-10/T-20/T-21/T-22」；L453「截至 v1.1」→「v1.2」。
- **重命名 13 条**：`T-10`~`T-22` → `TS-10`~`TS-22`。其中 `T-10`(L203/L403)、`T-21`(L414)、`T-22`(L415) 因文档内同时引用了 `external-deps` 的同号（`T-01~T-10` / `T-21` / `T-22` / `T-04`），**不能用 `replace_all`**，逐个定点改；其余 `T-11`~`T-20` 用 `replace_all`（无外部撞号）。
- **全仓同步引用**（指 tech-stack §8 项的）：`docs/README.md`(TS-11/TS-14)、`docs/03-locks/README.md`(TS-11/TS-14 + v1.2)、`docs/03-locks/schema.md`(L941/952/956 的 T-14/T-11)、`db/README.md`(检查单里的 TS-11/TS-12/TS-14 + v1.2)、`db/probes/README.md`(T-11/12/14 + v1.2 ×2 处)、`db/probes/type/README.md`(TS-11/TS-14 + v1.2)、`db/probes/fk/README.md`(TS-14 + v1.2)、`db/probes/collation/README.md`(TS-12/TS-11 + L11 的 `（T-14）`→`（TS-14）` + v1.2)、`external-deps.md`(L463 的「§8 T-10」→「TS-10」、L473 的「§8 T-18」→「TS-18」)。
- **保留未动**：`external-deps.md` §7 的 `T-01`~`T-32`（含与 tech-stack 撞号的 T-10/T-20/T-21/T-22，事项不同，是另一套编号，维持）；原型任务 ID（`T-1023`/`T-1008` 等四位）不属于确认项命名空间。

**踩坑（又一处漏改）**：T-14 这轮我漏了 `docs/README.md` L26 与 `db/probes/collation/README.md` L11（这俩文件没进 T-14 的 `replace_all` 批次），导致它们还留着裸 `T-14`。全仓 `T-1[0-9]|T-2[0-2]` 复扫才发现，已补。→ **教训：跨文件批量 rename 后，必须跑一次全仓 `T-1x|T-2x` 裸号复扫**，不能只信分批时的 grep。

**是否 bump 探针/README 版本**：按上轮立的判据②（纯反向清单/标签翻转不 bump），探针 README 只改了反向清单里的 `T-xx`→`TS-xx` 与版本钉版文字，**未 bump 它们自身版本**；只有 `tech-stack.md` 因标识符是规范性内容而升 v1.2。

**尚未闭环（待用户裁决）**：`TS-xx` / `T-xx`(external-deps) / `DS-xx` / `Q-xx` 仍**不在 `AGENTS.md` 编号体系那一行**。本次只消了撞号，没把命名空间写进宪法。要彻底闭合"编号是主键却不登记"的缺陷，需 bump `AGENTS.md` 到 v3 把这几套前缀登记进去——**未擅自做，留用户定**。

## 追加：BRD.md 迁入 docs/01-brd/（二级目录，全量反查）

**用户指令**：「BRD.md 看起来也需要放在 docs 的二级目录下，如果我直接改目录，是直接移还是需要反查关联文件？」→ 我答"必须反查"，用户选 `01-brd/` + "移动并全量反查"。

**做了什么**：
- `docs/BRD.md` → `docs/01-brd/BRD.md`（`mkdir` + `mv`；非 git 仓库，故用 `mv` 非 `git mv`）。**BRD 内部不用改**：L13 的 `../../AGENTS.md` 移动前指向"项目根父级"（本就断），移入二级目录后 `../../` 恰好回到项目根，**反而修对了**；L354 的 `docs/README.md` 不依赖自身位置，不变。这印证作者写 BRD 时就按二级目录设计的引用。
- **文档体系（*.md）10 文件、约 16 处相对路径**：子目录文件的 `../BRD.md` → `../01-brd/BRD.md`（03-locks/README、external-deps、tech-stack、schema、07-decisions/README、ADR-001/002/003）；`prototype/README.md` 的 `../docs/BRD.md` → `../docs/01-brd/BRD.md`。
- **前端原型（只扫 *.md 会漏掉，已补）**：prototype 下 9 个文件也引用 BRD——`pages/*.html`(5 个：result/goal/followup/propose/opportunities) + `assets/{app.js,base.css,data.js}`(3) 用 `../../docs/BRD.md` → `../../docs/01-brd/BRD.md`；`index.html` 用 `../docs/BRD.md` → `../docs/01-brd/BRD.md`。`tasks.html` 虽含 "BRD" 字样但非路径引用，未动。
- **索引/描述同步**：`AGENTS.md` L14 索引加 `01-brd/` 注；`docs/README.md` 目录表 `BRD.md`→`01-brd/BRD.md` + 反向说明两处；`external-deps.md` **T-30** 翻「✅ 已解决 2026-09-19」（原路径分歧消除）、**T-31** 描述更新为"现为 01-brd + 03-locks + 07-decisions，跳号缺 02/04/05/06 仍待定"；`DEC-PACK-001.md` L437（"第 2 条"改"部分解决：T-30 已决、T-31 仍待定"）+ L440（路径描述改写）。
- **BRD 不 bump 版本**（内容未变，仅位置变）；移动未触发任何锁文件版本 bump（纯路径校正，非规范性内容变更）。

**最终核对**：全仓 `../BRD.md` 裸号 = 0；`docs/BRD.md`（非 01-brd）残留仅 `external-deps.md` T-30 的"实际曾为 `docs/BRD.md`"历史记录（保留，说明分歧来源）；新路径引用计数正常（md 11 文件 + prototype 9 文件）。

**🔴 新教训（重要）**：全量反查一个被多文件引用的文档时，**只扫 `*.md` 不够**——本项目 prototype 的 html/js/css 也用相对路径引用兄弟文档。正确做法是 grep 全仓库（不限扩展名）`docs/BRD.md` 与 `../BRD.md`，把前端也纳入。这次先扫 md 漏了 9 个前端文件，复扫才发现补上。

**遗留（未擅自动）**：T-31 编号目录体系缺 02/04/05/06 层仍是开放项（已记入 external-deps T-31 待定）；`TS-xx` 等前缀仍未登记进宪法编号体系。

## 追加：进 db/ 落地（0001_init.sql + 0001_mock.sql）

**用户指令**：「现在可以进 db/ 落地了——迁移脚本 + 全 mock 种子数据（36 张表 + 12 个工具 + 25 组字典）。」两处未决项此前已裁决（AskUserQuestion）：**T-11 = 应用层校验**（不写库级长度 CHECK，仅留 Q-05 六要素 `CHECK(length(trim(x))>0)`）；**Q-07 / MD-14 = 建表不种**（skill_registry 照建表、0 行种子）。

**交付物**：
- `db/migrations/0001_init.sql`：36 表 DDL（MD-01~14 / PD-01~07 / CFG-01~08 / LNK-01~04 / EXT-01~03），含真实外键、`PRAGMA foreign_keys=ON`、`Q-05` 六要素 CHECK、§11 建议索引（10 条）。
- `db/seed/0001_mock.sql` + `db/seed/generate_mock.py`：生成器从 `prototype/assets/data.js` + `external-deps.md` §5 抽真实源，落 30 表有数据 / 6 表留空（`MD-14` 因 Q-07；`MD-12/PD-02/PD-04/PD-06/EXT-03` 原型无数据）。
- 反向引用三件套补齐：`db/migrations/README.md`、`db/seed/README.md` 新建；`db/README.md` 目录清单 / DDL 检查单 / 反向清单翻 ✅；`schema.md` §12、`external-deps.md` §8 的 `db/` 引用由 ⏳→✅。

## 追加：建 docs/02-prd 主子 PRD（M1-M6）

**用户指令**：「基于项目目录下已有材料（BRD、前端原型、字段 schema、外部依赖、技术栈…），构建主 PRD 和子 PRD；主 PRD 只做索引不写实现细节；子 PRD 一模块一份（M1-M6），放入 docs 二级目录 02-prd。」

**交付物（8 文件）**：
- `docs/02-prd/PRD.md`：主 PRD，**只索引**（M1-M6 表 + 一句话 + 链接子 PRD；范围/术语/验收均指针到 BRD 与三项锁定，不展开实现）。
- `docs/02-prd/PRD-M1.md`~`PRD-M6.md`：六份子 PRD，各含 文档卡(上游) / 模块目标 / 范围 / 功能需求(逐 F-xx：功能描述继承 BRD + 输入→输出 + 验收要点 + 关联 schema 表 + 关联工具或原型页 + 依赖衔接) / 关键约束与红线 / 验收总则 / 反向清单。
- `docs/02-prd/README.md`：目录枝杈索引（7 文件清单 + 状态 + 引用约定 + 反向）。

**模块→表/工具映射（已落子 PRD）**：M1=MD-01/02/03,MD-12,PD-01/02/03/04/05/07,CFG-04；M2=MD-04/05,CFG-01,EXT-02,MD-06/07/08,LNK-01/02/03/04,PD-06,CFG-06；M3=MD-06,MD-13,MD-14,LNK-03,CFG-05；M4=MD-07/08/09/10/11,MD-12,MD-13,PD-07；M5=CFG-02/03/04/05,EXT-01/03 + 12 工具 TOL-01~12；M6=六页面(goal/opportunities/opportunity-detail/propose/result/followup/tasks) + MD/PD 各表读。

**双向引用回写**：`docs/README.md` 加 `02-prd/` 行(✅ 已建，业务背景仍待建)；`AGENTS.md` L14 docs 行把「主子 PRD」由待建翻为已建（与 db 状态修正同模式，属状态登记非缺陷修复）。
**⚠️ 顺带发现未擅改**：`AGENTS.md` L19 `db/` 行仍为「🟡 部分：probes/ 已有；migrations/、seed/ 待建」——db 实际已全建，此行失实。按"宪法缺陷先登记不擅改"原则，未动，待用户点头再翻（与 T-31 编号目录缺 02/04/05/06 同列遗留）。

**🔴 关键坑（已修，可复用）**：
1. **列名不符**：首版生成器给 `run_policy` 写了 `registered_at` 列（来自一次失败的 self-fix 残留），但 `schema.md` CFG-04 无此列 → 载入报 "no column named registered_at"。→ 以锁定 schema 为准，删生成器该列（DDL 权威，生成器不得臆造列）。
2. **外键插入顺序**：生成器首版按段顺序输出，`run_policy`(CFG) 排在 `research_goal`(MD-01) 前、`opportunity_evidence`/`finding_evidence`(LNK) 排在 `evidence`(EXT-02) 前 → `PRAGMA foreign_keys=ON` 下 "FOREIGN KEY constraint failed"。→ 改生成器按 DDL 外键做**拓扑排序**输出（自引用除外）。
3. **自引用行序**：`task` 表里 `T-1023.parent_task_id='T-1021'`，但数据里 T-1023 排在 T-1021 前 → 同表单 INSERT 内子行先于父行也 FK 失败。→ 数据里把父行 T-1021 调到子行 T-1023 前。
4. **replace_all 把 `parts.append(ins(` 换成 `emit(` 会丢一个左括号**（原 `parts.append(ins(...))` = 2 开 2 闭，换后 `emit(...))` = 1 开 2 闭）→ SyntaxError。→ 用「行首 `emit(` 且行尾 `))` 则删最后一个 `)`」的脚本定点修（注意多行 emit 如 `dict_item` 列表推导会漏，需另修）；合法 `str(int(v))` 等 `))` 行不能被误伤。

**校验**（python3 + sqlite3，managed runtime `/Users/dongzhuo/.workbuddy/binaries/python/versions/3.13.12/bin/python3`）：
- 载入 0001_init + 0001_mock（FK ON）：无语法/CHECK 错误；`PRAGMA foreign_key_check`=0；`integrity_check`=ok；6 处六要素 CHECK 无违例；30 表有数据 6 表空。
- 行数：dict_item 84 / dict_type 26 / tool_permission 24 / tool_registry 12 / query_record 11 / task_object 12 / opportunity_evidence 10 / behavior_point 10 / evidence 7 / task 7 / opportunity 8 / finding_evidence 8 / research_finding 6 / research_goal_version 6 等。

**注意**：用户口述「25 组字典」，但 `schema.md` §9 实为 **26 组**——以锁定文件为准落 26。`MD-14` 留空待 Q-07 落定后单独补种。

## 追加：关闭 Q-07（schema.md）

**用户指令**：「Q-07 在 schema.md 中先关闭」。→ 在 `schema.md` 两处收口：
- §12 决策行（原 L924）：状态由「未决 / PM 评审 / 开放：阻塞 MD-14 种子」改为「**✅ 已决 2026-09-19**：映射确认 `clue-scan`→`S-A1`(M3)、`hva-five-checks`→`S-B1`(M4)；MD-14 `skill_no` 据此赋值；原『阻塞 MD-14 种子』随裁决解除」。待办列：`MD-14` 种子待 `db/seed/` 补种（解除阻塞）。
- §0.7 冲突裁决表（原 L92）：「具体映射待确认（§12 Q-07）」→「映射已确认（§12 Q-07 已决 2026-09-19）」。

**范围限定（用户说"先"）**：仅动 `schema.md`。未动 `db/README.md`、`db/seed/README.md` 中仍写「Q-07 仍开放 / 阻塞 MD-14」的陈旧引用；未补 `MD-14` 种子。下一步候选：① 把 `db/` 两处陈旧 Q-07 引用翻为「已决」；② 在 `db/seed/generate_mock.py` 给 `MD-14` 补 2 行（`S-A1`/`S-B1` 绑定 AGP-DISC/AGP-HVA）并重跑校验。待用户点头再动。

**校验**：全仓 Grep `Q-07|未决|开放` 于 `schema.md` 已无残留「未决/开放」（仅剩 §12 节标题与已决行）。

## 追加：开发计划 docs/04-plan/dev-plan.md

**用户指令**：「创建本项目开发计划。顺序 M2 共享上下文 → M5 工具执行 → M1 任务程序 → M3/M4 Agent → M6 前端（依赖排序：先底层存储与查询，再调度，再研究能力，最后界面）。产出 docs/04-plan/dev-plan.md」。

**产出**：`docs/04-plan/dev-plan.md`（✅ v1.0）+ `docs/04-plan/README.md`（枝杈）。
**结构**：§2 依赖排序逻辑（ASCII 依赖图 + 逐对为何）；§3 阶段 0~5（阶段 0 公共底座含 db 已建/mock server/agent-runtime 骨架/CI；阶段 1 M2 / 2 M5 / 3 M1 / 4 M3+M4 / 5 M6），每阶段列 覆盖模块+F-xx / 涉及 schema 表 / server 模块 / 工具 / 关键交付物 / 验收要点 / 依赖前序 / 风险与待确认；§4 里程碑(相对工期)；§5 跨阶段横切(CI/D1 纪律/mock 贯穿/agent-runtime 落地/硬红线)；§6 风险与待确认(TS-10/T-01~T-10/F-20 最脆弱等映射)；§7 验收总则(前后端分离判断标准等)。

**双向引用回写**：`docs/README.md` 新增 `04-plan/` 行(✅ 已建，顺序 M2→M5→M1→M3/M4→M6)，原「（待建）开发计划…」改「（待建）业务背景、测试用例、runbook」；`AGENTS.md` L14 docs 行把「开发计划」由待建翻为已建（仅余 业务背景/测试用例/runbook 待建）；6 份子 PRD 反向清单各追加「被开发计划 ../04-plan/dev-plan.md 按依赖顺序(阶段 x · Mx)编排」一行，兑现其「下游预计被引用：开发计划」。

**自检**：`awk` 查 `&#x` 转义 = 0 行（中文长文档无实体残留）。

**标注**：dev-plan 不含代码级实现（归 server/ 五模块开发文档）；M3/M4 阶段明确「把 PRD-M3/M4 §1.1 的 agent 配置落成 agent-runtime/ 真实文件 + Q-07 已决后 MD-14 补种 2 行」为具体交付物。

## 追加：agent 配置注入 PRD（M3 / M4）

**用户指令**：「agent 部分的配置已经有了，请先阅读这部分内容，再将这部分内容，注入到 PRD 中」（内容以用户贴出的 long-text 为准；仓库内尚无 `business-rules.md`/`agent.md` 独立文件，故 long-text 即权威源）。
**注入位置**：`PRD-M3-机会发现Agent.md`、`PRD-M4-HVA分析Agent.md` 各在「## 1. 模块目标与定位」后新增「## 1.1 领域能力定义（agent.md · business-rules.md · Skill 清单）」，含三子节：① 角色指令（agent.md 本 Agent 段：职责/输入/工作方式/输出/结束条件）；② 公共业务指令（business-rules.md 两 Agent 共用，8 条，含硬红线"不调生产写接口"）；③ Skill 清单表。
- M3 注入 S-A1~S-A4（服务 F-14/F-15/F-16），并标注 Q-07 已决 S-A1＝`clue-scan`。
- M4 注入 S-B1~S-B4（服务 F-19/F-20/F-21），并标注 Q-07 已决 S-B1＝`hva-five-checks`。
- 公共业务指令在两份子 PRD 各放一份（模块文档自包含，标明"由 M1 运行程序加载"）。
- 不动现有 1~5 节编号（用 1.1 子节避免重排）。

**待确认（未擅建文件）**：用户说"配置已经有了"，但仓库内 `agent.md`/`business-rules.md` 并非独立文件。若需把这部分落成 `agent-runtime/` 下真实文件（与 PRD 反向引用一致），待用户点头再建。

## 追加：子 PRD 文件命名改造（加定位标签）

**用户指令**：「子 PRD，文件命名，除了有模块 M1 以外，还需要标注定位，平台任务程序」。→ 文件名 = `PRD-M{num}-{定位}`，定位内部空格压缩（文件名友好）。

**新文件名**：
- `PRD-M1-平台任务程序.md` / `PRD-M2-共享上下文.md` / `PRD-M3-机会发现Agent.md` / `PRD-M4-HVA分析Agent.md` / `PRD-M5-工具执行程序.md` / `PRD-M6-运营端工作台.md`
- 文档编号（标题 `# PRD-M1 · …` 与 文档编号表 `PRD-M1`）**不改**，仍作 doc ID；文件名与 doc ID 解耦。

**引用更新**：`PRD.md` 模块索引表 6 行链接（`./PRD-Mx-定位.md`）+ `README.md` 文件清单 6 行反引号文件名。全仓 Grep `PRD-M[1-6]\.md` 已无残留旧名（doc ID 不带 `.md` 的引用保留）。

## 追加：测试用例集 docs/05-test-cases/（白盒逐点验收，F-01~F-32 全覆盖）

**用户指令（测试用例编制员角色）**：基于已定稿规约与开发计划，产出可执行、可验收测试用例集；唯一输入白名单＝AGENTS(宪法)/BRD/PRD+6子PRD/schema/tech-stack/external-deps/dev-plan/ADR-001~003；产出 `docs/05-test-cases/`（00-索引 + test-M1~M6 + README）；5 层分层（L1单元/L2契约/L3约束/L4 Agent行为/L5平台集成）；强约束①一条规则一处定义②白盒逐点③生产零写④证据四要素⑤失败不否定结论；未决项 Q-07 标⛔阻塞、§7 用 stub 打桩标⚠️待确认；双向登记；账实核对；开工前 AskUserQuestion 一次问完 4 点；产出后自检。

**AskUserQuestion 抉择**：① 覆盖 M1~M6 全建；② 只出用例规约表（不连脚本骨架）；③ 未决项默认标阻塞+打桩；④ 输出目录 `docs/05-test-cases/`。

**交付物（8 文件，全建）**：
- `docs/05-test-cases/00-索引.md`：§0 账实核对声明（3 处分歧据实记录未擅改）＋ §1 分层 ＋ §2 编号规则 ＋ §3 用例↔F-xx↔工作项对照总表（86 条）＋ §4 阻塞与待桩台账（A.Q-07 已关不阻塞；B.§7 未关 20 条 T-01~T-27＋T-31＋T-32 失效，开门动作非门禁）＋ §5 双向登记 ＋ §6 文末自检。
- `test-M1.md`(M1 平台任务程序 F-01~06，16 条：L1×2 + L3×8 + L5×6；L2/L4 显式不适用)
- `test-M2.md`(M2 共享上下文 F-07~12，19 条：L3×15 + L5×4；含 TC-D-M2-005 引 ADR-003 unknown_item 三态、TC-I-M2-003 证据四要素 ⚠️§7-T07/08/09)
- `test-M3.md`(M3 机会发现 Agent F-13~17，13 条：L1×1 + L2×1 + L3×2 + L4×6(S-A1~S-A4 六步+missing-field/倒挂隔离边界) + L5×3；MD-14 用 S-A1 因 Q-07 已关)
- `test-M4.md`(M4 HVA 分析 Agent F-18~22，17 条：L1×1 + L2×1 + L3×6 + L4×6(S-B1~S-B4) + L5×3；含 TC-A-M4-005 失败不否定 ⚠️§7-T06)
- `test-M5.md`(M5 工具执行程序 F-23~26，12 条：L1×1(价格约束域外模板) + L2×1 + L3×5 + L5×5；L4 不适用)
- `test-M6.md`(M6 运营端工作台 F-27~32，9 条：L2×1 + L3×1 + L5×7；L1/L4 不适用)
- `README.md`(枝杈索引：8 文件清单 + 分层编号 + 引用约定 3 条 + 反向清单✅)

**合计 86 条用例**（M1×16/M2×19/M3×13/M4×17/M5×12/M6×9）。

**自检（python 脚本实跑，非声称）结论**：
- 唯一 DEFINED id = 86；**零重复定义**（无同 id 在 >1 文件出现）。
- index↔modules 唯一 id 对齐：**双向均空**（defined not in index = []；index not defined = []）。
- **F-01~F-32 全覆盖，无孤儿**（F coverage missing = []）。
- 早期一处错位已修：test-M3 误引 `TC-A-M5-002` → 实为 `TC-I-M5-002`（M5 工具行为归 L5），重跑校验后对齐。

**双向引用回写（全部落盘，已 Grep 复核）**：
- `AGENTS.md` L14 docs 行：`测试用例` → `测试用例（05-test-cases/）`，状态由"业务背景、测试用例、runbook 待建"改为"业务背景、runbook 待建"。
- `docs/README.md` L18：新增 `05-test-cases/` 行 ✅ 已建（86 条，F-01~F-32 全覆盖）；原"待建"三项去掉测试用例。
- `docs/01-brd/BRD.md` L353 反向清单：测试用例 → 测试用例（docs/05-test-cases/）（本轮补，前次因 old_string 误拼两行失败，本次拆行已修）。
- `docs/02-prd/PRD.md` L47 + 6 子 PRD（PRD-M1~M6，按真实文件名 `PRD-Mx-定位.md`）：各下游预计被引用加 `测试用例（../05-test-cases/）`。
- `docs/03-locks/schema.md` L951：反向引用表加 `../05-test-cases/（测试用例）` ✅ 已建（86 条，白盒逐点）。
- `docs/04-plan/dev-plan.md` L192：下游加 `../05-test-cases/（测试用例，✅ 已建 2026-09-19）`。

**3 处账实不符（用户指令预设 vs 上游实态，据实处理未擅改，全记 00-索引 §0）**：
1. Q-07 已于前轮在 schema.md 关闭 → 相关用例直接引用 S-A1/S-B1，**不标⛔阻塞**。
2. 用户要求对齐的"8字段外层+6步契约"在白名单(tech-stack §2.5 仅 json_schema、scripts//cases/ 均不存在)未命中，且 creative/review/deliver 与 BRD §5.3 硬红线冲突 → L2 仅断言可解析 JSON+硬红线禁输出，标 ⚠️待确认(契约名)。
3. 用户称"§7 共21条未关"，实际 external-deps §7 共 21 登记、T-30 已解决、T-32 因 db/ 已建失效 → 实际未关 20 条，台账据实列。

**遗留（未擅自动）**：BRD/PRD/schema/dev-plan 的反向清单已补；`AGENTS.md` L19 `db/` 行仍标"migrations/、seed/ 待建"失实（db 实际已全建），与 T-31 编号目录缺 02/04/05/06 同列，待用户点头再翻。

## 追加：收口 4 项待决策（用户全选推荐项）

**用户指令**：「待决策的，你现在和我对齐」→ AskUserQuestion 4 题全选推荐项。

**① AGENTS L19 `db/` 行翻正**（Task #28）
- `🟡 部分：probes/ 已有；migrations/、seed/ 待建` → `✅ 已建`。状态登记校正，不动上游内容。

**② T-31 编号目录跳号收口**（Task #29）
- `external-deps.md` §7 T-31：原为"跳号缺 02/04/05/06"，但该评定写于只有 01/03/07 时；现 01-brd/02-prd/03-locks/04-plan/05-test-cases/07-decisions 已全建，仅 06(runbook) 待建（docs/README 目录表已标"待建"）。翻为 **✅ 已解决**：跳号不成立（06 为 runbook 预留层），本项关闭。

**③ AGENTS bump v3 + 编号体系登记前缀**（Task #30）
- 旧版 `AGENTS.md-v2.md` 先归档 `.trash/`（先归档再改），再 Write 整体重写为 **v3**。
- L37 编号体系新增：`TS-xx`(tech-stack §8 待确认) / `T-xx`(external-deps §7 待确认) / `DS-xx`(决策请示) / `Q-xx`(schema §12 待决)。闭合"编号是主键却不登记"缺陷，并正式消解 external-deps§7 与 tech-stack§8 的撞号 T-10/T-20/T-21/T-22。
- `schema.md` §0.7「AGENTS.md 现为 v2」→ v3，编号体系镜像同步；§0.7 标题补"TS/T/DS/Q 前缀于 v3 补登记"。
- `db/README.md` L10 钉版「宪法 v2」→ v3（db/ 行二次翻正）。

**④ MD-14 补种 + 刷新 db 陈旧引用**（Task #31）
- `db/seed/generate_mock.py`：MD-14 由 0 行补 2 行。映射来自 Q-07 已决 + 原型真实数据（T-1022 clue-scan v1.0 / T-1023 hva-five-checks v1.1）：`('S-A1','clue-scan','线索扫描','v1.0','discovery-agent',1)`、`('S-B1','hva-five-checks','HVA 五查','v1.1','hva-agent',1)`。
- **🔴 踩坑（DDL 权威）**：首版误用 `AGP-DISC`/`AGP-HVA` 作 `bound_agent_code`，但 `skill_registry.bound_agent_code` 的 FK 指向 `agent_profile(agent_code)`，`agent_code` 实为 `discovery-agent`/`hva-agent`（`AGP-DISC`/`AGP-HVA` 是 `profile_id`）。→ 改回 agent_code 值，重跑校验 `foreign_key_check=[]`、`integrity_check=ok`。教训：FK 目标列取值须按 DDL 口径，不能想当然用主键 id。
- 刷新 6 处陈旧引用（全 "Q-07 仍开放/待落定后补种" → "已决"）：`0001_init.sql` L305 注释、`db/seed/README.md` L27、`db/migrations/README.md` L29、`db/README.md` L23(30→31 表有数据/6→5 空) + L36(检查项) + L44(放行判定) + L50(v3 追加说明)；`generate_mock.py` 注释同步（先误写 AGP-DISC 后改 discovery-agent）。
- 最终：36 表 **31 有数据 / 5 空**（MD-12/PD-02/PD-04/PD-06/EXT-03 原型无数据），MD-14 已按映射补种。

**校验（python + sqlite3，managed runtime）**：`PRAGMA foreign_keys=ON` 下 `foreign_key_check=[]`、`integrity_check=ok`；正则剥离两文件 `PRAGMA foreign_keys`（避免载入阶段误开 FK 导致误报）后跑 `foreign_key_check` 定位违例——此法是可靠的诊断手法，记下来复用。

**未动项（按"钉版不回溯"）**：`docs/07-decisions/README.md` 与 `DEC-PACK-001.md` 中"宪法 v2"属历史钉版（描述 v2 bump 时点），未改。

---

## 开发阶段 · 阶段0-工程骨架（2026-09-19）

**角色**：开发工程师。第一个工作项：阶段0-工程骨架（wrangler.toml + 最小 Worker 入口 + 本地 D1 绑定）。用户选「先给阶段0开工顺序建议」→ 起点取工程骨架（dev-plan 阶段0 三待建漏列 Worker 骨架本身，CI 暗含 Worker 已存在，故补为第0步）。

**新增文件**：
- `wrangler.toml`：单 Worker `jd-growth-platform`，`main = server/api/index.js`，D1 绑定 `DB`（`database_name = jd-growth-platform`，`migrations_dir = ./db/migrations`）。无密钥落地（硬红线）。
- `server/api/index.js`：最小 Worker，仅 `/api/health` + `/api/db-ping`（只读 `SELECT 1`）+ 404。**零写操作**。
- `server/README.md`：枝杈索引（文档卡 + 反向清单 + 模块状态表）。
- `.gitignore`：忽略 `.wrangler/` `node_modules/` `.dev.vars` `.env` `*.log`。

**双向登记**：`server/README.md` 写文档卡+反向清单；`AGENTS.md` 索引 `server/` 行「待建」→「✅ 工程骨架已建 2026-09-19」（附单 Worker 入口 + TS-20 待确认拆分说明）。docs/README 不列 server（根目录不进 docs/）。

**验收（实跑，非声称）**：
- `wrangler d1 migrations apply jd-growth-platform --local` → `0001_init.sql` ✅，49 命令。
- `wrangler dev --local` 起 → `/api/health`=`{"status":"ok"}`、`/api/db-ping`=`{"d1":"reachable","ok":1}`、未知 404。
- `wrangler d1 execute --local --file=db/seed/0001_mock.sql` → `success:true`（真实本地 D1 FK 零违例）；探测 36 表在册、`skill_registry`=2 行（Q-07 补种生效）。

**门禁自检**：依赖前序无（阶段0 起点）；Q-07 已决（本项不写 S-A1/S-B1）；external-deps §7 未关（本项无外部调用）；TS-10 未决（不碰模型）。全部放行。

**N/A 项（未臆造）**：本项无 F-xx → 对应测试用例/ scripts 引用自检不适用；无 schema 变更 → 仅跑迁移 apply + 种子灌入校验。

**下一步建议顺序**：阶段0-mock server → 阶段0-agent-runtime 骨架 → 阶段0-CI。

---

## 阶段0-mock server（2026-09-19，第二个工作项）

**依据**：dev-plan 阶段0 关键交付物（L68 `prototype/mock/`，契约见 external-deps §6）+ 验收要点（L71「七类行为可返回并映射到 EXT-01 字段」）+ 依赖前序（L72 无，demo 占位红线）。
**落位**：`prototype/mock/`（external-deps L518 已暂定，与 dev-plan 一致）。
**交付（新增 4）**：
- `prototype/mock/scenarios.js` — 9 类响应（7 行为 §6.1~6.7 + 超时失败 + 执行中，§6 末段）
- `prototype/mock/index.js` — 零依赖 node http：`POST /query`（tool_code + force_behavior 路由）、`GET /health`；默认按 source_id 返回 ok 聚合
- `prototype/mock/verify.js` — 自检：启动临时实例、逐类断言「可映射 EXT-01 六字段 + 行为语义正确」、demo 值不进断言
- `prototype/mock/README.md` — 文档卡 + 反向清单 + 9 类↔EXT-01 映射表

**验收（实跑）**：`node prototype/mock/verify.js` → `VERIFY PASS: 9 类响应均可映射 EXT-01 六字段 + 行为语义正确（demo 值未进断言）`，EXIT=0。

**双向登记**：`prototype/README.md` 目录表加 `mock/` 行（✅ 已建 2026-09-19）；`external-deps.md` L518 反向清单 `⏳ 待建`→`✅ 已建`。AGENTS `prototype/` 行本就 `✅ 已有`（mock 为子目录，状态位无需翻转）。

**门禁自检**：依赖前序无；Q-07 已决（本项不写 Skill）；external-deps §7 未关 → demo 占位、值不进断言（已落实：verify 只断言结构/语义）；TS-10 未决（不碰模型）。全放行。
**N/A**：无 F-xx 用例、无 schema 变更（mock 不改 db，未跑迁移重生成）。

**下一步**：阶段0-agent-runtime 骨架（建壳 + 版本管理机制，agent.md/S-A1~4/S-B1~4 本体留阶段4）→ 阶段0-CI。

## 阶段0-agent-runtime 骨架（2026-09-19）
- **交付**：新建 `agent-runtime/` 13 文件 = `README.md`(枝杈) + `VERSIONS.md`(版本管理机制=MD-13/MD-14 登记载体，核心) + `business-rules.md` + `discovery/agent.md` + `hva/agent.md` + `discovery/skills/S-A1~S-A4`(4) + `hva/skills/S-B1~S-B4`(4)。全部「建壳」（仅文档卡+反向清单+建壳声明，本体留阶段4）。
- **版本机制（VERSIONS.md）**：定义 agent.md 随 git、任务启动组装 `agent_version_snapshot`（PD-01.task）形如 `discovery-agent v1.2 / agent.md r9 / skills: clue-scan v1.0`；镜像 MD-13(`profile_id`=`AGP-DISC`/`AGP-HVA`,`agent_code`=`discovery-agent`/`hva-agent`,`current_version`=`v1.2`/`v1.3`,`doc_revision`=`r9`/`r12`) 与 MD-14(`S-A1`=clue-scan/`S-B1`=hva-five-checks 已种，其余待 T-24)。
- **⚠️ 上游口径缺陷（发现未擅改）**：`PRD-M3-机会发现Agent.md` §1.1 L65 写 `agent_code=AGP-DISC`，与 DDL/种子不符（AGP-DISC 实为 `profile_id`，`agent_code`=discovery-agent）。VERSIONS.md §2.1 已注明以 DDL 为准，待 PRD-M3 订正。
- **门禁自检**：依赖前序无；Q-07 已决（S-A1/S-B1 已种）；external-deps §7 未关 → T-23/T-24 标⚠️不锁死版本形态/code 名；TS-10 未决（不碰模型）。全放行。
- **双向登记**：AGENTS.md L17 `agent-runtime/`「待建」→「✅ 工程骨架已建 2026-09-19」；README/VERSIONS 反向清单写 server/agent-orchestrator + dev-plan 阶段4 + test-M3/M4。
- **核验（python）**：13 文件齐全；README/VERSIONS 引用路径 0 悬空；6 版本串镜像种子一致；上游 9 文件全在；AGENTS 状态位落盘。scripts/ 自检脚本待建 → 以 python 轻量核对替代，未臆造。
- **下一步**：阶段0-CI（GitHub Actions 串 d1 migrations apply + deploy + PR 分支保护；须等可 deploy Worker，骨架已备）。

## 阶段0-CI + PRD-M3/M4 口径缺陷订正（2026-09-19 续）
- **缺陷订正（用户授权「可以补齐缺陷」）**：PRD-M3/M4 §1.1 L65 原 `agent_code=AGP-DISC/AGP-HVA` 误用列名（AGP-DISC/HVA 实为 `profile_id` 主键，`agent_code`=discovery-agent/hva-agent），且残留 Q-07「建表不种、映射待定」过期说法。已按 DDL/种子真源订正，并刷新 Q-07 已决状态。
- **走全套（锁文件改版惯例）**：PRD-M3/M4 bump v1.0→v1.1，旧版归档 `.trash/PRD-M3-v1.0.md`/`PRD-M4-v1.0.md`（sha 一致）；同步下游钉版 `docs/02-prd/README.md` L20/L21（v1.0→v1.1）、`dev-plan.md` L126 映射记号（`→AGP-DISC` 改为 `bound_agent_code=discovery-agent,profile_id=AGP-DISC`）、`agent-runtime/VERSIONS.md` L23 提示翻 ✅ 已订正。全仓 grep 已无 `agent_code=AGP` 残留。
- **阶段0-CI 交付**：`.github/workflows/ci.yml`（pr-title-check + validate + deploy；凭证仅经 Secrets，不落库）+ `.github/README.md`（枝杈 + 分支保护/Secrets 手动设置说明）；AGENTS.md 增 `.github/` 行「✅ 已建 2026-09-19」。
- **校验**：YAML 解析 OK、三 job 结构正确、PR 标题正则（F-xx/阶段N/chore/docs/ci 通过，feat/任意拒绝）；本地实跑 `wrangler d1 migrations apply --local --yes` + `node --check server/api/index.js` + `node prototype/mock/verify.js` 全绿。
- **阶段0 全部完成**：工程骨架 / mock server / agent-runtime 骨架 / CI 四项均落地验收。下一步进阶段1 M2·F-07（首个 F-xx 工作项）。

## 账实核对 + TS-11 裁定 + 历史任务收口（2026-09-19 续）
- **历史 5 项 pending（#5~#9）账实核对**：经本体核验 **全部实已完成**，仅状态未勾销——#5 schema v1.3（Q-03/04/05 均✅已决）、#6 tech-stack v1.2（TS-14✅实测、TS-11 待裁决）、#7 ADR-001~003 存在（01:10）、#8 双向引用（残留反向下游标签滞后已补）、#9 DDL 开工检查单＝`db/README.md` L27 章节。5 项已标 completed。
- **反向下游状态标签账实修正（9 处）**：schema L950 子PRD、tech-stack L448-451（server🟡/db✅/开发计划✅/测试用例✅/runbook⏳）+ L453 诚实说明、external-deps L519-520 + L522 诚实说明、db/README L60-61、db/probes/{collation,fk,type}/README + probes/README 的 db/ 下游行，均由 `⏳ 待建` 翻为实际状态。保留项：frontend/runbook（真待建）、mock README 的阶段2/4（真待建）、ADR-003:174 与 DEC-PACK-001:467（决策留档，钉版不回溯）。
- **TS-11 锁文件冲突裁定（用户定：维持「待裁决」）**：db/README 检查单第3项/已知缺口第4项/放行判定 + db/migrations/README 第3条 + `0001_init.sql` L7 头注 均曾写「已裁决：应用层校验」，已订正为「**仍待裁决**（首版暂不写库级长度 CHECK；若日后裁决库级 CHECK 须补迁移）」。理由：db/ 所引「证据」即说待裁决的 tech-stack §8，且 DEC-PACK-001 无 TS-11 记录。
- **下一步**：阶段1 M2·F-07（首个 F-xx，用户已确认继续）。

## 阶段1 · M2 · F-07 业务背景管理（2026-09-19，首个 F-xx 工作项）
- **开工前置自检**：依赖前序＝阶段0（已全建）✓；门禁：Q-07 已决、external-deps §7 未关→本项无外部调用、TS-10 未决→不碰模型 ✓；环境：wrangler d1 migrations apply + 本地 D1 可起 ✓。
- **交付物（新增 3 / 修改 6）**：新增 `server/shared-context/index.js`（MD-04 背景条目 + MD-05 触点清单读写 + 背景简报）、`server/shared-context/test-f07.mjs`（node:sqlite + D1 适配层，载真实 DDL 跑约束）、`server/shared-context/README.md`；修改 `server/api/index.js`（F-07 路由：/api/business-context、/api/touchpoints、/api/background-briefing + 错误码映射 409/400/500）、`server/README.md`、`AGENTS.md`（server/ 行→🟡 部分已建）、`.github/workflows/ci.yml`（validate 增 F-07 用例步）。
- **验证实跑**：`node server/shared-context/test-f07.mjs` → **VERIFY PASS（5 组）**；端到端 `wrangler dev` 探针：POST 平台级背景 201（goal_id:null）、同名触点 409（UNIQUE）、缺必填 400、background-briefing 正常返回；`node --check` 入口 OK；YAML 6 步 OK；迁移本地应用 OK。
- **oracle 对齐**：TC-D-M2-001（MD-04 goal_id 可空 FK 正反）、TC-D-M2-002（MD-05 touchpoint_name UNIQUE 反例）＝ test-M2 中 F-07 的全部用例，均已变绿。
- **双向登记**：模块头部文档卡 + 反向清单；`server/README.md` 模块表登记；AGENTS `server/` 行状态翻转。
- **未决项仍挂起**：TS-12（F-27/F-28 中文排序，阶段1/5）、external-deps §7（M2 无外部调用）、TS-11（待裁决，本项不涉及长度 CHECK 写法）。
- **下一步**：M2 下一功能点 F-08（可用来源与工具登记，CFG-01）——待用户指定。

## 阶段1 · M2 · F-08 可用来源与工具登记（2026-09-19）

- **口径**：F-08 落 `CFG-01 source_registry`（五来源 CDP/HJE/PIM/MKT/ACT，`source_id` PK、`source_name` UK、`availability_status` 走 `dict:SOURCE_STATUS`=ok/degraded/unauthorized）。schema §F-xx 映射含 CFG-02，但 **CFG-02 工具的写/注册（MCP 化、is_enabled）归 F-23（M5 阶段2）**；本项只做 CFG-01 读写 + 只读交叉引用 CFG-02 产出「来源与工具说明」。
- **实现**：`server/shared-context/index.js` 加 `registerSource`/`updateSourceCapability`/`listSources`/`getSource`/`getSourceGaps`/`getSourceToolBriefing`；API 加 `/api/sources`(GET/POST)、`/api/sources/capability`(PATCH)、`/api/source-gaps`、`/api/source-tool-briefing`。
- **oracle 对齐**：TC-D-M2-010（PK 拒 NULL/拒重复 + 5 来源种子齐全）、TC-I-M2-003（**来源登记不代替实际证据**：缺口 `is_evidence=false`；`unauthorized`→`source_unavailable`、能力边界→`capability_limit`）＝ test-M2 中 F-08 的全部用例，均已变绿。
- **缺口语义**：缺口＝未接入（整源不可查）或 `capability_cannot`（能力边界）。**不硬编码「恰好 5 个来源」**——T-09（是否有第六源）未关，`source_id` 走字典可增。
- **未决项仍挂起**：§7 的 T-07（ACT degraded 常态/临时）、T-08（ACT 有无参与明细）、T-09（第六源）；TS-12、TS-11 同前。
- **下一步**：M2 F-09（证据管理，EXT-02 + LNK-01/02，证据四要素）。

### 阶段1 · M2 · F-09 证据管理（第三个 F-xx）

- **开工前置自检**：① 依赖前序＝阶段0（四件全建）+ F-07/F-08 已建；② 门禁＝Q-07 已决 / external-deps §7 未关（本项写自有 D1、无外部调用、demo 值不进断言）/ TS-10 不碰模型；③ 环境＝迁移 apply + 本地 D1 可起。
- **口径**：F-09 落 `EXT-02 evidence`（`evidence_id` PK；`query_id` FK→EXT-01、`source_id` FK→CFG-01；NOT NULL：evidence_title/query_condition/info_time_point/applicability_scope/result_summary/missing_note）+ `LNK-01 opportunity_evidence`（UK(opportunity_id,evidence_id,link_kind)；`link_kind` ∈ dict:EVIDENCE_LINK_KIND = initial_basis/related_update）+ `LNK-02 finding_evidence`（UK(finding_id,evidence_id)）。
- **证据四要素**（BRD §7.3 / PRD-M2 F-09）＝ `source_id`(来源) / `query_condition`(查询条件) / `info_time_point`(信息时点) / `applicability_scope`(适用范围)。缺任一 → **不可作为有效依据**（应用层 fail、不落库）。
- **实现**：`server/shared-context/index.js` 加 `validateEvidenceCompleteness`(纯函数) / `createEvidence`(只 INSERT) / `getEvidence` / `listEvidence` / `getEvidenceTrace`(证据→查询记录→来源回查链路) / `linkOpportunityEvidence` / `linkFindingEvidence` / `listEvidenceByOpportunity` / `listEvidenceByFinding`；导出常量 `EVIDENCE_FOUR_ELEMENTS`、`EVIDENCE_ELEMENT_LABELS`。API 加 `/api/evidence`(GET 列表可按 source_id/query_id/opportunity_id/finding_id 过滤；POST 登记)、`/api/evidence-trace/{id}`、`/api/evidence/{id}`、`/api/evidence-links/opportunity`、`/api/evidence-links/finding`；errorResponse 增「四要素不齐」→400。
- **oracle 对齐**：TC-D-M2-007（EXT-02 `query_id`/`source_id` 不存在→FK 拒）、TC-I-M2-001（**四要素齐全校验** + **新证据加入不覆盖原依据**，EXT-02 只新增行、LNK-01 `link_kind` 区分新旧）、TC-D-M2-015（LNK-04 复合 UK，标 F-09/F-12）＝ test-M2 中 F-09 全部用例，均已变绿（`test-f09.mjs` **11 组断言全绿**）。
- **边界（不跨项）**：EXT-01 `query_record` 写入归 M5 tool-executor(F-24/25)，本项只读回查；**LNK-04 `task_object` 写路径归 M1 task-runner(F-02/04/06)**，执行器只按 oracle 断言其库级复合 UK；机会/发现写入归 F-10/F-21。
- **实跑校验**：`node --check` 入口 OK；F-07 5 组 / F-08 7 组 / F-09 11 组全绿；YAML 8 步；迁移 apply OK；端到端 `wrangler dev` 探针——GET 列表/过滤/单条/回查链路(traceable=true)/机会与发现关联全绿，POST 四要素齐全 201、**缺条件 400**、`query_id` 不存在 **409 FK**、重复 link **409 UK**、新旧依据 `[('EV-1041','initial_basis'),('EV-E2E01','related_update')]` 并存。探针数据已清回（evidence 7 / LNK-01 10 / LNK-02 8）。
- **登记**：`server/shared-context/README.md` 加 F-09 覆盖段 + `server/README.md` 模块进度 + `AGENTS.md` `server/` 行 + `ci.yml` 加 F-09 用例步（8 步）。
- **未决项仍挂起**：§7 的 T-07/T-08/T-09（来源侧）；TS-12、TS-11 同前。F-09 无新增未决项。
- **下一步**：M2 F-10（机会记录管理，MD-06 + LNK-03 + PD-05；unknown_item 二态见 ADR-003）。

### 阶段1 M2·F-10 机会记录管理（2026-09-19）
- **oracle（test-M2）**：`TC-D-M2-003`（六要素任一 NULL→NOT NULL 拒）＋`TC-D-M2-004`（六要素任一 `''`→CHECK 拒）＋`TC-D-M2-005`（`unknown_item` 二态 + `'   '` **须应用层拒**）＋`TC-D-M2-012`（LNK-01 复合 UK）＋`TC-D-M2-014`（LNK-03 自环应用层拒 + 复合 UK）。
- **交付**：新增 `server/shared-context/test-f10.mjs`；改 `server/shared-context/index.js`（＋`assessOpportunitySixElements`/`classifyUnknownItem`/`createOpportunity`/`getOpportunity`/`listOpportunities`/`changeOpportunityStatus`/`logOpportunityStatus`/`listOpportunityStatusLog`/`linkOpportunityRelation`/`listOpportunityRelations`/`getOpportunityRecord`，导出 `OPPORTUNITY_SIX_ELEMENTS`/`UNKNOWN_ITEM_STATES`）；改 `server/api/index.js`（F-10 四条路由 + 错误映射增 六要素不齐/未知项/自环→400、不存在→404）；改 `shared-context/README.md`、`server/README.md`、`ci.yml`（9 步）、`AGENTS.md`。
- **实测**：F-10 **20 组断言全绿**（F-07 5 / F-08 7 / F-09 11 仍全绿）；YAML 9 步；迁移 apply OK；端到端探针——8 机会、deferred 2、`pending_supplement=1` 命中未评估新机会、读模型 complete/pending 判定正确；POST 六要素齐备 201、`target_object=''` **400**、`unknown_item='   '` **400**、`goal_id` 不存在 **409 FK**；PATCH 状态链 `candidate→deferred→candidate` 且 **defer_reason 自动清空**、派生 log_id `LG-OPP-E2E10-001/002`；LNK-03 自环 **400**、重复 **409 UK**、换 kind **201**。探针数据已清回（opp 8 / pdl 4 / lnk3 2）。
- **★ 新踩坑（已写入 SOP 技能与模块 README）**：**数值列的空串不被库级拦**——SQLite 类型序 TEXT > INTEGER，`'' >= 1` 求值为 TRUE，故 `goal_version_no` 的 `NOT NULL + CHECK(>=1)` 放行 `''`；须应用层 `trim()===''` 兜底（本项目 `assessOpportunitySixElements` 已兜）。同源结论：`unknown_item` 二态无库级 CHECK（ADR-003 §3.2）→ `'   '` 同样漏到应用层。
- **⚠️ 上游数据缺陷（已登记未擅改，待用户裁定）**：`db/seed/generate_mock.py` L438 的 LNK-03 种子行 `LK-OR-001` 的 `relation_kind = "related_update"` **属值域外**——`dict:OPP_RELATION` 仅 `same_issue` / `superseded`；`related_update` 实为 `dict:EVIDENCE_LINK_KIND`（LNK-01 用）的取值，属复制粘贴错误。该表无物理约束拦字典域，故种子可载入。建议订正为 `same_issue`（须用户点头）。
- **未决项仍挂起**：无新增；§7 的 T-07/T-08/T-09、TS-11/TS-12 同前。

---

## 进展盘点（2026-09-19 收尾，只读核查，未改代码）

**实跑复核**：`node server/shared-context/test-f07~f11.mjs` 全绿（F-07 5组 / F-08 7组 / F-09 11组 / F-10 20组 / F-11 14组，EXIT=0）。

**已完成**：阶段0 四项全落地（工程骨架 / mock server / agent-runtime 骨架 / CI）；阶段1 M2 的 **F-07 / F-08 / F-09 / F-10 / F-11** 五个功能点已落地且用例全绿（`server/shared-context/`）。

**未完成**：① 阶段1 M2 仅剩 **F-12 上下文按任务注入**（PD-06 + CFG-06；`server/shared-context/index.js` 与 `server/api/index.js` 全库 grep `context_injection`/`context_template` **0 命中**，确认未实现）；② `scripts/` 引用自检脚本仍**不存在**（AGENTS 索引仍标「待建」）——而它是每个 PR 的硬性门禁；③ 阶段2~5 全部未开工。

**账实修正候选已消解**：`skill_registry` / `agent_profile` 补种**早已生效**——`db/seed/0001_mock.sql` 实有 `INSERT INTO skill_registry` ×2、`agent_profile` ×2，故「阶段0-账实修正(补种skill_registry)」无需再做。

**新查出的账实不一致（已登记未擅改）**：
1. **git 未纳管（阻塞「一 F-xx 一 PR」）**：`git rev-parse --show-toplevel` = `/Users/dongzhuo/Documents/VScode仓库`，HEAD 只跟踪该根下一个 `AGENTS.md`；`京东-agent项目/` 整体处于 `??` 未跟踪（未被 .gitignore 忽略），且仓库内混有 3 个无关分支（`heartbreaking-library`、`agents/can-you-run`、`agents/single-digit-input-handler`）。→ 当前**无法真正开 PR**，须先定纳管策略（在本项目内 `git init` 独立仓库为推荐）。
2. **dev-plan L66 数字滞后**：写「30 表有种子 / 6 表留空」，实测（DDL 36 表 vs 种子 INSERT 去重）**31 表有种子 / 5 表留空**（留空＝`research_proposal`、`task_step`、`goal_gap`、`context_injection`、`external_validation`，与 `db/README.md` 口径一致）。滞后源于 skill_registry/agent_profile 补种未回写 dev-plan。
3. **F-11 已落地但本日志缺条目**：`server/shared-context/README.md` 与 `AGENTS.md` 均已登记 F-11，本文件止于 F-10——记忆滞后，非功能缺失。
4. **遗留待用户裁定**：`db/seed/generate_mock.py` L438 `LK-OR-001.relation_kind="related_update"` 属值域外（应为 `same_issue`）。

---

## 进展盘点后续：git 纳管 + 阶段1 M2·F-12（2026-09-19）

**用户四项预审选择**：① 做 F-12；② 外部调用走 mock/demo（本项无外部调用，自动满足）；③ 同步翻转状态位；④ 项目内 `git init` 独立仓库。

**git**：已在项目内 `git init -b main` 并打基线提交 `a95a301`（122 文件；`.gitignore` 补 `__pycache__/`）。**坑**：`git rev-parse --show-toplevel` 原为父目录 `VScode仓库`（父仓只跟踪一个 `AGENTS.md`，且混有 3 个无关分支）→ 现为嵌套独立仓；父仓仍会把本项目当未跟踪目录（未动父仓）。F-12 改动**尚未提交**，停在分支 `F-12-context-injection` 待用户确认。

**F-12 交付（阶段1 M2·F-12 上下文按任务组织注入）**：
- `server/shared-context/index.js` 增 F-12 段：`registerContextTemplate`/`listContextTemplates`/`getContextTemplate`（CFG-06）、`recordContextInjection`/`listContextInjections`（PD-06）、`buildTaskContext`（纯读装配）、`initTaskContext`（装配+落库，**幂等**）、`getTaskContext`（回读）；导出 `CONTEXT_OBJECT_TYPE`/`CONTEXT_WITHOUT_OBJECT_TYPE`。
- 新增 `server/shared-context/test-f12.mjs`（**14 组断言全绿**）；M2 六个点全绿：F-07 5 / F-08 7 / F-09 11 / F-10 20 / F-11 14 / **F-12 14**。
- `server/api/index.js` 增 4 条路由：`/api/context-templates`(GET/POST)、`/api/task-context/init`(POST)、`/api/task-context/{task_id}`(GET)、`/api/context-injections`(GET/POST)。
- **端到端探针（无 wrangler 的替代法，可复用）**：本机已无 `wrangler` 命令 → 写临时脚本直接 `import worker from server/api/index.js`，用 `node:sqlite` 造 `env.DB` 调 `worker.fetch(new Request(...))`。实测：模板列表 200、新组合 201、重复组合 409、装配两次 body 完全一致、init 201、重复 init 幂等、FK 409、任务不存在 404、db-ping 200。**此法不依赖网络与 wrangler，值得复用。**
- 双向登记：shared-context/README（F-12 覆盖段 + 文件清单 + 反向清单）、server/README、AGENTS `server/` 行、ci.yml 增 F-12 步（validate 9→10 步）。

**★ 新发现 Q-08（已登记 schema.md §12，未擅自改口径）**：`PD-06.ref_object_type` 的值域 `dict:OBJECT_TYPE` 只有 goal/opportunity/research/proposal 四项，装不下 `background`(MD-04 条目)/`source`(CFG-01)/`existing_evidence`(EXT-02) 三类；而 ADR-001 §3 第 3 条要求 PD-06「记下注入了哪几条对象」。**当前按方向②实现**（三类只装配、不落 PD-06 行，装配结果打 `no_object_type_q08=true` 并写进断言），待裁决。

**另一处新发现（未擅改）**：`docs/03-locks/schema.md` 反向清单写 `../07-decisions/ADR-001.md`（实际文件名带后缀「-业务背景不与目标版本联动」），属悬空引用；ADR-002/003 同。**已登记，等用户点头再改。**

**自检**：本次改动文件抽出 27 条相对路径引用 → 悬空 1（即上面那处存量缺陷，非本次引入）；Q-08 编号登记于 schema §12（唯一登记处）+ 4 处引用，无重号。`scripts/` 引用自检脚本**仍待建**（本次以等价脚本替代）。

## F-12 已提交（用户确认后）

- **仓库**：项目内独立 git 仓已建；基线 `a95a301`（阶段0 四项 + M2 F-07~F-11），F-12 提交 `d41936c` 落在分支 `F-12-context-injection`（未合回 main，等用户决定）。提交标题 `F-12: 阶段1 M2·上下文按任务组织注入`，符合 ci.yml pr-title-check 白盒正则。
- **提交内容**：新增 `server/shared-context/test-f12.mjs`；改 7 文件（shared-context/index.js、api/index.js、两处 README、ci.yml、AGENTS.md、schema.md §12 新增 Q-08）。
- **M2 阶段1 六个功能点全部收口**（F-07~F-12），用例组数 5/7/11/20/14/14 全绿。
- **下一步候选**（用户未指定，勿自动开工）：① `scripts/` 引用自检脚本（AGENTS 仍标待建，是每个 PR 的硬门禁）；② 订正 dev-plan L66「30/6」→ 31/5；③ 种子 `LK-OR-001.relation_kind="related_update"` 值域外（应为 `same_issue`）；④ 修 schema.md 反向清单 ADR-001/002/003 悬空引用；⑤ Q-08 裁决；⑥ 阶段2 M5。

## 用户三项裁决（同日）

1. **F-12 合回 main** —— 已 fast-forward，`main` = `1b6f108`。
2. **Q-08 采纳方向②** —— schema.md §12 Q-08 已由「待裁决」翻为「已决 2026-09-19」；后续若要库内留痕再改判（改 `CONTEXT_OBJECT_TYPE`/`CONTEXT_WITHOUT_OBJECT_TYPE` + 补 `dict_item` 即可，无 DDL）。提交 `2ef7630`（分支 `docs-q08-ruling`）。
3. **下一工作项 = scripts/ 引用自检** —— 已实施，停在分支 `chore-scripts-ref-check` 待确认。

## chore · scripts/ 引用自检脚本（2026-09-19，待用户确认后提交）

- **产出**：`scripts/ref-check.mjs`（本体，三类判定口径的唯一可执行定义写在其文档卡）、`scripts/ref-check-allowlist.json`（2 条豁免）、`scripts/tests/test-ref-check.mjs`（夹具骨架 18 断言）、`scripts/README.md`；改 `ci.yml`（validate 增 2 步）、`AGENTS.md`（`scripts/` 待建→✅ 已建）。
- **实测**：全仓扫描 80 文件 → 悬空 0 / 孤儿 0 / 重号 0，EXIT=0；夹具骨架 18/18 绿。
- **★ 判定口径踩坑（重要，下次改判定前先看）**：① 目录引用与无扩展名片段**不判**悬空（待建目录是合法状态）；② 「相对仓库根命中」是本项目主流写法，**不算**偏差（曾误判 310 条）；③ 重号若按「标题复述 + 列表反引号引用」宽判，一次报 68 条误报——正解是**只认「同一表格块首列重复」与「列表项裸写/加粗登记重复」**，反引号包裹算引用不算登记；④ 范围简写 `S-A1~S-A4.md`、含空格的命令行要滤掉；⑤ `scripts/tests/` 含夹具路径字面量，须排除扫描，否则自造 5 条悬空。
- **查出但未擅改**：真悬空 3 条中的 `../07-decisions/ADR-001.md`（schema.md 反向清单缺 ADR 文件名后缀，ADR-002/003 同口径）与 2 条 `scripts/validate_output.py`（05-test-cases 声明本步未出）——**已登记进 allowlist 并上报**；另有 20 条「路径不精确」软偏差（多为 `assets/data.js` 简写与 `prototype/mock/README.md` 的 `../03-locks/...`）不计入失败、同样待修。

## 阶段2 · M5 · F-23 工具注册与权限检查（2026-09-19，**已提交合回 main**）

- **分支** `F-23-tool-registry-permission`。产出 `server/tool-executor/index.js` + `test-f23.mjs` + `README.md`；改 `server/api/index.js`（4 条路由）、`server/README.md`、`ci.yml`（validate +1 步）、`AGENTS.md`（server 状态位）、`schema.md` §12（新增 **Q-09**）。
- **实测**：`test-f23.mjs` **38/38 绿**（TC-D-M5-001~004 + TC-I-M5-001 + TC-I-M5-005）；全量回归 M2 六点 + 引用自检 + mock server 全绿。
- **判定链（七步互斥）**：tool_not_found → tool_disabled → source_not_mcp_ready → source_unauthorized → no_permission → restricted(allow_flag=0) → allowed。**降级不阻断**，作 `limits[]` 返回（供四要素之「限制」）。
- **★ 种子账实（重要）**：12 个工具 `is_enabled` **全 0**、24 条授权 `allow_flag` **全 1**——对应 external-deps §5「未接入/降级/不存在」，是**如实登记**而非种子缺陷。故种子上无任何工具可被允许，「允许」分支只能靠用例内临时 `UPDATE is_enabled=1` 演示。造过期用例时**别用 `discovery-agent`**（种子给它每个工具都配了长期授权，会覆盖过期那条）。
- **★ 适配层差异**：真机 D1 的 `run()` 遇约束冲突会抛；本地 node:sqlite 适配层返回 `success:false`。两条路径必须在模块里**都判失败**，否则探针会出现「FK 冲突却返回 201 + body null」。已在 `registerTool`/`registerPermission` 加固。
- **新登记 Q-09（未决）**：CFG-02 只有 `is_enabled`，无法区分「存在但未接入」与「不存在」（external-deps §5 结论 1 要求如实登记 TOL-12 的不存在）。当前按方向②（文本表达），裁决后改一张映射表或加字段。
- **后续**：F-24 已落地（见下节）、F-25 EXT-01 落痕与 F-26 重试与暂停待建。本机无 `wrangler`，迁移 apply 未实跑。

## 阶段2 · M5 · F-24 查询执行与真实返回（2026-09-19，已提交合回 main）

- **确认与收口**：用户确认 F-23 后提交 `aaaaaa9` 并 ff-only 合回 main；同轮新建分支 `F-24-real-query`，F-24 经验收通过后提交 **`14de783`** 并合回 main（工作区干净）。
- **产出**：新增 `server/tool-executor/mcp-client.js`（**DS-06 传输层**：五项协议面 + 条件序列化 + 限制萃取 + 实体级隔离；**零 SQL 零写**）、`server/tool-executor/test-f24.mjs`（**74 断言全绿**）；改 `index.js`（F-24 编排 `describeTools`/`executeQuery`）、`server/api/index.js`（`/api/tool-descriptions`、`/api/query`，受限→403）、`tool-executor/README.md`（九类行为→EXT-01 映射表）、`server/README.md`、`ci.yml`（validate +1 步）、`AGENTS.md`、`schema.md` §12（新增 **Q-10**）。
- **编排顺序**：`checkToolPermission`（F-23）受限即返回、**不发起外部调用**（`transport_called=false`）；允许才走 transport → `mapResponseToResult` → EXT-01 字段口径信封 + 四要素。
- **★ 传输层单独成文件的原因（重要）**：把 `mcp-client.js` 与 `index.js` 分开，是为了让「生产零写」这条硬红线能**被静态验证**——该源码零 SQL，HTTP 唯一方法为 POST，`test-f24.mjs` 直接扫源码断言无 INSERT/UPDATE/DELETE 且无 PUT/PATCH/DELETE 通道。
- **★ 踩坑**：① 注入式 transport 若内部引用未初始化的字段会抛错，而编排层会把异常兜成 `fail`——表现为「用例莫名 fail」，真实原因藏在 transport 里；② `query_record` 种子**已有 11 行**，断言「执行后 0 行」是错的，应断**执行前后行数不变**；③ `collectLimits` 初版把 `rows`/`items` 当多值字段，造成噪音，须排除结果集键；④ 探针里覆写 `globalThis.fetch` 会让真机 HTTP 链路必然失败，要验真实 HTTP 就别覆写，直接起 mock server 跑。
- **失败/非终态口径**：`restricted` → `fail + restricted_flag=1 + fail_reason=权限未开通`，且 `result_summary=null`（schema EXT-01 规定失败时为空）；`running` 非终态不判失败；空结果=`ok + returned_rows=0`；不可算=`ok` 且保留 `*_reason`；覆盖边界的**时点写 `coverage.actual` 而非请求区间**。
- **新登记 Q-10（未决）**：`EXT-01` 无承载「限制」的字段，F-24 的 `four_elements.limits[]` 目前只在信封里，转 `EXT-02 evidence` 时归 `applicability_scope`；是否增列待裁决。Q-09 仍挂起。
- **下一步候选**：F-25 `EXT-01` 落痕（失败也留痕，不受外部契约阻塞）｜F-26 重试与暂停（依赖 Queues max_retries≤100，与 M1 F-06 共用状态机口径）。

## 阶段2 · M5 · F-25 查询记录保存（2026-09-19，待用户确认后提交）

- **分支** `F-25-query-record`（自 main `e5b2876` 起）。产出 `server/tool-executor/test-f25.mjs`（**53 断言全绿**）；改 `index.js`（F-25 落痕段）、`server/api/index.js`（3 条路由）、`tool-executor/README.md`（F-25 段：落痕不变量表 / 回查入口 / 取号）、`server/README.md`、`ci.yml`（validate +1 步）、`AGENTS.md`（server 状态位）、`schema.md` §12（新增 **Q-11**，并补 Q-10 的 F-25 落地事实）。
- **一句话口径**：`recordQuery` = `executeQuery`（F-24）+ `saveQueryRecord`（F-25）。**每一次执行落一行 `EXT-01`**——成功/失败/受限/执行中**都落**。
- **落痕不变量（6 条，缺一项即拒落）**：① `result_status` ∈ `dict:QUERY_STATUS`（**值域从 `dict_item` 读，不内联**）；② `query_condition` 非空（否则不可复现）；③ `fail` ⇒ `fail_reason` 非空（**失败也留痕 ≠ 留了等于没留**）；④ `returned_rows` ≥0 整数或空；⑤ `task_id` 必填；⑥ `source_id` 必填。FK 一律交给库级拒绝（实测坏 FK → 409）。
- **取号**：`Q-` + 5 位数字，取库内已用最大值 +1（种子 `Q-90242` → 下一条 `Q-90243`），确定性可复现不撞号。
- **回查**：`getQueryRecord` / `listQueryRecords`（组合筛选、`query_id` DESC 稳定排序）/ `readbackQuery`（派生四要素前三样 + 显式 `limits_not_persisted: true`）。
- **★ 新登记 Q-11（未决）**：`EXT-01.source_id` 是 NOT NULL 外键，**装不下「工具/来源根本未登记」这类失败**（F-23 判定链在 `tool_not_found` 处短路，此时没有来源可写）。当前按方向②：判定链短路＝没发生执行，不落 `EXT-01`，改由 `persist_skip.reason_code` 显式说明「为何没留痕」（`POST /api/query-records` 回 **202** 而非 201），**绝不静默丢**。方向①（为「被拦下的尝试」也留痕）需改 DDL + 迁移 + 种子三处。
- **★ 踩坑**：① 断言「缺 task_id → 拒落」时必须**构造不含 task_id 的信封**——`mapResponseToResult` 的 ctx 带 task_id 时信封里就有，`opts.task_id ?? envelope.task_id` 会兜住，断言前提不成立；② `recordQuery` 一开始没把 `task_id` 透传给 `executeQuery`（被解构走了），信封里缺归属任务 → 已改为 `executeQuery(db, { ...rest, task_id })`；③ ④「失败也留痕」的失败行统计要**取基线**——`query_record` 种子里本身就有 fail 行。
- **实测**：`test-f25.mjs` **53/53 绿**（TC-D-M5-005 task/source/message 三处 FK → 失败；TC-I-M5-003 成功/失败/受限/执行中/传输异常五路落痕 + 不变量 + 回查；TC-I-M5-005 生产零写，配置三表落痕前后行数不变）；端到端探针走**真实 HTTP 打到 mock server :8788**：允许 201 / 失败 201 / 受限 201 各落一行，工具未登记 202 + `persist_skip`，坏 task_id 409，回查 200 四要素齐全，EXT-01 11→14 行（新增 3 行＝三次真实执行）。
- **全量回归**：F-07~F-12（5/7/11/20/14/14 组）+ F-23(38) + F-24(74) + F-25(53) + 引用自检 0 命中 + 骨架 18 + ci.yml 可解析 —— 全绿。
- **未决仍挂起**：Q-09、Q-10、**Q-11**（三者都在 `schema.md` §12）；旧账四笔（dev-plan L66「30/6」→31/5、种子 `LK-OR-001.relation_kind` 值域外、20 条路径软偏差、ADR 反向清单缺后缀）。
- **下一步**：F-26 失败重试与受限返回（M5 最后一点；重试＝代码逻辑、卡死保留状态停止，与 M1 F-06 共用状态机口径；`EXT-01.retry_count` 已有承载，`PD-03 task_block` 与 M1 F-06 协同）。

## 阶段2 · M5 · F-26 失败重试与受限返回（2026-09-19，待用户确认后提交）

- **分支** `F-26-retry-restricted`（自 main `f0c9030` 起）。**M5 阶段2 四点全收口。**
- **产出**：`server/tool-executor/task-state.js`（**新文件**）、`index.js` F-26 段、`server/api/index.js` 四路由、`test-f26.mjs`（**100 断言全绿**）；登记 `tool-executor/README.md`（F-26 段：重试判定表 / `retry_count` 双来源口径 / 受阻矩阵映射 / 卡死门槛 / 任务态守卫 / 写入面静态验证）、`server/README.md`、`ci.yml`（validate +1 步）、`AGENTS.md`、`schema.md` §12（**新增 Q-12**）。
- **开工预审三问（用户全采推荐口径）**：① **两层分工**——执行层重试写 `EXT-01.retry_count`，任务层写 `PD-03` + `PD-01.task_status/retry_count`；② **卡死门槛**＝重试用尽首犯→`blocked`，**同类受阻未解除再犯→`stopped`**；③ **受限返回不重试但落受阻**（矩阵第 3 行 `source_unavailable`）。
- **一句话口径**：`runQueryWithRecovery` = 前置守卫 → `executeQueryWithRetry`（上限取 `CFG-04 retry_limit`，**封顶 100**，越界截断并标注）→ `saveQueryRecord`（F-25，一次执行一行）→ `handleQueryFailure`（仅失败/受限）。
- **重试判定（互斥）**：`ok` 不动任务态（**成功不算失败**）｜`running` 不重试不动态｜**受限不重试**｜`fail` 非受限才重试，用尽 `exhausted`。
- **`retry_count` 双来源（显式标注）**：本层重试过 → 本层次数 + `retry_count_source='platform'`；本层未重试 → 沿用返回体透传值 + `'upstream_passthrough'`（§6.1 慢响应）；皆无 → `'none'`。
- **★ 关键架构决策（值得复用）**：**新增写面单独成文件**——F-26 的任务态处置（改 `PD-01` / 插 `PD-03`）落在新文件 `task-state.js`，于是 `index.js` 仍**不含 UPDATE/DELETE**，`test-f23.mjs` 那条「模块 SQL 无 UPDATE / DELETE」静态断言**逐字未动**。理由与 `mcp-client.js` 当初拆出来一样：让「生产零写」这条硬红线**始终可由静态扫描证明**，而不是每次扩写面就得去改既有 oracle。
- **★ 踩坑（都值得记住）**：
  - **`assert(listTaskBlocks(...).length >= 2)` 忘了 `await`** —— Promise 的 `.length` 是 `undefined`，断言**静默假失败**（报错指向别处，白白多查一轮）。凡是 async 函数出现在断言表达式里，先确认有没有 `await`。修法是先把结果取进变量再断言，顺带把**实测值写进断言文案**（`实测 N 条`），下次一眼可见。
  - **`previous_task_status` 取成了改后态** —— 命名的「改前」必须在 `setTaskStatus` 之前 `getTask` 一次；写在一串 `await` 之后就会拿到改后的值。
  - **静态扫描扫的是全文，含注释**：`/\b(UPDATE|DELETE|DROP|ALTER|TRUNCATE)\b/` 会被**中文注释里的字面英文词**命中。所以 `index.js` 的散文里只能写「改行 / 删行类 SQL」，不能出现裸的 `UPDATE`/`DELETE`；新文件 `task-state.js` 因为由自己的用例扫描，改用**先去注释再扫**（`stripComments`），散文才能写全关键字。
- **★ 前置守卫（用户口径之外、端到端探针逼出来的改进）**：初版是「先执行 → 落 EXT-01 → 写 PD-03 → 最后 `setTaskStatus` 抛错」，于是一个 400 响应背后已经留了**一条无主 EXT-01 + 一条多余 PD-03**。改为 `assertTaskRunnable` **在执行之前**判：已 `stopped`/`done` 的任务根本不进入执行（`test-f26.mjs` 断言写入数不变）。守卫文案也顺手改掉歧义（旧文案对已停止任务会说「不允许改回 stopped」，读着莫名其妙）。
- **实测**：`test-f26.mjs` **100/100 绿**；端到端探针走**真实 HTTP 打到 mock server :8788**——工具未登记 `202 + persist_skip=tool_not_found`（且任务落 `source_unavailable` 受阻）、用尽失败 `201 + exhausted/retried=3 + blocked(call_failed)`、再犯 `201 + stopped + ended_at`、已停止再触发 `400`（**写入不变**）、已完成任务 `400`（执行前即拒）、受限 `403 + retried=0 + blocked(source_unavailable)`；`EXT-01` 11→14、`task_block` 2→6（4 条新受阻＝4 次真实执行）。
- **全量回归**：F-07~F-12（5/7/11/20/14/14 组）+ F-23(38) + F-24(74) + F-25(53) + **F-26(100)** + mock 自检 9 类 + 引用自检 0 命中 + 骨架 18 + `node --check` 三入口 —— 全绿。
- **未决仍挂起**：Q-09、Q-10、Q-11、**Q-12**（四者都在 `schema.md` §12）；旧账四笔（dev-plan L66「30/6」→31/5、种子 `LK-OR-001.relation_kind` 值域外、20 条路径软偏差、ADR 反向清单缺后缀）。
- **下一步**：**阶段2 已收口** → 进入 **阶段3 · M1 平台任务程序 F-01~F-06**（`task-runner`：目标与口径版本化、两阶段调度、人工节点研究建议、追问与版本派生、**任务记录与异常恢复 F-06**——F-26 的 `task-state.js` 正好是它的复用面）。

## 阶段3 · M1 F-01 研究目标登记与口径管理（2026-09-19，已提交）

- **交付**：新建目录 `server/task-runner/`——`step-plan.js`（共用骨架件：`TYPE_STEPS` 四类任务步骤模板逐字对齐 `prototype/pages/tasks.html`、`TASK_TYPE_STAGE`、`nextTaskId`/`createTask`、`planTaskSteps`/`advanceStep`/`refreshProgress`、`LNK-04 task_object` 关联）、`goal.js`（F-01 本体）、`test-f01.mjs`（**88 断言全绿**）、`README.md`、`../api/index.js` 尚未接路由（F-01 的路由随下一个 F 一并接）。
- **开工自检四项预审（用户全采推荐口径）**：① F-02 的 `run_frequency` 支持**四式**（每日 HH:MM / 每日 多时点 / 每 N 小时 / 每周X HH:MM，其余报错，最小粒度 1 分钟）；② F-04 建议与机会版本冲突 → **以机会为准 + `version_mismatch` 提示**；③ F-06 矩阵第 6 行「研究完成但依据不足」→ **`done` + 写 `PD-03(insufficient_basis)` 留痕**；④ 队列投递 → **可注入 `enqueue` 端口 + spy 断言消息恰含 `{task_id, step_no}`**。
- **★ 五处上游缺陷（发现即登记，未擅自改；落 `schema.md` §12 Q-13/Q-14/Q-15）**：① 种子 `gap_rule.target_field` 的 GAP-3=`scope`、GAP-4=`period` **越 `dict:GOAL_FIELD` 值域**（Q-13）；② M1 上游登记/引用失真四处——`dev-plan` 阶段3 表清单漏 `LNK-04 task_object`、`PRD-M1` F-01 关联 schema 漏 `PD-01`、`test-M1` TC-I-M1-002 把 `idempotency_key` 的表号写成 PD-05（实为 MD-12）、`server/README` task-runner 行漏 F-01（Q-14）；③ 种子 `T-1023.parent_task_id='T-1021'` 与自身 `trigger_basis`（在 R-007 上追问）+ `task_object` output=R-007 **不自洽**（T-1021 产出的是 R-006）（Q-15）。
- **★ 值得复用的经验：反向清单只记「既有事实」，不预告未建文件名。** 首轮我按「预计会被 `./schedule.js`、`./proposal.js` 引用」写进文档卡，`scripts/ref-check.mjs` 立刻报 **10 条悬空引用**（判据：文本里出现 `./x.js` 就查仓库有无同名文件）。改法是把未来引用写成**不带文件名的表述**（「被后续 F-02/F-03 的实现引用，各自落地时补记」）。**下游文件名补记的时点＝该文件真正落地的那次提交。**
- **★ `dictCodes` 的 ESM 坑**：`export { X } from "mod"` 是**导出转发**，**不会**在本模块作用域创建 `X` 的本地绑定——`createTask` 里直接用 `dictCodes` 会 `ReferenceError`。凡本模块既要用、又要转发，必须**先 `import` 再 `export`**。
- **L3 反例的正确断言姿势**：D1 适配层的 `run()` 把库级错误**收成返回值** `{success:false, error}`（不抛），所以 `try/catch` 抓不到——反例断言要写成 `const r = await ...run(); assert(r.success === false && /FOREIGN KEY/.test(r.error), ...)`。`first()`/`all()` 仍会抛，两类不要混。
- **实测**：`test-f01.mjs` **88/88 绿**（含 TC-I-M1-005 版本不混期：保存 v4 后 v3 **逐字节未变**；`is_applied` 应用回退后**恒 1 行**；TC-D-M1-001/002/006 三个 L3 反例）。全量回归 F-07~F-12(5/7/11/20/14/14) + F-23(38) + F-24(74) + F-25(53) + F-26(100) + **F-01(88)** + 引用自检 **0 命中** + `node --check` + 实体转义自检全绿。
- **下一步**：F-02 机会发现任务调度（`run_frequency` 四式解析 + `CFG-04` 策略选取 + 建 discovery 任务 + 五步 + F-12 上下文装配 + `enqueue` 端口）。

## 阶段3 · M1 F-02 / F-03 / F-04（2026-09-19，同日接续，已提交；此处补记）

> F-01 之后、F-05 之前的三个功能点在同一天的接续轮次中完成并各自提交，原上下文被摘要裁剪未落盘，这里补一条极简索引供后续会话续读。

- **F-02 机会发现任务调度**（`schedule.js`，**86 断言全绿**）：`CFG-04` 策略登记（写入侧从严：retry_limit>100 / max_duration_min>15 / 频率不可解析一律拒）与选取（目标级优先→回落平台级）、`run_frequency` 四式→Cron Triggers（最小粒度 1 分钟 / 250 条上限）、Queue 消息恰 `{task_id, step_no}` 两键 + `delegateToAgent` 守卫、`createDiscoveryTask` 建任务→`LNK-04`→五步→`CFG-06` 上下文现读→发消息。
- **F-03 研究建议管理（人工节点）**（`proposal.js`，**82 断言全绿**）：`MD-12` 建议登记（研究问题必需、可选列 `varchar(300)` 拒超长）、幂等键＝机会+问题+假设+限制的 SHA-256 摘要（`UK` 落地「同键不新建、不重复启动相同任务」，用 JSON 序列化而非分隔符拼接避免撞键/爆列长）、建议与机会版本绑定（版本号从机会现读）、研究问题不明确只提示不阻断、机会 `candidate→submitted` 留 `PD-05`（改行经 F-10 单一写入面）、`markProposalTriggered` 守住二次启动。
- **F-04 HVA 研究任务调度**（`hva.js`，**45 断言全绿**）：建议提交后建 `hva_research` 任务→`LNK-04` 锚点→五步→按 `CFG-06 hva_research` 模板落 `PD-06` 二阶段上下文（selected_opp 关联机会、product_question 关联建议）→取 `CFG-03` 按 `hva-agent` 工具权限→幂等守卫同一建议不重复启动→发 Queue 消息恰两键 + `delegateToAgent` 守卫；**第二阶段启动时点＝建议提交**、**版本冲突以机会为准并提示**、**`hva_followup` 创建归 F-05**。
- 三者与 F-01 共用骨架件 `step-plan.js`；均零外部调用；均已登记各自 README 段、`server/README.md`、`AGENTS.md`、`ci.yml`（validate 各 +1 步）。

## 阶段3 · M1 F-05 追问与版本管理（2026-09-19，已提交 main）

- **分支** `F-05-followup-version`（自 main `7ffc3b3`/F-04 起），**已 `git merge --ff-only` 快进 main 至 `932bb0d`**（本地快进；远端 main 仍受 PR 审核门禁，按「一 F-xx 一 PR」纪律须走 PR 合入）。**M1 六点已收口五点，剩 F-06。**
- **产出**：`server/task-runner/followup.js`（**F-05 本体，267 行**）、`test-f05.mjs`（**57 断言全绿**）、`api/index.js` 接入 `POST /api/followup-tasks` + `GET /api/followup-tasks/{task_id}`；`hva.js` doc 注 ⑦ 显式指向 `createFollowupTask`（消除悬空引用）；登记 `task-runner/README.md`（文件清单 + F-05 用例表 + §8 路由 + §8.1 口径六条 + 反向清单下游补 F-05 + 文件引用 + 种子基线 `followup_message` 由 2 行更正为 3 行 MSG-001~003）、`server/README.md`（状态位/模块表/api 路由）、`AGENTS.md`（状态位）、`ci.yml`（validate + F-05 用例 + 文档卡函数列表）。
- **口径落地（白盒，逐条可测）**：① 关联原研究建新 `hva_followup` 任务——`parent_task_id`＝原研究 `start_task_id`（原任务）、新 `MD-07` 的 `parent_research_no`＝原研究、`start_task_id`＝新任务；② 原研究/原任务**只读不改写**（新旧版本不混）；③ 版本管理——新任务/新研究默认沿用原研究 `goal_version_no`，可显式传新版本（原研究/原任务维持启动版本不变，MD-02 新版本创建归 F-01）；④ 二阶段上下文按 `CFG-06 hva_followup` 模板落 `PD-06`（含 `related_history`）；⑤ `CFG-03` 按 `hva-agent` 取 12 条权限（同 F-04）；⑥ PD-07 追问消息双外键库级强制（TC-D-M1-008）；⑦ Queue 消息恰两键 + `delegateToAgent` 守卫。
- **★ 单一写入面（硬红线落地，值得复用）**：`followup.js` 经 `test-f05.mjs` S1/S2/S3 静态核验——**零外部调用**、**不直接写 PD-01/LNK-04/PD-06/MD-07**（全部走 step-plan / shared-context 既有写面），**仅新增 PD-07 单一写入面** `recordFollowupMessage`（含取号 `MSG-`+3补零）。本目录「不直写库」纪律未被破坏。
- **★ 踩坑（值得记）**：
  - **`nextResearchNo` 编号断言不能用字典序比较**：种子 `R-006/R-007`，库内最大+1 得 `R-008`；初版写成 `rn > "R-TEST-001"`（`R-008` 字典序小于 `R-TEST-001`）→ 56/1 失败。修法：精确断言 `rn === "R-008"`。教训：**编号类断言用精确等值，别用字符串 `<`/`>`**。
  - **`followup.js` 初版重复 import `./hva.js`**（两个独立 import 行）且 import 了未使用的 `listPermissions` —— 合并为单行 `import { HVA_AGENT_PROFILE_ID, resolveHvaToolPermissions } from "./hva.js"` 并删掉多余 import；权限实际在 `resolveHvaToolPermissions` 内部取。
  - **断言恒真 bug**：A8 初版 `r.research.research_question === ... || true` 恒真；修法先抽 `NEW_Q` 常量再 `assert(=== NEW_Q)`。
- **实测**：`test-f05.mjs` **57/57 绿**（A 默认继承版本完整建任务 / B 显式新版本 / C 原研究不存在报错 / D 无启动任务报错 / E PD-07 FK 反例×2 / F PD-01 自引用父先落×2 / G 编号推进 / S 静态核验）。**全量回归 15 执行器全绿**（F-07~F-12 + F-23~F-26 + F-01~F-05＝88/86/82/45/57）；`ref-check` **0 命中**（既有 2~3 条悬空/软偏差已登记豁免，无新增）；实体转义 **0**；`node --check` 入口 OK。
- **未决仍挂起**：Q-09~Q-15（在 `schema.md` §12，含 F-05 发现并登记的 Q-13/Q-14/Q-15）；旧账（dev-plan L66「30/6」→31/5、种子 `LK-OR-001.relation_kind` 值域外、20 条路径软偏差、ADR 反向清单缺后缀）。
- **下一步**：**F-06 任务记录与异常恢复**（阶段3 M1 最后一个点；复用 F-26 的 `task-state.js` 写入面 + `PD-03 task_block` 受阻矩阵；任务 #11~#14 已建待完成）。

## 阶段3 · M1 F-06 任务记录与异常恢复（2026-09-19 接续，已提交 main）

- **分支** `F-06-task-recovery`（自 main `932bb0d`/F-05 起），**已 `git merge --ff-only` 快进 main 至 `4b78ad9`**。**M1 六点全部收口（F-01~F-06 全落地）。**
- **产出**：`server/task-runner/recovery.js`（**F-06 本体，153 行**）、`test-f06.mjs`（**47 断言全绿**）、`api/index.js` 接入 `POST /api/task-recovery`（action: block/stop/resume）；登记 `task-runner/README.md`（文件清单 + F-06 用例表 + §8 路由 + §8.3 口径六条 + 反向清单 + 文件引用）、`server/README.md`（状态位/模块表）、`AGENTS.md`（状态位）、`ci.yml`（validate + F-06 用例 + 文档卡函数列表）。
- **★ 复用 F-26 单一写入面（最省事也最合规）**：`recovery.js` **唯一 import 来自 `../tool-executor/task-state.js`**（F-26 写入面），只写 PD-01/PD-03；零裸 SQL、零外部调用、不写 `MD-07`/`EXT-02`。`test-f26.mjs` 的静态断言要求 `task-state.js` 的 `UPDATE` 只落 `task` 表——故 **resumeTask 不改写 `task_block` 行**（PD-03 是追加式历史缺口日志），flip `blocked→running` 只在 PD-01。
- **★ 关键辨析（避免误写）**：`retry_limit` **不在 `task` 表，在 `run_policy`(CFG-04)**——所以 `capRetryLimit` 校验的是运行策略值（≤100 显式报错，TC-U-M1-002），不是任务列。BRD F-06 受阻矩阵六类已全在 `BLOCK_REASON` dict 种子里。
- **口径落地（白盒，逐条可测）**：① 受阻矩阵＝保留 `done_part`＋置 `blocked`＋写 `PD-03`；② 停止＝置 `stopped`＋停止不自动重启（前置守卫拒写）＋写 `PD-03(limit_or_cancel)`；③ resume＝`blocked`→`running`；④ 运行失败不作为否定 HVA 依据（只动 PD-01/PD-03，不碰 MD-07/EXT-02）。
- **★ 踩坑（值得记）**：
  - **`test-f26.mjs` 静态约束约束了 F-06 的写法**：其 `UPDATE` 只许落 `task` 表（L394-396）。初版想给 resume 加 `task_block` UPDATE——会直接撞 F-26 静态断言。修法：resume 只翻 PD-01 状态，block 行保持追加式只读。**复用既有写入面时先读它的静态断言，别反向改它。**
  - **种子 0 条 `task_step`，研究/证据表有数据**：F-06「研究内容与运行态分开记录」的断言改为验证 `MD-07`/`EXT-02` 不被触碰（只 PD-01/PD-03 变），而非依赖 `task_step` 行。
- **实测**：`test-f06.mjs` **47/47 绿**（U retry_limit 封顶报错 / D PD-03 FK 反例 / A blocked＋done_part 保留 / B resume / C stopped 不自动重启前置守卫 / done 不可改写 / 失败不否定 HVA 静态 / 零裸 SQL 静态）。**全量回归 16 执行器全绿**（F-07~F-12 / F-23~F-26 / F-01~F-06＝88/86/82/45/57/47）；`ref-check` **0 命中**（3 豁免 + 3 已登记悬空，无新增）；实体转义 **0**；`node --check` 入口 OK。
- **未决仍挂起**：Q-09~Q-15（含 F-05 发现并登记的 Q-13/Q-14/Q-15）；旧账（dev-plan L66「30/6」→31/5、种子 `LK-OR-001.relation_kind` 值域外、20 条路径软偏差、ADR 反向清单缺后缀）。**阶段3 M1 整体完成，下一步可进入 dev-plan 后续阶段（如阶段4 agent-orchestrator）。**

## 阶段4 M3 · F-13 角色指令配置（agent-orchestrator 打底）— 2026-09-20
- **分支/提交**：`F-13-agent-profile` → `bcc7977`，已快进合并 `main`。**首落 `server/agent-orchestrator/` 模块**（全新目录 + 模块 README）。
- **本体**：`profile.js`（115 行）= MD-13 `agent_profile` / MD-14 `skill_registry` **首个（也是唯一）写入面**；导出 `getAgentProfile`/`listAgentSkills`/`registerAgentProfile`/`bumpAgentProfileVersion`/`registerSkill`/`composeAgentVersionSnapshot`。
  - **版本管理＝同 `agent_code` 单行推进**（`current_version`/`doc_revision`），UNIQUE 不新建行；历史版本不落表，任务启动由 F-06 `task.agent_version_snapshot` 冻结。
  - **约束失败即抛**（与 F-26 `task-state.js` 一致：`run()` 失败即 `throw`），故 TC-D-M3-001/002 反例用 `assertThrows` 断言；初始版本误写成「返回 `{success:false}`」会静默假失败——已改为抛错。
- **门禁边界（重要）**：A-1 LLM 推理服务 ⬜ 未提供（external-deps §7 最大风险）；**F-13 是配置/版本管理类，不依赖实时 LLM**，故可先行；**TC-C-M3-001/TC-C-M4-001（LLM 产出契约）登记为「门禁未关闭、非发布门禁」**，待 A-1 落实后由 F-14~F-22 补验。
- **★ 踩坑**：SQLite PK 违例报 `UNIQUE constraint failed: skill_registry.skill_no`（不是「PRIMARY KEY」）——反例断言要按列精确匹配，别写「PRIMARY KEY」字面。
- **实测**：`test-f13.mjs` **30/30 绿**（读全字段 / 版本推进单行 / 注册 Skill 成功 / MD-14 PK-UK-FK 三反例 / MD-13 UNIQUE 反例 / 快照串对齐种子 / 静态零外部调用+唯一写入面）。**全量回归 17 执行器全绿**（新增 F-13＝30）；`ref-check` **0 命中**；实体转义 **0**；`api/index.js` 语法 OK。
- **双向登记**：新建 `agent-orchestrator/README.md`（文件清单+用例表+§路由+§口径五条+反向清单+种子基线）、`server/README`（状态位+模块表）、`AGENTS`（server/ 状态位）、`ci.yml`（validate + F-13 用例步 + 文档卡函数列表）。
- **下一步**：F-14~F-22（M3/M4 运行期上下文装配 + LLM 推理编排），受 A-1 门禁约束，须以 mock/demo 推进、demo 值不进断言。

## 用户裁决落地（决策收口，分支 decision-resolution-q09-q15 → 53ab523，main 已快进）

- **背景**：用户就 4 类待决项拍板——① Q-09~Q-14 按现状②收口；② 种子值域偏差（Q-13 GAP-3/4、Q-15 T-1023、C2 LK-OR-001）**改种子对齐字典**；③ C1/C3 旧账授权一次性清理；④ 外部门禁（A-1 / T-xx）维持「mock/demo 推进、demo 值不进断言、等对接方」。
- **schema.md §12**：Q-09~Q-15 全部标「已决」（Q-09~Q-12 ② 维持现状兜底并测试锁死；Q-13/Q-15 ① 改种子）；bump v1.3→v1.4，旧版归档 `.trash/schema.md-v1.3.md`（从 git HEAD 取精确副本）；C4 修正反向引用卡 `ADR-001~003` 文件名后缀（消除真悬空）。
- **种子对齐（已实测 FK ON 载入零违规）**：GAP-3 `'scope'`→`'business_scope'`、GAP-4 `'period'`→`'focus_period'`；LK-OR-001 `'related_update'`→`'superseded'`（选 superseded 而非 same_issue：OPP-014 为 OPP-009 后续精炼，且能保住 test-f10 的「同 from/to same_issue 计数＝1 不被覆盖」）；T-1023.parent_task_id `'T-1021'`→`NULL`（遵循 F-05 规则＝原研究.start_task_id，R-007.start_task_id 为 NULL，trigger/LK-TO-004(output=R-007) 维持，三者自洽）。
- **旧账清理**：C1 dev-plan 计数 30/6→**31/5**（据实核对 36 表：31 有种子 / 5 执行期写入留空：context_injection/external_validation/goal_gap/research_proposal/task_step）；C3 修 prototype/mock/README.md 10 处路径深度（`../03-locks|04-plan|05-test-cases`→`../../docs/...`）+ AGENTS.md、DEC-PACK-001.md 各 1 处；8 处叙事性 `assets/data.js|app.js` 与 external-deps T-30 历史提及 registered 进 `ref-check-allowlist.json`（kind:imprecise，消噪不擅改散文）。
- **用例同步（因种子改动波及）**：test-f01 ③ 改断言——种子 GAP-3/4 已合规，使用处不再回报别名（别名防御仍由 ② `normalizeGoalField` 单元覆盖）；test-f10 因 LK-OR-001 改 superseded 自然恢复绿。**教训**：改种子须同时回归加载真实种子的 test 执行器（test-f01/f10 都载真实种子），否则会静默破坏断言。
- **验证**：17 执行器全绿（test-f01 88 / test-f10 20 修复后复绿）；`ref-check` **0 命中**（10 豁免：2 dangling + 8 imprecise，无新增悬空/孤儿/重号/软偏差）；种子 FK ON 载入零违规；实体转义 0。
- **未决仍挂起**：外部依赖门禁 A-1（LLM ⬜）+ T-01~T-27（工具契约待对接方）；这些按④维持 mock 推进，不阻塞 F-14~F-22。
