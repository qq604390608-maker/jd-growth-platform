# 2026-09-21

## TS-10 第二遍 sweep（00:40 起）
- 首轮 sweep 模式过窄（只扫「TS-10 保持未关/仍未关闭/未决」四个短语），漏了 go-live-checklist §3 门禁C 的 TS-10 行（仍写「⬜ 最大风险 须实机验证」）、external-deps §7 T-21 行（注释说关闭、行本体仍 ⬜ 的半截登记）、05-test-cases 索引行。已全部翻正，提交 ff-only 合入。
- 教训补充：**反向清单 sweep 的模式必须从「旧状态的措辞」扩到「该编号的全部出现处逐个目检」**——编号出现次数少时直接列全比猜措辞靠谱。另：本机 BSD grep `\|` 交替不生效，一律 `-E`。
- 未完成盘点（用户问「还有哪些未完成」）已当面给出：门禁 B（远程仓库+Secrets）、门禁 C（T-01~T-10、T-20/22/24/25~27、TS-12~22 大部分）、frontend 六项登记、TS-14 --remote 补测、docs/06 runbook 待建。

## 三项自主收口（00:48~01:20，用户裁决「继续完成剩下的部分」）
1. **TS-14 --remote 补测**（ae58051）：实建远程 D1 `jd-growth-platform`（id 853be8d3，建库时账号零库零数据，探针表测毕 DROP）。R1~R3 与本地一致（默认强制/违规拒/OFF 无效）；**远程更严两条新事实**：`defer_foreign_keys` 在 d1 execute 批次内不放行（每语句即隐式事务，无窗口）、SQL 显式 BEGIN 被平台禁（code 7500）→ **远程迁移/种子必须靠语句排序满足引用完整性**。database_id 已入 wrangler.toml。
2. **docs/06-runbook 建立**（32e254e）：环境（npx 记得 -y）/本地两通道/迁移（含远程差异）/回归/探针/部署/已知运维（429 限流等）；双向登记 docs/README + AGENTS.md。ref-check 0 命中。
3. **frontend 缺口 2/7/8/12 补齐**（2776f54）：② checkGoalGaps 去重（open_gap_exists 跳过）；⑦ GET /api/dicts/{code} + app.js 启动拉字典覆盖徽标文案（失败兜底静态表）；⑧/⑫ GET /api/opportunity-readmodels 批读（上限100、missing_ids 显式回报），机会页 13→10 次取数。回归 f27~f32 + f01/f02/f09/f10/f11 + llm-client 全绿（f28 159→163）。
- 测试基建注意：frontend test 把页面 fetch 接**真实 worker.fetch**（非业务 mock），新路由天然被覆盖；绿≠字典路径真走到，需显式断言（已补 4 条）。
- 剩余未完成不变：门禁 B（远程仓库+Secrets+分支保护，全部人工）、门禁 C（外部契约 16 条）、TS-12/13/15~22 裁决类、业务背景文档、类型探针 --remote 复测（部署后）。

## 探针三件套 --remote 全闭环（08:50~09:20）
- 起因：远程 D1 已建 → 类型探针（TS-11）「部署后复测」其实立即可做。同范式顺手把 collation（TS-12 事实面）一并收口。
- **网络事实：*.workers.dev 大陆不可达**（连接重置；api.cloudflare.com 可达）。部署+curl 通道走不通 → 发明 **REST 垫片通道**：node 直接 import 探针 worker 源码（一行不改），env.DB 垫片走 D1 REST /query。范式文件 `db/probes/type/run-remote-rest.mjs`。
- 结果：type 147/148 步一致（唯一差异 boolean 绑定：JS 驱动落 integer、REST JSON 布尔落 text——判归传输层非引擎，§3「不引入 boolean」维持）；collation 6 组逐字节一致；fk 昨日已收。授权层（sqlite_version/PRAGMA 拒）线上同规则坐实。
- **workers.dev 子域名 dongzhuo.workers.dev 已注册**（用户选定名字；账号级，wrangler deploy/dev --remote 的硬前置，门禁 B 前置少一条）。探针期 worker 部署后已删，账号现零 worker。远程库现有 3 个：jd-growth-platform（业务）/ type-probe / collation-probe（探针，保留供复验）。
- 提交三笔 ff-only：293b7ed（TS-11）、e4b7f49（TS-12 事实面 + probes/README 三行翻新）、8bf8c20（runbook 同步）。
- 提交信息写入时注意：REST 参数类型不含 boolean 的语义要写清证据与判定归属，别让后人误读成引擎差异。
- 至此「我能做」清单真正清空：剩余门禁 B（远程仓库+Secrets+分支保护，人工）、门禁 C（16 条外部契约）、TS-12 策略面/TS-13/15~22 裁决、业务背景文档（等业务输入或另行裁决）。

## 剩余项四组裁决落地（09:25 起）
- 用户裁决：门禁 B 现在提供仓库地址（**URL 尚未给出，任务 104 挂起等 URL**）；业务背景写草稿；TS 裁决清单整理；提问单排优先级。
- 02defd6 业务背景草稿 `docs/00-background/business-background.md`：提炼导航型、每节标出处、显式声明真源在 BRD；评审前 BRD 不回指（防口径互牵）。docs/README + AGENTS.md 双登记，docs/ 待建清零。
- a594c0c `docs/04-plan/ts-decision-requests.md`：拍板类 8 条（TS-12 策略面/13/15/16/17/18/19/20，每条问题+备选项+影响面+不决后果）+ 复核类 TS-21 + 外部类 TS-22；tech-stack §8 头部加指针。
- d69212b 提问单 v1.1：§0.2 催办单按 P0 假性就绪（T-02/05/01/04/03）→ P1 静默错误（T-06/10/07/08）→ P2 结构（T-09/20/25/22）→ P3 归属（T-26/27）排序；**T-21 随 TS-10 收口标注，实际待催 15 条**。
- 全部 ref-check 0、实体转义 0、各一 PR ff-only。

## 门禁 B 推进（09:47~10:05）
- 用户提供仓库 github.com/qq604390608-maker/jd-growth-platform（空仓库）。HTTPS 无凭据、无 SSH key → 走方式 A：生成 ~/.ssh/id_ed25519（本机首个 SSH 密钥），用户把公钥贴 GitHub 后认证通过（qq604390608-maker）。remote 已切 SSH，main 已 push。
- **CI 首跑暴露 ci.yml 真 bug**：`d1 migrations apply` 不接受 `--yes`（Unknown argument，本地复现 exit=1）——validate 全挂。CI 非交互自动跳过确认，参数本就多余。修两处（validate L44 + deploy L137 的 --remote 同款）+ npx 加 -y，干净状态用 --persist-to 临时目录复验 exit=0。提交 6511d1e+89a6404，push 后 **validate 全绿**（迁移校验+全部用例在 CI 真实环境通过）。
- 剩余：deploy 挂在远程迁移=预期（Secrets 未配）。用户两步网页操作：Settings→Secrets 配 CLOUDFLARE_API_TOKEN/ACCOUNT_ID（现 token 已验证有 D1+Workers 权限）+ main 分支保护；配完 Re-run failed jobs 即绿。
- 教训：**本地没跑过 = CI 必炸**。ci.yml 的步骤此前从未被真实 CI 执行过（一直没远程仓库）；今后凡写 CI 步骤，本地用 `--persist-to` 干净目录模拟 CI 状态复验一次再提交。

## 门禁 B 推进（09:47~10:05）
- 用户提供仓库 github.com/qq604390608-maker/jd-growth-platform（空仓库）。HTTPS 无凭据、无 SSH key → 走方式 A：生成 ~/.ssh/id_ed25519（本机首个 SSH 密钥），用户把公钥贴 GitHub 后认证通过（qq604390608-maker）。remote 已切 SSH，main 已 push。
- **CI 首跑暴露 ci.yml 真 bug**：`d1 migrations apply` 不接受 `--yes`（Unknown argument，本地复现 exit=1）——validate 全挂。CI 非交互自动跳过确认，参数本就多余。修两处（validate L44 + deploy L137 的 --remote 同款）+ npx 加 -y，干净状态用 --persist-to 临时目录复验 exit=0。提交 6511d1e+89a6404，push 后 **validate 全绿**（迁移校验+全部用例在 CI 真实环境通过）。
- 剩余：deploy 挂在远程迁移=预期（Secrets 未配）。用户两步网页操作：Settings→Secrets 配 CLOUDFLARE_API_TOKEN/ACCOUNT_ID（现 token 已验证有 D1+Workers 权限）+ main 分支保护；配完 Re-run failed jobs 即绿。
- 教训：**本地没跑过 = CI 必炸**。ci.yml 的步骤此前从未被真实 CI 执行过（一直没远程仓库）；今后凡写 CI 步骤，本地用 `--persist-to` 干净目录模拟 CI 状态复验一次再提交。

## 决策项2 落地：生产配置种子拆分（13:10~）
- 用户裁决：方案A（提取生成 0002，0001 一字不动——30+ 用例 oracle 零影响）+ CI deploy 自动灌入。
- 上轮收尾：4204a1e（门禁B收口）push 补齐，main 与远端同步。
- 新增 `db/seed/0002_config.sql`（11 表 / 183 行）：dict_type 26 / dict_item 84 / source_registry 5 / tool_registry 12 / tool_permission 24 / gap_rule 4 / context_template 20 / agent_profile 2 / skill_registry 2 / touchpoint 3 / run_policy 1。
- **关键发现**：`run_policy.goal_id REFERENCES research_goal(goal_id)`，目标级 POL-Q3 挂 mock 目标 GOAL-2026Q3-01，整表提取会 FK 违约 → 行级过滤仅取 `goal_id IS NULL` 平台级行。
- 新增 `scripts/extract-config-seed.mjs`（提取 + `--check` 复算防漂移，0002 为派生物禁手改）与 `scripts/probe-config-seed.mjs`（node:sqlite FK ON 正向逐表断言 + 反向「含 POL-Q3 必违约」实测 FOREIGN KEY constraint failed）。
- ci.yml：validate 加 --check + 探针；deploy 在 migrations apply 后 `d1 execute --remote --file db/seed/0002_config.sql -y`（d1 execute 支持 -y，与 migrations apply 不同）；deploy 步 npx 补 -y。
- 登记：db/seed/README.md（提取规则节）+ scripts/README.md 文件清单 + go-live-checklist 决策项2 翻已决（文档 bump v1.1）。
- 提交 b5c4c55，ff-only 合 main 已 push，CI run 35565307896 盯绿中。ref-check 0 / 实体转义 0。
- 教训：**提交回执会骗人**——`git commit` 输出 "nothing to commit" 但提交实际成功（疑似 hook 重放输出）；以 `git log` 为准复核，勿盲信回执重跑。

## 补：幂等调和修复（13:55~，7d21d45）
- **自设陷阱被抓**：05bfb71（纯 docs 提交）触发的 CI run 35566538994 deploy 失败于灌种子步——0002 是纯 INSERT，第二次重放撞 PK。教训：**写「会被重放执行的 SQL 文件」必须第一天就做幂等**，不能假设只跑一次。
- 修法：提取脚本改为「头部逆拓扑序（子表在前）DELETE 全部配置行 + 拓扑序 INSERT」= 每次部署把配置表对齐到文件声明态；配置值变更走「改 0001 → 重跑提取 → 合 main」。
- 探针加 P3（同库重放行数不变），P1/P2 保持过。推送 7d21d45 后 CI 盯绿中。
- CI 确认：7d21d45 run completed success（validate + deploy 含幂等重放全绿）。决策项2 全链路收口。
- CI 确认：7d21d45 run completed success（validate + deploy 含幂等重放全绿）。决策项2 全链路收口。

## TS 拍板类八条裁决回填（14:50~14:55，c263c6f）
- 用户逐条确认：TS-12 前端排序 / TS-13 维持现状 / TS-15 长期同源 / TS-16 应用层截断 / TS-17 代码固定 / TS-18 留 prototype / TS-19 remote dev 即预览 / TS-20 双 Worker（采纳 §7.3 倾向）。
- T-24 六 code 名定案：basic-verify / journey-insight / opportunity-form / crowd-compare / behavior-check / result-assembly；T-20/T-25 框架确认成立；T-27 随 TS-18 收口。
- 回填四文件：tech-stack §8 八行、external-deps §7 四行、ts-decision-requests v1.1（裁决记录节）、go-live-checklist v1.2（待催外部项 16→11）。ref-check 0 / 转义 0。
- 纪律重申：涉及代码的（TS-16 截断、TS-20 双 Worker、MD-14 六 code 名回填）下一轮 PR 落地，不因裁决先行改代码。
- 至此技术栈侧唯一余项＝TS-21（实施前复核平台限制数字）；项目剩余全部为门禁 C 外部契约（T-01~T-10+T-22，11 条）。

## T-24 落地：MD-14 六 code 名回填（15:10~）
- generate_mock.py skills 清单 2→8 行（S-A2 basic-verify / S-A3 journey-insight / S-A4 opportunity-form / S-B2 crowd-compare / S-B3 behavior-check / S-B4 result-assembly），重生成 0001（diff 仅 skill_registry 节）。
- **白盒纪律实践**：加种子行打破 3 个既有 oracle——test-f13 ③（生效 Skill 计数 2/1→5/4，插行改用空位 S-A9 避免与真实 S-A2 混淆）、test-f13 ⑥ / test-f02 / test-f18 的快照精确串。断言改写为新事实并保留「实测 N」文案。
- 全量回归 34/34 套件绿。0002 重提取（189 行）+ 探针 P1/P2/P3 过（probe 内硬编码期望数同步 2→8——教训：探针期望值也是断言，改数据必须同步）。
- 壳文件×6（标题+建壳声明）、两 agent.md Skill 表、VERSIONS.md §2.3、agent-runtime/README.md（目录树+状态节）全部回填；external-deps T-24 行与 go-live-checklist T-24 状态位去掉「下一轮 PR」尾巴。
- grep 残留扫描「待 T-24 / code 名待 / 待 PM」清零。
- zsh 坑复现：`for f in $(ls ...)` 循环因 zsh 不分词导致通过/失败计数假象（0/1），改 while IFS= read 逐行才准。

## TS-16 落地：应用层截断＋留痕标注（15:40~，b114c10）
- 新增 server/tool-executor/text-limit.js：truncateForStorage 纯函数（上限 1,000,000 字节，UTF-8 字节计量，fatal 解码回退字符边界，[TRUNCATED <列名>] 留痕）。接 F-25 saveQueryRecord（EXT-01）与 F-09 createEvidence（EXT-02）两个单一写入面；分析面拿全量、存储面截断。
- test-ts16.mjs 16 断言（① 纯函数 ② EXT-01 端到端 ③ EXT-02 端到端）进 ci.yml；全量回归 35 套件绿。
- 两个实测坑：① d1From 适配层的 all() 忘了包 { results: [...] }，queryStatusDict 拿 undefined → 值域空，assertRecordable 报「值域为空」——**抄夹具要逐行抄全，不能凭印象**；② 默认 TextDecoder 非 fatal 会把残缺字节替换成 U+FFFD（3 字节），截断主体反而超限——**按字节截断必须 fatal+回退重试到字符边界**。
- 事实修正：PD-06 context_injection 只存 ref_object_id 引用不存内容（DDL 为证），不落大文本——从 TS-16 约束对象移除（tech-stack §4/§8 同步修正，未擅自改其他口径）。
- 至此三项「下一轮 PR」已落两项（T-24 回填 442f385、TS-16 截断 b114c10）；余 TS-20 双 Worker 拆分。

## TS-20 落地：双 Worker 部署形态（16:20~，3b3d44c）
- 新增 server/task-runner/index.js（runner 入口薄壳）+ wrangler.runner.toml（jd-growth-platform-runner，宽 cron 每分钟）+ CI deploy 两步部署。test-ts20.mjs 15 断言进 CI，全量回归 36 套件绿。
- **设计要点**：到期判定不臆造——CFG-04 无 last_run/next_run 列，从 PD-01 task 推导（task_type='discovery' 的 MAX(started_at)），以「最小间隔」（daily=1440/hourly=N*60/weekly=10080 分钟）近似「到点触发」→ **新登记 Q-17 等用户追认**（schema bump v1.7 + .trash 归档）。真 Queues 未接前投递走 createLocalEnqueue（D1 痕迹式）。
- 夹具坑：种子自带 4 行 discovery 任务干扰 MAX(started_at)——夹具改用 UPDATE 种子行时点而非新插；种子 task_step 0 行 → delegateToAgent 守卫要求步骤真实存在，先 planTaskSteps 再消费。
- 至此「下一轮 PR」三项全部落地：T-24 回填（442f385）、TS-16 截断（b114c10）、TS-20 双 Worker（3b3d44c）。技术栈侧仅余 TS-21（实施前复核）与 Q-17 追认；门禁 C 11 条外部契约不变。
- CI 确认：3b3d44c run completed success——runner Worker（jd-growth-platform-runner）**首次真实部署成功**，双 Worker 形态在远程生效。本轮（T-24 + TS-16 + TS-20）全链路收口。

## Q-17 追认收口（15:50~，ef435f1）
- 用户确认采纳近似方向①（PD-01 推导 + 最小间隔到期判定），方向② CFG-04 增列否决留档；真 Queues / per-policy cron 接入后可整体重审。
- schema bump v1.8（旧版归档 .trash，SHA 76665a6fad62）；go-live-checklist 备注同步。技术栈侧至此**全部清零**（仅余 TS-21 实施前复核，自查性质）。
- 项目剩余唯一阻塞＝门禁 C 11 条外部契约（T-01~T-10+T-22），等对接方回复 intake-questions v1.1。

## TS-21 关闭：平台限制数字复核（15:55~，1a9aa3a）
- 对照官方 D1/Queues/Workers 三页 Limits 现值（2026-09-21）逐项核对 §4 约 25 个数字：一致 20+ 项。
- **修正 2 处**：① CPU（Cron/Queue Consumer）原记 15 分钟不准——15 分钟是**墙钟**；CPU 实为 Cron <1h 间隔 30 秒 / ≥1h 15 分钟、Queue Consumer 默认 30 秒（limits.cpu_ms 可提至 5 分钟）→ runner 每分钟 cron 走 30 秒预算，轮询保持轻量（设计不变）；② Worker 体积 3MB/10MB 已过时 → 未压缩 64 MiB 两计划相同。
- **补充 1 处**：Queues 消息保留期 Free 固定 24h 不可配（Paid 可配至 14 天）。
- 三条设计结论（必须 Paid / 墙钟切步 / 消息瘦状态厚）复核后全部不变；tech-stack bump v1.4 + 归档（SHA 7a4bf3396667）；§8 TS-21 与拍板单同步翻已结。
- **至此项目内部事项全部清零**：唯一剩余＝门禁 C 11 条外部契约（等对接方回复 intake-questions v1.1）。

## 远程运行态验证收尾（16:00~）
- 双 Worker 在线确认（jd-growth-platform / jd-growth-platform-runner，均 2026-09-21T07:58Z 部署）；runner 每分钟 cron 已注册（schedules API 实查 `* * * * *`）。
- runner scheduled 处理器**真机首测通过**：`wrangler dev --remote --test-scheduled` 触发 `__scheduled`，200 OK「Ran scheduled event」，边缘运行时打真远程 D1；远程库 0 个 active goal → 无副作用（task/task_step 仍 0 行），符合 Q-17 近似口径预期。
- 远程 D1 配置新鲜度：skill_registry **8 行**（6 个新 code 全在）、dict 84 / 工具 12 / 平台策略 1、mock 0 行——MD-14 回填已随 CI 幂等灌入生效。
- 至此部署面全链路（部署→cron→处理器执行→数据面）全部有实证；内部事项与验证面均清零，唯一剩余＝门禁 C 11 条外部契约。

## 前端同源上线（16:10~，8b8d830）
- 用户两项裁决：前端现在就以同源静态资产挂线上 Worker；访问通道先用 workers.dev（自定义域名以后可补绑）。
- **关键事实澄清**：frontend/ 不是 mock 原型页——是读真库的运营工作台（F-27~F-32 六页：goal/opportunities/propose/followup/result/tasks），全站唯一网络出口 assets/api.js 只发 /api/ 同源相对路径（守卫代码级强制），原型 mock 数据被宪法禁止复制进前端。后端 21+ 条路由全在 server/api/index.js。
- 落地：wrangler.toml 加 [assets] directory=./frontend（命中静态文件平台直服，/api/* 穿透 Worker）。本地 dev 三测过：/ 200 HTML、/assets/api.js 200、/api/db-ping JSON。合 main push 触发 CI 首次上传资产。
- 实测坑：本地 dev 因 [ai] remote 绑定必须带 CLOUDFLARE_API_TOKEN（非交互环境直接拒起）——提取凭证后过。
- 访问注意：workers.dev 大陆直连不可达，看页面需代理；业务数据当前 0 行，页面显示空列表属预期。
- 上轮报的 T-26 / docs/README 两处状态位漂移仍未获用户点头，未动。
- CI 全绿（175e19b）＋线上三测过（本机代理实测）：`https://jd-growth-platform.dongzhuo.workers.dev/` 返回 200 HTML（前端外壳）、/api/health ok+ai bound、/api/dicts/OPP_STATUS 返回 3 项——**前端+API 同源正式上线**。大陆直连仍需代理；自定义域名待用户提供后可补绑。

## 状态位漂移修复收口（a443b98，用户点头「两处都改」）

- external-deps.md **v1.1→v1.2**：T-26 翻✅（DS-03+TS-20 实装收口）；§7.3 尾注的 T-25「仍待 PM」/T-27「归属仍待定」旧表述一并清除；旧版进 .trash（SHA eefe4750）。
- **连带发现第三处漂移**：external-intake-questions.md v1.1 只同步过 T-21，T-20/T-25/T-26/T-27 四项已决仍列开口（⬜ 待填）。升 **v1.2** 全量同步：待催口径 16→**11 条**（T-01~T-10＋T-22）；旧版进 .trash（SHA 256f289d）。
- **连带发现第四处漂移**：docs/README.md 03-locks 行版本号全滞后（schema 标 v1.3 实为 v1.8、tech-stack 标 v1.3 实为 v1.4）——一并校正；04-plan 索引补 go-live/ts-decision/intake 三文件；§7 未关数 18→11。
- 03-locks/README.md、04-plan/README.md 钉版引用同步。
- 复核：旧串清零、新串在场（ERE 13 处 ✅）、实体转义 0、git 7 files 合 main push（a443b98）。
- **环境坑（再证）**：本 shell 的 grep BRE 交替 `\|` 间歇失灵（同模式忽有忽无结果），一律改 `grep -E "a|b"` 或单模式逐查，勿信多模式 BRE 结果下结论。
- 至此**内部事项真正清零**：唯一实质阻塞＝门禁 C 11 条外部契约（T-01~T-10＋T-22），发 intake-questions v1.2 即可催办；自定义域名待用户提供。

## ADR-004：无外部对接方，11 条契约自拟冻结为基准 v1（a96fc52，用户裁决「自拟契约基准+允许进断言」）

- 裁决内容：①项目无外部对接方，T-01~T-10+T-22 由我方双重角色自答回填；②自造值转正允许进断言（标注基准版本）；③「真实返回」红线改述「来源可追溯」；④替换通道＝ADR 修订（真对接方出现 bump 基准 v2）。
- 落地：ADR-004 新建（docs/07-decisions/）；external-deps v1.3（§7 十一条自拟+§0.2 星号语义改述+§5/§6 定案）；tech-stack v1.5（TS-22 凭证项关闭）；mcp-client.js contract demo→baseline-v1 + test-f24 断言同步；go-live 门禁 C 翻绿（三道门禁全关）；intake-questions v1.3 转性为自拟契约记录；四个 README 登记。旧版三个均归档 .trash（SHA 在案）。
- 自拟关键值：工具清单＝§5 十二 tool_code；query_condition 统一入参；历史深度/权限按 §2 定案；失败三形（超时重试/403 受限不重试/空与不可算落 ok）——QUERY_STATUS 不扩域无 DDL；ACT 降级=临时态；确认无参与明细；无第六数据源；样本阈值 n=50 不自动截断；MCP=DS-06 五面实现冻结。
- 验证：ref-check 0 命中 PASS；server 30 套件全绿。中途踩「重号」坑：11 行替换块与单独转换的 T-22 行重复登记，ref-check 抓到后去重——**块替换前先查目标区是否已含同编号行**。
- 坑位再证：grep BRE `\|` 交替在本机恒不可靠（又踩两次），一律 grep -E。
- 后续待办（未做，不阻塞）：约 25 个测试文件头「demo 数值不进断言」注释的批量 sweep（行为不变，纯口径文案）；后续补数值断言时按「标注基准 v1」新口径写。
- 至此：三道门禁（代码/部署/外部契约）全部关闭，系统在契约基准 v1 上完整可运行。

## 口径 sweep 收口（6cb522a）：「demo 不进断言」→「契约基准 v1」

- 34 个测试/源码文件头注释批量 sweep（python 脚本有序替换，最长串优先）；mock 三文件与 tool-executor/index.js 门禁段散文级改写；ci.yml 步骤名同步。行为零变化，纯口径文案。
- **宪法核查发现**：AGENTS.md 硬红线仅「白盒原则」一条，「真实返回」红线不在宪法（在测试头/mock 头/锁定文档）——ADR-004 登记的「宪法改述」待办实际不适用，免一次宪法改版。
- tool-executor/index.js 门禁段实况更正：T-01/02/05/06/T-22/TS-22 全部已收口，`TC-I-M5-004` 不再依赖「未关闭不设门禁」豁免。
- 验证：server 30 套件 + frontend 全套 + mock verify 全绿；ref-check 0 命中 PASS；「不进断言」旧串全库清零（grep 证）。
- 运行期字符串决策：`verification.js` 的 note「真实返回」等运行期标签**未改**（语义＝「返回值来自真实查询执行而非模型编造」，在基准 v1 下 mock 即可追溯来源，满足「来源可追溯」改述；改动会牵连 test-f15 断言，收益低）。
- 至此 ADR-004 全部落账，项目无遗留内部待办。

## F-27 空库引导修复（f6810d5，用户实测报「saveGoalVersion：goal_id 必填」）

- 根因：goal_id 只由两条路自动产生（选已有目标带出 / 「＋新增六要素」新建生成）；空库（零目标）时 currentGoalId() 返回空串、页面初始停在「已有目标」分支且无守卫 → 保存打出 POST /api/goals//versions。
- 修复（goal.html 四处）：pageInit 空库自动 isNew=true；save()/btn-edit 前置守卫；下拉空选中渲染占位项。
- 用例：test-f27 新增 §⑪（空库＝纯 DDL＋仅 dict 行——业务服务端校验依赖字典值域，纯 DDL 会报「goal_status 不在值域」；130→137 断言全绿）；frontend 6 套件绿。
- 架构备忘：app.js 启动 IIFE 在 loadGoals 完成后才调 window.pageInit(U)，页面 boot 时目标列表已就绪——有种子时 curId 自动落第一项，所以种子环境的测试从未暴露空库缺陷。
- 坑位再证：grep BRE `\|` 本机恒不可靠（又踩），用 grep -E 或单模式。

## 用户线上实测发现：discovery 任务卡 0/5 步（机会为空的真实原因）

- 线上实况：GOAL-2026Q3-01 v2 已应用；T-0001（goal_check）done；**T-0002（discovery）running 0/5 步**从 09:24 挂起至今；机会 0、task-blocks 0（自洽——未执行即无受阻）。
- 根因：①runner `queue()` consumer 只调 `delegateToAgent` **阶段4 契约占位**（消费即回报，不执行 F-14/F-15/F-16 编排）；②真 Queues producer/consumer 未接（wrangler.runner.toml 明示暂不配置），投递走 createLocalEnqueue（D1 痕迹）且**无人轮询消费**——任务建了、计划好了、没人干活。
- 待办（新登记，未做）：阶段4 接线 PR——queue consumer 接真实编排执行体（discovery.js/verification.js/opportunity.js 已有实现与测试）+ 投递通道二选一（接真 Queues 或 cron 顺带轮询 D1 待投递痕迹）。
- 「32 功能点写完」≠ 业务闭环：编排实现与调度接线是两回事，此缺陷此前所有测试都未覆盖（单测各自绿、端到端无人跑）。

## 阶段4 接线 PR（discovery 调度回路接通，待提交）

- 新增 `server/task-runner/executor.js`：五步确定性编排（零 LLM）——①载入目标/时间窗 ②F-14 规划 ③真实查询经 M5（CDP/HJE/MKT/ACT）+ F-26 处置停手 ④F-14 线索聚合（或 EXT-01 ok 记录确定性重算）⑤F-16 机会形成/缺口；`runStepMessage` 统一跃迁、`runPendingDiscoveryWork` cron 自驱动（零新依赖）。
- `index.js`：queue consumer discovery→执行体、非 discovery 仍占位；scheduled 顺带 cron 驱动。`discovery.js` 工具码映射订正 4 处（MKT→mkt.benefit.issue、ACT→act.activity.list，F-14 旧码库内不存在）。
- 踩坑实录：执行体返 done 未落当前步→驱动器死循环（抽出 runStepMessage 修）；F-26 终态守卫冲突（步骤3 前置检查任务态）；证据号 F-09 不自动取号→派生 EV-<query_id>；重算包装漏 verified:true→机会 0 个静默假判；ACT 种子 degraded→夹具置 ok（远程库数据更新登记 go-live）。
- 测试：`test-stage4.mjs` 45 断言全绿（①静态零删行/改表 ②ok 全链路 ③F-26 blocked ④幂等 ⑤结构受阻 ⑥queue 路由）；test-ts20 oracle 更新（discovery→执行体）15/0；server 全量 31 套件全绿。
- 文档登记：task-runner/README.md（文件表 executor.js + 「阶段4 接线」节 + 反向清单）、dev-plan 阶段4「接线进展」、go-live-checklist §2 运维待办 3 条（远程启用 TOL-01/04/09/11、ACT 恢复、重跑发现任务验证——均 D1 数据更新不改 DDL）、ci.yml 增 test-stage4 一步、AGENTS.md server/ 状态位增补。
- 自检：ref-check 0 命中、实体转义 0（5 文件）。

## go-live §2 运维固化 + [e2e] 远程闭环冒烟（10bb82c）

- 远程配置两条 UPDATE 固化进 ci.yml deploy（幂等）：`source_registry` ACT→ok/is_mcp_ready=1；`tool_registry` 4 计划工具 is_enabled=1。**不改 0001/0002 种子**——degraded/disabled 是 mock 反例口径被测试 oracle 钉死，改种子会牵连 30+ 套件。
- 新增 `scripts/remote-e2e-smoke.mjs`：经公网 API（零凭证）POST /api/discovery-tasks → 轮询 task-dispatch 五步 → 断言 /api/opportunities > 0。门控＝commit message 含 `[e2e]`（避免每次 push 造业务数据）；GitHub runner 出网可达 workers.dev，本机走代理仍 SSL_ERROR_SYSCALL（实测）。
- 通道事实：本机 wrangler 4.135.0 可用（npx）但**未登录**；git credential 无 HTTPS token；远程验证只能走 CI（Secrets）或用户提供的 env 凭证（runbook 的 D1 REST 垫片范式 `db/probes/type/run-remote-rest.mjs`）。
- 判读要点：周末期间 runner cron 已在跑但工具未启用，T-0002 及后续自动任务预期 blocked/stopped（F-26 处置，属预期）；[e2e] 新建的任务才是启用工具后的闭环验证对象。stopped 不可 resume（F-06 守卫），blocked 可经 POST /api/task-recovery action=resume。
- 提交 10bb82c 已推送，CI 预计 ~15 分钟（validate + deploy + UPDATE + 冒烟 ≤11 分钟）。

## CI [e2e] 远程闭环反复失败——根因排查与启用步加固（49c23e6）

- 本机无法读 CI 日志（无 gh / 无 GITHUB_TOKEN / workers.dev 被墙 SSL_ERROR_SYSCALL），所有诊断靠本地读码 + 等红步自报。
- 已排除的假设（本地坐实）：① 运维 UPDATE 的 WHERE 值（ACT + 4 个 tool_code）与 0002 种子逐字一致；② `resolveRunPolicy` 读 `run_policy` 表（非 strategy 表），0002 已带平台级生效 `POL-PLAT`(is_active=1)，且 T-0002 曾创建→证明远程 goal+生效策略齐全，POST 前置不成立问题；③ 4 计划工具在 0002 是 is_enabled=0（TOL-01/04/09/11）、discovery-agent 的 tool_permission 已 allow_flag=1，ACT 在 0002 是 degraded+0。
- 关键事实：0002 每部署 `DELETE+INSERT` 重置 tool_registry/source_registry（mock 反例口径被 oracle 钉死，不能改种子），故「出关启用」每部署必跑、且在 0002 之后。
- 根因收敛到两类：A) 内联 `--command` 启用步在 CI 静默失败→工具仍 0→QUERY_RESTRICTED；B) runner cron 没在 Cloudflare 绑定触发→超时。
- 加固：新增 `db/seed/0003_enable_remote_plan.sql`（两条幂等 UPDATE，单事务），ci.yml 出关步由两次 `--command` 改为 `--file`（更稳，免 CI shell 引号坑）。[e2e] 复跑自验证；脚本已硬化打印 block_reason_code（9540365 那次）。
- 判定映射（REASON_HINTS）：QUERY_RESTRICTED→查 tool/source；CRON_NOT_FIRED→查 Cloudflare Cron Triggers 面板；TARGET_UNCLEAR/GOAL_POLICY_MISSING/AGENT_PROFILE_MISSING→远程补主数据。

## CI deploy 反复 exit 1 根因排查与 e2e 加固（待提交）
- 49c23e6 deploy job exit 1：本地坐实非 0003 SQL 问题——整文件 exec 模拟 wrangler `--file`（node:sqlite）：ACT→ok、4 工具 is_enabled=1、幂等复跑仍 4，证明 0003 写法对（0002 同 `--file -y` 已成功）。e2e 字段映射 task_status/step_no/step_state 与 DDL（task/task_step 表）一致，排除「必然超时假失败」。
- 关键缺陷：task 表无 block_reason_code/stop_reason_code/last_error_code 列（在 task_block 表 PD-03），e2e 读 d.task.block_reason_code 永远 undefined → 用户看不到根因。
- 加固（commit）：① e2e 从 deploy job 拆为独立 job（needs: deploy，[e2e] 门控）→ deploy 红=部署真问题、e2e 红=远程验证问题；② e2e 前置核对 /api/tools?is_enabled=1 + /api/sources?status=ok&mcp_ready=1 断言 0003 启用态（闸门前移，不再等任务 blocked 才猜）；③ blocked 打印卡点步骤；④ 失败经 ::error:: annotation + $GITHUB_STEP_SUMMARY 写入 Actions 摘要，根因直接可见。
- 待用户确认：49c23e6 具体哪个 step 红（出关运维待办 0003 / 部署 Worker / 部署 runner / 旧 e2e）——本次拆分后下次 CI 自动区分；若 deploy 红且是 0003 步，再深挖 wrangler 执行差异。

## CI deploy 反复 exit 1 真因定位与根治（根因＝0002 种子 FK，非 0003/非 e2e）
- 经 GitHub API（repo=qq604390608-maker/jd-growth-platform）拉到 run #31：deploy job FAILURE，e2e SKIPPED（needs deploy）。逐步定位到 step5「灌生产配置种子 0002」失败，step6(0003)/7/8 被 cascade 跳过——此前的 e2e/0003 猜测全错。
- 真因：0002 顶部 `PRAGMA foreign_keys = ON;` + 对配置表 `DELETE FROM`（source_registry/agent_profile/gap_rule 等）。业务表 query_record/evidence→source_registry、research→agent_profile、research_goal→gap_rule 有外键引用；首部署空库能过，库一有业务数据（用户配了目标/跑了任务）DELETE 即触发 FOREIGN KEY constraint failed。0001 迁移用 CREATE TABLE IF NOT EXISTS 故 step4 恒成功，对照明显。
- 根治（改生成器非手改 0002，保 --check 不漂移）：extract-config-seed.mjs 头部 `PRAGMA foreign_keys = ON`→`OFF`（DELETE+INSERT 重插入配置行主键不变，业务外键引用自然恢复）；重生成 0002 + `--check` 通过。
- 本地仿真坐实（.tmp_seed_test.mjs，已删）：D1 默认 FK ON + 注入业务数据后，旧 0002 重放报 FOREIGN KEY constraint failed；新 0002（OFF）重放成功，配置行计数不变、业务数据仍在、外键孤儿 0。
- 注：本机无 gh/token，job 日志需 admin 权限（403），但 runs/jobs 列表与 step 结论可达——后续可直接用 API 拉 CI 状态，不必再让用户贴日志。
