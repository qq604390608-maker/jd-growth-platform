# 2026-09-21

## TS-10 第二遍 sweep（00:40 起）
- 首轮 sweep 模式过窄（只扫「TS-10 保持未关/仍未关闭/未决」四个短语），漏了 go-live-checklist §3 门禁C 的 TS-10 行（仍写「⬜ 最大风险 须实机验证」）、external-deps §7 T-21 行（注释说关闭、行本体仍 ⬜ 的半截登记）、05-test-cases 索引行。已全部翻正，提交 ff-only 合入。
- 教训补充：**反向清单 sweep 的模式必须从「旧状态的措辞」扩到「该编号的全部出现处逐个目检」**——编号出现次数少时直接列全比猜措辞靠谱。另：本机 BSD grep `\|` 交替不生效，一律 `-E`。
- 未完成盘点（用户问「还有哪些未完成」）已当面给出：门禁 B（远程仓库+Secrets）、门禁 C（T-01~T-10、T-20/22/24/25~27、TS-12~22 大部分）、frontend 六项登记、TS-14 --remote 补测、docs/06 runbook 待建。

## 三项自主收口（00:48~01:20，用户裁决「继续完成剩下的部分」）
1. **TS-14 --remote 补测**（ae58051）：实建远程 D1 `jd-growth-platform`（id 853be8d3，建库时账号零库零数据，探针表测毕 DROP）。R1~R3 与本地一致（默认强制/违规拒/OFF 无效）；**远程更严两条新事实**：`defer_foreign_keys` 在 d1 execute 批次内不放行（每语句即隐式事务，无窗口）、SQL 显式 BEGIN 被平台禁（code 7500）→ **远程迁移/种子必须靠语句排序满足引用完整性**。database_id 已入 wrangler.toml。
2. **docs/06-runbook 建立**（32e254e）：环境（npx 记得 -y）/本地两通道/迁移（含远程差异）/回归/探针/部署/已知运维（429 限流等）；双向登记 docs/README + AGENTS.md。ref-check 0 命中。
3. **frontend 缺口 2/7/8/12 补齐**（2776f54）：② checkGoalGaps 去重（open_gap_exists 跳过）；⑦ GET /api/dicts/{code} + app.js 启动拉字典覆盖徽标文案（失败兜底静态表）；⑧/⑫ GET /api/opportunity-readmodels 批读（上限100、missing_ids 显式回报），机会页 13→10 次取数。回归 f27~f32 + f01/f02/f09/f10/f11 + llm-client 全绿（f28 159→163）。
- 测试基建注意：frontend test 把页面 fetch 接**真实 worker.fetch**（非业务 mock），新路由天然被覆盖；绿≠字典路径真走到，需显式断言（已补 4 条）。
- 剩余未完成不变：门禁 B（远程仓库+Secrets+分支保护，全部人工）、门禁 C（外部契约 16 条）、TS-12/13/15~22 裁决类、业务背景文档、类型探针 --remote 复测（部署后）。

## 探针三件套 --remote 全闭环（08:50~09:20）
- 起因：远程 D1 已建 → 类型探针（TS-11）「部署后复测」其实立即可做。同范式顺手把 collation（TS-12 事实面）一并收口。
- **网络事实：*.workers.dev 大陆不可达**（连接重置；api.cloudflare.com 可达）。部署+curl 通道走不通 → 发明 **REST 垫片通道**：node 直接 import 探针 worker 源码（一行不改），env.DB 垫片走 D1 REST /query。范式文件 `db/probes/type/run-remote-rest.mjs`。
- 结果：type 147/148 步一致（唯一差异 boolean 绑定：JS 驱动落 integer、REST JSON 布尔落 text——判归传输层非引擎，§3「不引入 boolean」维持）；collation 6 组逐字节一致；fk 昨日已收。授权层（sqlite_version/PRAGMA 拒）线上同规则坐实。
- **workers.dev 子域名 dongzhuo.workers.dev 已注册**（用户选定名字；账号级，wrangler deploy/dev --remote 的硬前置，门禁 B 前置少一条）。探针期 worker 部署后已删，账号现零 worker。远程库现有 3 个：jd-growth-platform（业务）/ type-probe / collation-probe（探针，保留供复验）。
- 提交三笔 ff-only：293b7ed（TS-11）、e4b7f49（TS-12 事实面 + probes/README 三行翻新）、8bf8c20（runbook 同步）。
- 提交信息写入时注意：REST 参数类型不含 boolean 的语义要写清证据与判定归属，别让后人误读成引擎差异。
- 至此「我能做」清单真正清空：剩余门禁 B（远程仓库+Secrets+分支保护，人工）、门禁 C（16 条外部契约）、TS-12 策略面/TS-13/15~22 裁决、业务背景文档（等业务输入或另行裁决）。

## 剩余项四组裁决落地（09:25 起）
- 用户裁决：门禁 B 现在提供仓库地址（**URL 尚未给出，任务 104 挂起等 URL**）；业务背景写草稿；TS 裁决清单整理；提问单排优先级。
- 02defd6 业务背景草稿 `docs/00-background/business-background.md`：提炼导航型、每节标出处、显式声明真源在 BRD；评审前 BRD 不回指（防口径互牵）。docs/README + AGENTS.md 双登记，docs/ 待建清零。
- a594c0c `docs/04-plan/ts-decision-requests.md`：拍板类 8 条（TS-12 策略面/13/15/16/17/18/19/20，每条问题+备选项+影响面+不决后果）+ 复核类 TS-21 + 外部类 TS-22；tech-stack §8 头部加指针。
- d69212b 提问单 v1.1：§0.2 催办单按 P0 假性就绪（T-02/05/01/04/03）→ P1 静默错误（T-06/10/07/08）→ P2 结构（T-09/20/25/22）→ P3 归属（T-26/27）排序；**T-21 随 TS-10 收口标注，实际待催 15 条**。
- 全部 ref-check 0、实体转义 0、各一 PR ff-only。

## 门禁 B 推进（09:47~10:05）
- 用户提供仓库 github.com/qq604390608-maker/jd-growth-platform（空仓库）。HTTPS 无凭据、无 SSH key → 走方式 A：生成 ~/.ssh/id_ed25519（本机首个 SSH 密钥），用户把公钥贴 GitHub 后认证通过（qq604390608-maker）。remote 已切 SSH，main 已 push。
- **CI 首跑暴露 ci.yml 真 bug**：`d1 migrations apply` 不接受 `--yes`（Unknown argument，本地复现 exit=1）——validate 全挂。CI 非交互自动跳过确认，参数本就多余。修两处（validate L44 + deploy L137 的 --remote 同款）+ npx 加 -y，干净状态用 --persist-to 临时目录复验 exit=0。提交 6511d1e+89a6404，push 后 **validate 全绿**（迁移校验+全部用例在 CI 真实环境通过）。
- 剩余：deploy 挂在远程迁移=预期（Secrets 未配）。用户两步网页操作：Settings→Secrets 配 CLOUDFLARE_API_TOKEN/ACCOUNT_ID（现 token 已验证有 D1+Workers 权限）+ main 分支保护；配完 Re-run failed jobs 即绿。
- 教训：**本地没跑过 = CI 必炸**。ci.yml 的步骤此前从未被真实 CI 执行过（一直没远程仓库）；今后凡写 CI 步骤，本地用 `--persist-to` 干净目录模拟 CI 状态复验一次再提交。

## 门禁 B 推进（09:47~10:05）
- 用户提供仓库 github.com/qq604390608-maker/jd-growth-platform（空仓库）。HTTPS 无凭据、无 SSH key → 走方式 A：生成 ~/.ssh/id_ed25519（本机首个 SSH 密钥），用户把公钥贴 GitHub 后认证通过（qq604390608-maker）。remote 已切 SSH，main 已 push。
- **CI 首跑暴露 ci.yml 真 bug**：`d1 migrations apply` 不接受 `--yes`（Unknown argument，本地复现 exit=1）——validate 全挂。CI 非交互自动跳过确认，参数本就多余。修两处（validate L44 + deploy L137 的 --remote 同款）+ npx 加 -y，干净状态用 --persist-to 临时目录复验 exit=0。提交 6511d1e+89a6404，push 后 **validate 全绿**（迁移校验+全部用例在 CI 真实环境通过）。
- 剩余：deploy 挂在远程迁移=预期（Secrets 未配）。用户两步网页操作：Settings→Secrets 配 CLOUDFLARE_API_TOKEN/ACCOUNT_ID（现 token 已验证有 D1+Workers 权限）+ main 分支保护；配完 Re-run failed jobs 即绿。
- 教训：**本地没跑过 = CI 必炸**。ci.yml 的步骤此前从未被真实 CI 执行过（一直没远程仓库）；今后凡写 CI 步骤，本地用 `--persist-to` 干净目录模拟 CI 状态复验一次再提交。
