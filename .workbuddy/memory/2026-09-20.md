# 工作日志 · 2026-09-20

## 阶段4 · M3 F-14 围绕目标寻找线索（agent-orchestrator）

- **交付**：`server/agent-orchestrator/discovery.js`（F-14 本体，188 行）+ `test-f14.mjs`（**33 断言全绿**）；`server/api/index.js` 接入 4 路由（`POST /api/discovery-plan` / `GET /api/discovery-context/{task_id}` / `POST /api/clue-summary` / `POST /api/intention-score`）。提交 **`7420dd3`**，main 已快进。
- **白盒口径**：F-14 为**纯编排层零写库**（与 F-13 不同，**无独立写面**）——三项纯计算（`normalizeIntentionScore` 意向分归一 TC-U-M3-001 / `assembleDiscoveryPlan` S-A1 确定性查证计划 / `summarizeCluesAsJourney` S-A3 线索归纳）+ 一项薄读（`loadDiscoveryContext` 复用 shared-context 读面）。唯一业务 import 来自 `../shared-context/index.js`。
- **A-1 门禁边界**：S-A1/S-A3 真实运行须 LLM（A-1 ⬜ 未提供）→ 本文件提供**确定性 fallback 编排**（查证顺序 `CDP>HJE>MKT>ACT`，对齐 PRD-M3 L74-75 数据源 + TOL-01~06）；**TC-A-M3-001/003 登记为「门禁未关闭、非发布门禁」**，断言只锁结构契约（计划有序 / 每条线索有归属）、**demo 值不进断言**。
- **验证**：18 执行器全绿（含 test-f14 33 断言）；`ref-check` 0 命中 PASS；实体转义 0；api 语法 OK。**双向登记**：agent-orchestrator/README + server/README + AGENTS + ci.yml。
- **踩坑（记，供后续 M3/M4 参照）**：
  1. **断言正则写中文近义词易静默假失败**——`stop_condition` 代码实际写「足够依据」，我断言却按「依据已足够」写，正则不匹配但只报一行失败、无堆栈；做法：**先用 `node -e` 打印实际串再定正则**。
  2. **`stripComments` + `trim().startsWith("import ")` 提多行 import 只拿到首行**（`import {`），路径在末行 `} from "..."`，按 `l.includes(路径)` 过滤会 **0 命中**；改用全文 `src.match(/from "\.\.\/shared-context\/index\.js"/g)` 计数。
  3. 纯编排层无 DB 依赖，`loadDiscoveryContext` 用 **fake-db stub**（`prepare().bind().first()` 返回 `{}` truthy）即可验证「复用读面 + 零写」，不必起真实 D1。
- **下一步**：F-15 基础查证（五查口径 + 经 M5 真实查询，可复用 `normalizeIntentionScore` 与 `loadDiscoveryContext` 注入清单）。

## 阶段4 · M3 F-15 基础查证（agent-orchestrator）

- **交付**：`server/agent-orchestrator/verification.js`（F-15 本体，544 行）+ `test-f15.mjs`（**87 断言全绿**）；`server/api/index.js` 接入 6 路由（`POST /api/metric-verification` / `POST /api/five-checks` / `POST /api/evidence-isolation` / `POST /api/missing-fields` / `POST /api/verification-evidence` / `POST /api/verification-query`）。提交 **`7c7b3c9`**，main 已快进。
- **白盒口径（本点的关键设计）**：F-15 是**「零自有写语句」的编排层**——查询与留痕（EXT-01）**全经 M5** `runQueryWithRecovery`，证据（EXT-02）**全经 F-09** `createEvidence`；外部访问也**只经 M5**（唯一出口，TC-I-M3-002）。⑧ 静态断言实测写语句命中 0、读语句仅面向 `gap_rule`。**扩写面时不新增写面**，是这个拆法的第一目的（沿用 F-14 的思路）。
- **硬红线「禁止以模型预期代替查询结果」的落地法（可复用）**：① `result_summary` 一律原样透传自 M5 信封；② **只有 `result_status='ok'` 才 `verified=true`**；③ 调用方传 `model_expectation`/`expected_summary` → **显式解构后丢弃**，并留 `used_model_expectation:false` / `model_expectation_ignored` 自述；④ 用例用**哨兵串**（`MODEL-FABRICATED-9.9`）断言该串不出现在任何输出字段里（含全链路一步编排）。**这四步是「红线做成可静态/运行期断言」的模板**。
- **五项检查**：来源（`query_id`/`source_id` 可回查）/ 适用范围（**目标未声明范围 → 记「未知」，不猜**，且 `all_present=false`，不把未知当通过）/ 信息时点（含**倒挂判定**）/ 已有机会（比对 MD-06，命中建议关联更新而非新建）/ 信息缺口（**规则读库内 CFG-05 `gap_rule`**，`match_pattern` 未命中即开放缺口——「命中即视为已写清」，与 `goal.js` 的 `checkGoalGaps` 同语义）。
- **两条边界 oracle**：TC-A-M3-006 倒挂隔离 = `parseStamp` 抽时点 → `info_time_point > 取数时刻` → **仅该条 `isolated`、其余照常参与、`batch_failed=false`**（不污染全量）；TC-A-M3-005 = 缺必填即 `action='request_supplement'` 且 **`draft=null`（不返回半成品）**、必填字段须显式传入不隐式取默认。
- **踩坑**：
  1. **`parseStamp` 正则写 `(\d{2})` 只认补零日期**，`2026-9-6` 直接不匹配 → 放宽为 `\d{1,2}` 再 `padStart`；**自由文本抽取的容错要按最松写，再在输出侧统一规整**。
  2. **又踩「多行 import 按行过滤只拿到首行」**（F-14 同一坑，此次是本体 `import {\n...\n} from "..."`）→ 改用 `[...src.matchAll(/from\s+"([^"]+)"/g)]` 抽全部模块说明符。**M3/M4 静态断言一律用这个写法，勿改回按行过滤**。
  3. 五查需要真库（`listOpportunities` + `gap_rule` 读面），但**纯函数段（倒挂/缺字段/解释/parseStamp）不需要 DB** → 用例按「需库 / 不需库」分段，前者 `freshDb()`、后者直接调，避免为纯函数起库。
  4. 种子 `tool_registry` 全部 `is_enabled=0`、`source_registry` 需 `is_mcp_ready=1`，**要让查询真的走通必须三步齐**：`UPDATE tool_registry SET is_enabled=1` + `UPDATE source_registry SET is_mcp_ready=1, availability_status='ok'` + `registerPermission` 一条有效期内允许授权（沿用 `test-f24` 的 `allow()` 范式）；授权对象用**自造 `qa-f15-agent`**，避免撞种子长期授权。
- **验证**：19 执行器全绿（F-15 87 断言）；`ref-check` 0 命中 PASS；实体转义 0；api 语法 OK。**双向登记**：agent-orchestrator/README（文件清单 + F-15 用例表 + §路由 6 行 + §3 口径七条 + 反向清单 + 只读参照）、server/README、AGENTS（**顺带把 AGENTS 里过期的「Q-09~Q-15 待裁决」同步为已收口**）、ci.yml。
## 阶段4 · M3 F-16 机会形成与去重（agent-orchestrator）

- **交付**：`server/agent-orchestrator/opportunity.js`（F-16 本体，388 行）+ `test-f16.mjs`（**73 断言全绿**）；`server/api/index.js` 接入 5 路由（`POST /api/opportunity-formation`（201 机会 / 200 缺口）/ `POST /api/opportunity-six-elements` / `POST /api/opportunity-basis` / `POST /api/opportunity-gap-record` / `GET /api/opportunity-next-id`）。提交 **`cc9d9e6`**，main 已快进。
- **白盒口径（本点的关键设计）**：F-16 比 F-15 更彻底——**「零自有写语句」+「零裸 SQL」**（⑨ 静态断言实测 写语句 0 / `SELECT` 0）。MD-06 归 F-10 `createOpportunity`、LNK-03 归 F-10 `linkOpportunityRelation`；**连取号与判重也不写 SQL**，改用 `shared-context` 读面 `listOpportunities` / `listOpportunityRelations` 自己算「库内最大 +1」。**唯一业务 import 来自 `../shared-context/index.js`**，判重还直接复用 F-15 五项检查的 `existing_opportunity.matches`（不另造关键词逻辑）。
- **「依据足够」做成确定性必要条件（缺一即退回缺口，不硬凑）**：`real_return`（`fact.ok && verified`，红线：不许模型预期代替）+ `source_traceable`（来源可回查）+ `not_contradicted`（未倒挂）+ `six_elements_complete`（六要素**都已评估**，接 ADR-003）+ `title_present`（`opportunity_title` 是 NOT NULL 列）。**适用范围未判定不阻断**（记进 `caveats`），因为「目标未声明范围」是合法情形，不该因此丢掉已成立的证据——由 F-03 人工节点复核。
- **「没有足够依据也是合法产出」的落地法**：缺口**不落表**（先查过 schema：**无缺口专表**，缺口的出处是 `EXT-02.missing_note` 与 `CFG-01.capability_cannot`）→ 缺口记录是**返回的确定性结构**（`scope_checked` + `info_gaps` + `affected_judgements`），且 `outcome='gap'` 时**一行都不写**（用例断 MD-06/LNK-03 行数与改前相等）。HTTP 层据此把 gap 回 **200 而非错误**（缺口不是失败）。
- **ADR-003 二态与 F-16 判据串起来了**：`unknown_item` `NULL`（未评估）→ `six_elements_complete=false` → 直接判依据不足退回缺口（**不放过漏评估**）；`''`（已评估且确无）/ 文本（有未知项）→ 计入齐全；**纯空白串 → 应用层显式拒**（库级 `NOT NULL` 拦不住空白串，与 `createOpportunity` 同口径，两层都堵）。
- **LNK-03 方向口径（登记）**：命中判重 → 仍形成新机会（新现象有独立价值）**并**落 `same_issue`。方向**对齐种子 `LK-OR-002`**：`from`＝已有机会（原机会）、`to`＝新机会。schema 只写了「关系发起方 / 指向方」，未给方向约定，故**以现有种子数据为准**（不做无依据自造）。
- **踩坑**：
  1. **同一条消息里对同一文件两次 `Edit` → 静默丢一条**（两条都回执 success，`buildGapRecord` 的形参 `sixElements` 没落上，跑测试才炸 `sixElements is not defined`）。这是 user-level 记忆里登记过的坑，**又踩一次**。做法：同文件多处改动**一次只改一处**，或合并成一段替换。
  2. **静态 import 扫描扫到文档卡里的「用法」示例**（` * 用法：import { ... } from "./opportunity.js";`）→ 报「import 语句 2 条」。**静态扫描前必须先 `stripComments`**（F-15 同类坑的变体：那次是扫到注释里的英文写词）。
  3. 曾用 `arguments[0]` 取形参外的参数（`buildGapRecord` 里读 `sixElements`）——**改成显式解构形参**；`arguments` 在函数签名变更时极易失配，且不可静态核对。
  4. 判重「复合 UK 反例」不该写在 F-16 用例里：换一个 `to` 就不撞 UK，真撞它要先撞 MD-06 的 PK；该反例**属 F-10 的 oracle（TC-D-M2-014，`test-f10` 已覆盖）**——F-16 只断「按新机会逐个登记、方向稳定」。
- **验证**：**20 执行器全绿**（F-07~F-12 / F-23~F-26 / F-01~F-06 / F-13 / F-14 / F-15 / F-16 73 断言）；`ref-check` **0 命中 PASS**；实体转义 0；api 语法 OK。**双向登记**：agent-orchestrator/README（文件清单 + F-16 用例表 9 组 + §路由 5 行 + §4 口径七条 + 反向清单 + 文件引用 + 种子基线）、server/README（状态位 + 模块表 + api 路由）、AGENTS（状态位）、ci.yml（validate 加 F-16 用例步 + 文档卡函数列表）。
- **下一步（已被 F-17 取代）**：~~F-17 两步衔接~~。

## 阶段4 · M3 F-17 两步衔接（M3 收尾）· 2026-09-20

- **交付**：`server/agent-orchestrator/handoff.js`（F-17 本体，294 行）+ `test-f17.mjs`（**66 断言全绿**）；`api/index.js` 接入 4 条路由（`POST /api/pm-decision-context`、`GET /api/handoff-gate/{opportunity_id}`、`POST /api/pm-supplement-requests`、`POST /api/handoff-package`）；提交 `960546b`，main 已快进。**M3（F-13~F-17）五点全收口**。
- **F-17 的本质是「契约/守卫」而非「计算」**：M3 产出（机会＋初步依据）→ **PM 决策上下文**（PM 输入前提）→ **人工节点**（M1 F-03）→ 交接物。四个导出：`assemblePmDecisionContext`（上下文）/ `evaluateHandoffGate`（守卫）/ `collectSupplementRequests`（反向问 PM）/ `composeHandoffPackage`（交接包）。
- **最关键的判据设计（防假通过）**：人工节点是否完成的判据＝**`MD-12 research_proposal` 有无该机会的真实建议行**，**绝不看 `opportunity_status`**。因为**种子 `OPP-012`/`OPP-010` 标 `submitted` 却无建议行**（`research_proposal` 在「有意留空」表清单里）——若以状态为判据就会**假通过**（状态说有建议、实际没有）。这一条已写成 test-f17 ② 的核心断言（「状态为 submitted 但 MD-12 无建议 → 仍不得交接」）。
- **「不自动交接」做成可实测断言**：不是靠注释声明——**不 import `../task-runner/hva.js`**、代码中**不出现** `createHvaResearchTask`（⑧ 静态断言），并用「`composeHandoffPackage` 组装前后 `task` / `hva_research` 行数不变」**运行期实测**证明不代为启动 M4（TC-I-M3-001「无人工节点时 M4 不被调用」）。`auto_handoff` 恒 `false`。
- **本点无写面、零裸 SQL**：全程只读（`getOpportunity`/`listEvidenceByOpportunity`/`getEvidence`/`listProposals`），import 恰 2 条（`shared-context` 读面 + `task-runner/proposal.js` 的 F-03 只读 `listProposals`）。**注意**：F-17 引入「本目录反向读 `task-runner`」的依赖——这是**只读**（非写面），已在反向清单登记；`handoff.js` 仍是唯一由 `api/index.js` 消费、不被本目录其它文件 import。
- **证据四要素的来源与缺口**：四要素＝来源 `source_id` / 条件 `query_condition` / 时点 `info_time_point` / 适用范围 `applicability_scope`。**`listEvidenceByOpportunity` 只 SELECT 了 4 列中的 3 列**（缺 `query_condition`），故本文件对每条 link **再 `getEvidence(evidence_id)` 取完整行**——这是必要的 N+1（机会证据通常几条），比新造一个读面更合纪律。
- **新登记待裁决项 Q-16**：`opportunity_evidence.linked_at` 在本文件定 `datetime`「关联建立时点」，而种子 10 行一律写**产出任务 ID**（`'T-1022'` 等）——该列**无 CHECK**，库级拦不住（同 Q-13 性质）。F-17 按「**只透传不解析**」推进（`linked_at` 原样带出、不当时点参与任何判定/排序），**未擅自改种子**；已挂 `schema.md` §12 Q-16，schema **bump v1.4→v1.5**、旧版归档 `.trash/schema.md-v1.4.md`。
- **踩坑**：
  1. **夹具 INSERT 照记忆里写的列名会错**：`task` 表**没有** `is_deleted`/`is_manual` 列（我凭印象写的），实际必填列是 `task_type`/`task_stage`/`goal_id`/`goal_version_no`/`task_status`/`trigger_basis`/`agent_version_snapshot`/`progress_text`/`done_part`/`started_at`/`created_at`。**造 fixture 前先读 `CREATE TABLE` 本体**，别凭 DDL 印象。
  2. **`freshDb()` 里忘了调 `seedFixture`**：夹具机会（`OPP-F17-A`）没插进去，测试跑起来才暴露（`getOpportunity` 返回 null → 报「机会不存在」）。**适配层抽出来后，务必确认每个 fixture 都被调用**。
  3. （沿用）静态断言一律先 `stripComments`；`test-f17.mjs` 的 ⑧ 段对 `handoff.js` **源码**扫描，文档卡里的 `createHvaResearchTask` 字面因此不被误判。
- **验证**：**21 执行器全绿**（F-07~F-12 / F-23~F-26 / F-01~F-06 / F-13~F-17 66 断言）；`ref-check` **0 命中 PASS**（10 豁免，无新增）；实体转义 0；api 语法 OK。**双向登记**：agent-orchestrator/README（文件清单 + F-17 用例表 8 组 + §路由 4 行 + §5 口径六条 + 反向清单 + 文件引用 + 种子基线）、server/README（状态位 + 模块表 + api 路由）、AGENTS（状态位）、ci.yml（validate 加 F-17 用例步 + 文档卡函数列表）。
- **下一步**：进 **M4（F-18~F-22，受 A-1 门禁）**——M4 是 HVA 分析 Agent 的运行期能力（研究起点 → 五查 → 结果生成 → 追问衔接），消费 F-04 已建的二阶段上下文与 F-06 任务态；须以 mock/demo 推进、demo 值不进断言。未决 **Q-16** 待裁决。

## 阶段4 · M4 F-18 角色指令配置（M4 开局）· 2026-09-20

- **交付**：`server/agent-orchestrator/role.js`（F-18 本体，240 行）+ `test-f18.mjs`（**71 断言全绿**）；`api/index.js` 接入 4 条路由（`GET /api/agent-roles/{agent_code}`、`POST /api/research-closure`、`POST /api/hypothesis-label`、`POST /api/output-boundary-scan`）；**同批落地 agent-runtime 本体**：`business-rules.md`（公共业务指令 8 条）与 `hva/agent.md`（角色指令五段）由建壳升级为本体。提交 **`26c41e4`**，main 已快进。
- **白盒口径（本点的关键设计）**：F-18 是 **「零写库 + 零裸 SQL」的装载与判定层**——MD-13/MD-14 读面**全复用 F-13 `profile.js`**（`getAgentProfile`/`listAgentSkills`/`composeAgentVersionSnapshot`），**不新增写面**（MD-13/MD-14 的唯一写入面仍属 profile.js）。**唯一 import 恰 1 条**＝`./profile.js`。
- **「不复制第二个口径」的可测形式（新范式，已补进 SOP 技能）**：叙述文本本体留 `agent-runtime/`，`role.js` 只持「段落键 / 指令编号（BR-01~BR-08）/ 判据键 + 来源回指」；`test-f18` ② 组读 `business-rules.md` 抽 `^\d+\. \*\*(.+?)\*\*` 标题列表，断言与 `BUSINESS_RULE_IDS.map(r=>r.title)` **逐条相等**——本体改标题而代码未跟随，用例立刻红。
- **F-18 验收要点「结束条件二选一」做成确定性判据**：`evaluateResearchClosure` 四路——①有回答+有 `evidence_refs` → `research_answer`；②无回答但有 `limitation_reason` → `limitation_stated`（**合法结束**，对应 BRD §7 第 4 条「不硬做」）；③二者皆缺 → `closed:false` + `missing` 两项；④有回答但无依据 → 拒（回指 BR-01）。
- **两条红线做成「拒绝型判据」（不只是注释）**：`answer_nature='hypothesis'` → 拒当研究回答（联动 `labelProductHypothesis`）；`answer_stance='unsupport'` 且 `due_to_failure=true` → 拒结束且 `negation_allowed:false`；**而「非失败的未支持」仍判为完整结果**（PRD-M4 F-21 口径）——红线旁边必须留合法反例，否则会把合法产出一起拒掉。
- **`TC-C-M4-001` 硬红线的可运行形式**：`FORBIDDEN_PRODUCTION_PATTERNS` 恰 4 项（活动配置/权益组合/预算/排期，取自 PRD-M4 §1.1.2 第 7 条 + BRD §5.2），`scanProductionActions` 返回 `clean`/`hits`（带位置），HTTP 层「含生产动作」回 **409**。七要素结构化 JSON 那部分属 A-1 LLM 契约，**登记为门禁未关闭、非发布门禁**。
- **端到端探针（本机 `node:sqlite` + 直接调 `worker.fetch`）**：10 项全符合预期——装载 200 / 不存在 404 / 有依据 201 / 空入参 200（未结束）/ 失败导致否定 200（拒）/ 假设标注 200 / 空假设 400 / 干净 200 / 含生产动作 409；**探针前后 MD-13/MD-14 行数均不变**（零写实测）。
- **⚠️ 实施发现并登记（未擅自改）**：`agent-runtime/discovery/agent.md` **仍为建壳**，其建壳声明写「待阶段4（M3）落地」，而 M3（F-13~F-17）已于 2026-09-20 收口——**F-13 当时只落了服务端 MD-13/MD-14 登记与版本管理，未落 agent.md 本体**，M3 侧角色指令本体因此形成缺口。已登记到 `agent-runtime/README.md` §状态 与 F-18 报告，**等用户裁决是否补写**。
- **踩坑（新，已补进 SOP 技能）**：
  1. **回归脚本 `2>&1 | tail -1` 会抓到 `node:sqlite` 的 `ExperimentalWarning`** → 把两个本来全绿的执行器（test-f11/test-f12）误判为 FAIL，白查一轮。判定行一律用 `2>/dev/null | grep -E "VERIFY PASS"`。
  2. **`old_string` 猜测空格会失配**：给 README 长行加锚点时猜「）与」前有空格 → `String to replace not found`。做法：`awk 'NR==<行号>' 文件 | tail -c 300` 取真实字节；锚点选**短的唯一结尾串**。
  3. **常量标题要与本体标题逐字一致**：本体写「不调生产写接口（硬红线）」而常量写「不调生产写接口」→ 逐条对齐断言失败一次；把修饰语移到正文、标题保持短。
- **验证**：**22 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-18 / F-23~F-26；F-18 71 断言）；`ref-check` **0 命中 PASS**（10 豁免，无新增）；实体转义 0（11 个改动文件全查）；api 与 role.js 语法 OK；ci.yml YAML OK。**双向登记**：`agent-orchestrator/README`（文件清单 + F-18 用例表 7 组 + §路由 4 行 + §6 口径七条 + 反向清单 + 上游引用）、`server/README`（模块进度 + api 行 + 模块表）、`AGENTS`（server/ 行 + **agent-runtime/ 行状态位**）、`agent-runtime/README`（目录树 + 状态段）、`agent-runtime/VERSIONS`（§2.1 hva 行 + §2.2 business-rules 状态）、`ci.yml`（功能点列表 + validate 加 F-18 用例步）。
- **下一步**：**F-19 研究起点处理**（M4 第二点；三分支路径选择（查证假设 / 寻找候选 / 说明限制）＋ S-B1 研究计划，接 `role.js` 的装载结果与 `evaluateResearchClosure`；受 A-1 门禁 → 确定性 fallback 编排、demo 值不进断言）。未决 **Q-16**（`opportunity_evidence.linked_at` 种子值形式）与 **`discovery/agent.md` 本体缺口** 待用户裁决。

## 阶段4 · M4 F-19 研究起点处理 · 2026-09-20

- **交付**：`server/agent-orchestrator/research-start.js`（F-19 本体）+ `test-f19.mjs`（**94 断言全绿**）；`api/index.js` 接入 4 条路由（`GET /api/research-start-context/{task_id}`、`POST /api/research-start`、`POST /api/research-suitability`、`POST /api/comparison-conditions`）。
- **白盒口径**：F-19 是 **「零写库 + 零裸 SQL」的纯编排 + 薄读层**。**import 恰 2 条**＝`./role.js`（复用 F-18 `labelProductHypothesis`——假设「不预设结论」的唯一口径，不复制第二份）＋`../shared-context/index.js`（读面 `getTaskContext`/`listResearch`）。
- **三分支路由（TC-A-M4-001）确定性优先级**：③ 适合性判定为「不适合」→ `state_limitation`；① 有候选行为假设 → `verify_hypothesis`；② 只有研究问题 → `find_candidate`。三条路径一律 **`stopped=true`、停止条件＝「路径确定」**（S-B1）。
- **两条红线做成可运行判据**：**产品假设不预先作为结论**（假设经 F-18 口径标注 → `is_conclusion=false`；输出恒带 `hypothesis_is_conclusion=false`；起点③ 即便带假设也不进结论位；起点① 计划首步即「检验假设」）；**允许找不到有足够依据的候选行为＝完整结果**（起点② 置 `allow_no_candidate=true` + `complete_result_possible=true`，不把「未支持」当失败）。
- **A-1 门禁的落地范式（本点新范式）**：适合性判断（「问题是否适合 HVA 研究」）须 LLM，但 A-1 ⬜ 未提供 → 把它做成**可注入判据**：`suitability={suitable,reason,signals}` 可传；**未传时走确定性 fallback**（`suitable=true`、`source='default_no_llm'`）并标 `llm_gated=true`——**不硬造「不适合」也不硬做**；`suitability` 形态非法（非对象 / `suitable` 非布尔）→ **报错不静默忽略**。`TC-I-M4-003` 登记为「门禁未关闭、非发布门禁」。
- **比较条件「能定则定、缺则如实登记」**：三项（涉及哪些用户 / 观察哪个阶段 / 什么结果口径）优先取显式入参，否则回落 `population_limit`／目标版本 `focus_period`／目标版本 `metric_definition`；缺项列入 `missing` **不编造、不静默回退**，但**不阻断「路径确定」**（补齐归 F-20）——即「缺信息不等于路径未定」。
- **排查证顺序＝F-20 五查顺序**（`population_comparability(CDP) > temporal_order(HJE) > metric_alignment(口径) > alternative_explanations(PIM/MKT/ACT) > information_sufficiency(汇总)`），只填**来源系统代号**（与 `CFG-01.source_id` 同域），**不内联工具名**（工具契约真源在 `external-deps.md` §5）。可按来源过滤；**不含来源的两步恒保留**。
- **夹具走真实调用链**：`test-f19` ④ 用 F-03 `submitProposal` 造建议 + F-04 `createHvaResearchTask` 造任务链（任务 + LNK-04 锚点 + CFG-06 `hva_research` 模板落 PD-06），**不手工拼任务行**；实测拿到的清单＝机会 `OPP-012`（v3）／证据 2 条（＝LNK-01 关联数）／历史 `R-007`／PD-06 留痕 3 条。
- **踩坑（新）**：**`node:sqlite` 的行对象是 null-prototype，`String(row)` 直接抛 `Cannot convert object to primitive value`**——`assembleResearchCheckSequence(only)` 原本写 `only.map(s => String(s))`，真实夹具把 CFG-01 来源**行对象**传进来即崩。改法：按形态取值（字符串直接用；对象取 `source_id`；其余形态一律忽略不参与过滤），**不靠 `String()` 兜底**。
- **⚠️ 实施发现并登记（未擅自改 · F-14 缺陷）**：`loadDiscoveryContext` 的 `goal`/`background`/`capabilities`/`sources` 四键**恒为空**——它读的是 `getTaskContext()` 返回对象的**顶层键**，而该函数只返回 `sections`（按 CFG-06 模板分组）等，并无这四个顶层键。实测真实任务链：`{goal:null, background:null, capabilities:[], sources:[]}`，而同一任务 `sections` 里这三类**确有条目**。`test-f14` ④ 只用 fake-db 断言「六键存在」故**静默通过**。F-19 按正确方式实现（从 `sections` 取值）；已登记到 `agent-orchestrator/README.md` §2 ⑥，**未改 F-14 代码**（一 F-xx 一 PR），建议后续补「值非空」断言。
- **验证**：**23 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-19 / F-23~F-26）；`ref-check` **0 命中 PASS**；实体转义 0（7 个改动文件全查）；api 语法 OK；ci.yml YAML OK。**端到端探针 10 项**：清单 200（研究问题/证据 2/历史 1）／起点① 201 `verify_hypothesis`（6 步）／起点② 201 `find_candidate`／起点③ 201 `state_limitation`（`hard_run=false`）／缺问题 400／适合性默认 200 `true/default_no_llm`／适合性非法 400／比较条件全缺 200（missing 三项）／任务不存在 404／**纯路由前后行数 8/3/1/2 → 8/3/1/2（零写实测）**。**双向登记**：`agent-orchestrator/README`（文件清单 + F-19 用例表 7 组 + §路由 4 行 + §7 口径七条 + 反向清单 + 上游引用 + 种子基线）、`server/README`（模块进度 + api 行 + 模块表）、`AGENTS`（server/ 行）、`ci.yml`（功能点列表 + validate 加 F-19 用例步）。
- **下一步**：**F-20 人群比较与行为关系检验**（M4 第三点；S-B2 人群可比性检查 + S-B3 候选行为五查；是全平台**对外部依赖最脆弱**的功能点——12 工具中 11 个 `blocks_features` 含 F-20；受 A-1/T-03/T-06/T-08/T-10 门禁 → mock/demo 推进、demo 值不进断言；消费 F-19 的比较条件与查证顺序）。未决 **Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷** 三项待用户裁决。

## 阶段4 · M4 F-20 人群比较与行为关系检验 · 2026-09-20

- **交付**：`server/agent-orchestrator/behavior-store.js`（**F-20 写入面：MD-09/MD-10 唯一写入面**，本模块**第二个**写面）+ `behavior.js`（F-20 编排层）+ `test-f20.mjs`（**138 断言全绿**）；`api/index.js` 接入 6 条路由（`POST /api/behavior-threshold`、`/api/population-comparability`、`/api/behavior-checks`、`/api/comparison-gate`、`/api/behavior-verification`、`/api/candidate-behavior`）。
- **架构拆法（本点确立）**：一个模块可以有**多个写面**——`profile.js` 管 MD-13/MD-14、`behavior-store.js` 管 MD-09/MD-10；**编排文件（`behavior.js`）零写语句、零裸 SQL**，落库全经写面文件，**取号也不写 `SELECT MAX`**（用写面读面返回的全量自算库内最大 +1）。好处同 F-26 拆分：扩写面时不动既有 oracle。
- **`behavior.js` 的 import 恰 3 条**＝`./behavior-store.js`（写面）＋`./verification.js`（**F-15 复用**：`runMetricVerification`＝经 M5 的唯一查询出口、`isolateContradictedEvidence`＝倒挂口径、`parseStamp`＝时点抽取）＋`./research-start.js`（**F-19 复用**：`RESEARCH_CHECK_SEQUENCE`＝五查顺序、`resolveComparisonConditions`＝比较条件口径）。**五查键序是 `RESEARCH_CHECK_SEQUENCE.map(s => s.check_key)` 派生出来的**，用例断言两者逐条相等——**「派生优于复制」**，改一处不可能漏另一处。
- **四条红线做成判据**：① **因果不臆断**（`causal_claim_allowed` 恒 false，即便五查全通过也只说「关联」）；② **活动存在 ≠ 用户参与**（其他解释按 `evidence_kind` 分档：只有 `participation` 能排除，`existence_only`／`none` 一律 `unexcluded`，**未排查维度记 `not_checked` 且不默认无关**）；③ **未支持也是完整结果**（`complete_result=true`/`is_failure=false`，仍写 MD-09 状态列 + 不支持条目）；④ **失败不否定结论**（`due_to_failure` 条目 `usable_as_negation=false`、**不落库为不支持条目**、转信息缺口）。
- **倒挂隔离（TC-A-M4-003）与 TC-A-M3-006 同语义 → 直接复用 F-15 `isolateContradictedEvidence`**（不重写一份）；条目身份用 `point_id`、缺失时以序号兜底（`point_ref`），保证「**仅该条失效**」可逐条核对。
- **A-1/T-03/T-08/T-10 门禁的落地（延续 F-19 范式）**：`checkPopulationComparability` 的语义结论做成**可注入判据**（`comparability={comparable,reason,signals}`）；未注入时按差异清单的 `explained` 标记做**确定性推导**；**推导不出来（无差异清单）就记「无法明确」并限制行为作用判断**，**不硬下「可比」结论**。入参形态非法一律**报错、不静默忽略**。
- **入池阈值（TC-U-M4-001）**：`judgePoolThreshold` 取**闭区间**（`composite_raw ≥ pool_threshold` → 入池）、确定性无随机、`pool_threshold` **不隐式取默认**；`Number(null)=0` 会静默放行 → 用**严数值化**（`null`/`undefined`/`""`/布尔一律拒）。不传入池两参则**不预筛**（调用方已筛）；未入池＝**合法筛选结果**，不进入五查、不写库、口径明示「不是失败」。
- **缺比较条件守卫（TC-A-M4-006）**：复用 F-19 `resolveComparisonConditions`，缺项或 `materials_available=false` → `action='request_supplement'` + **`draft=null`** + 逐项理由 + 指引；守卫在编排里**优先于五查**；退回与未入池**都不写库**。
- **夹具与查询链路**：`test-f20` ⑤ 用 F-03 `submitProposal` + F-04 `createHvaResearchTask` 造**真实任务链**（不手工拼行）；⑦ 用**注入式 transport**（复用 `prototype/mock/scenarios.js` 的 `defaultOk`）+ 自造授权（`qa-f20-agent`，不复用种子长期授权）跑通 `queryForBehaviorCheck`，并以**哨兵串**证 `model_expectation` 不进事实、无授权时 transport **零调用**。
- **新踩坑（本点）**：写 `createCandidateBehavior` 时**先留了一个无意义的占位分支**（`if (!Number.isInteger(...) && false) {}`），自己读回时才发现是噪声——**写完必读回**，别把草稿留在交付物里。
- **⚠️ 文档面同步（易漏）**：引入同目录内新 import（`behavior.js` → `verification.js`）会**改变「本目录文件间引用图」**——`verification.js` 此前「只由 `../api/index.js` 消费、不被本目录其它文件 import」，现在被 F-20 复用；模块 README 的「文件间引用」段与模块级硬红线（原写「本目录只写 MD-13/MD-14」）**都必须同改**，否则文档与现实不符。
- **验证**：**24 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-20 / F-23~F-26）；`ref-check` **0 命中 PASS**；实体转义 0（8 个改动文件全查）；api 语法 OK；ci.yml YAML OK。**端到端探针 17 项**：阈值 200/200/缺参 400／可比性 200/200/非法 400／五查 200（status=`not_supported`、isolated=1、failure=1、causal=false）/缺 at 400／守卫 200（缺 3 项）/200（齐）／一步编排 200（`request_supplement`/`out_of_pool`/`not_supported`）／落库缺条件 200 不写库／研究不存在 404／落库正常 **201**（`CB-003` + `BP-011`）／**行数 before 2/10/2/7 → after 3/11/2/7（只新增 MD-09/MD-10）**。**双向登记**：`agent-orchestrator/README`（文件清单 3 行 + F-20 用例表 8 组 + §路由 6 行 + §8 口径七条 + 反向清单 3 段 + 种子基线 + 模块级硬红线）、`server/README`（模块进度 + api 行 + 模块表）、`AGENTS`（server/ 行）、`ci.yml`（功能点列表 + validate 加 F-20 用例步）。
- **下一步**：**F-21 研究结果生成**（M4 第四点；S-B4 七要素组装 + 关键发现逐项挂证据 + 改善方向逐项对应 + 未支持亦完整 + **失败不否定**；写 MD-07/MD-08/MD-11，是本模块第三个写面；消费 F-20 的候选行为与支持情况）。未决 **Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷** 三项待用户裁决。

## 阶段4 · M4 F-21 研究结果生成 · 2026-09-20

- **交付**：`server/agent-orchestrator/result-store.js`（**F-21 写入面：MD-07 内容填充 / MD-08 / MD-11 唯一写入面**，本模块**第三个**写面）+ `result.js`（F-21 编排本体，666 行）+ `test-f21.mjs`（**136 断言全绿**）；`api/index.js` 接入 6 条路由（`POST /api/research-result`、`/api/research-result-boundary`、`/api/improvement-correspondence`、`/api/research-report`、`GET /api/research-result/{research_no}`、`GET /api/research-next-id`）。
- **`result.js` 的 import 恰 5 条**＝`./result-store.js`（写面）＋`../shared-context/index.js`（**F-11**：MD-07 读面 `getResearch`/`listResearchFindings` + **LNK-02 唯一写入面 `linkFindingEvidence`**）＋`./role.js`（**F-18**：`FORBIDDEN_PRODUCTION_PATTERNS` 禁词表）＋`./behavior.js` + `./behavior-store.js`（**F-20**：⑤ 候选行为两态键名与读面）。**编排层零 SQL 语句**（连读语句都不写，全经写面/读面），静态断言 0 命中。
- **最关键的分工裁定（本点确立）**：**MD-07 的「建行」归 F-11 `createResearch`，「内容填充」归 F-21 `updateResearchReport`** —— F-21 **不复制建行口径**。`updateResearchReport` **只改 6 个正文/状态列**（`e1_goal_statement`/`e2_scope_method`/`e4_population_diff`/`e6_limits`/`out_of_scope_note`/`research_status` + `finished_at`），**绝不触碰** `research_no`/`opportunity_id`/`goal_id`/`goal_version_no`/`parent_research_no`/`start_task_id`——启动快照与追问链因此在结构上不可能被出报告这一步改动。`TC-D-M4-001`（MD-07 FK 反例）因而**经 F-11 的建行面复现**，F-21 侧只验「研究不存在即拒」前置守卫。
- **七要素唯一真源 + 派生**：`RESULT_ELEMENTS`（7 项：键 + 条目名 + `storage` 落库回指）是唯一真源；`ELEMENT_KEYS` 由它派生，用例断言「键序由 `RESULT_ELEMENTS` 派生（不复制第二份清单）」；条目名与 `test-M4.md` TC-C-M4-001 的七要素名**逐条相等**。
- **`out_of_scope_note` 豁免硬红线扫描**（易误判点）：MD-07 的「不覆盖范围声明」本身就承载硬红线文案（「活动配置 / 权益组合 / 预算与排期不在本研究结论范围内」），**它本身不被禁词扫描**——扫描对象只有七要素正文。用例里放了**豁免反例**（把禁词放进 `e1_goal_statement` 仍须 409），防止豁免被写成「整表不扫」。
- **四条红线做成判据**：① **逐发现挂证据**（每条发现带 `evidence_refs`，缺证据的发现**不进 ③、记信息缺口**）；② **支持与不支持并列**（`support`/`unsupport` 两类 point 并列，未支持也是完整结果）；③ **`due_to_failure` 发现不进「证据与关键发现」**（转 `failure_induced_findings` 缺口，`usable_as_conclusion=false`，`checkFailureDoesNotNegate` 保证「失败不否定」——降级保留 warning、既有结论不推翻）；④ **未找到有足够依据支持候选 HVA 亦成完整结果**（完整性只由七要素齐全决定：`missing.length===0`，**不与 `negation_allowed` 混用**）。
- **改善方向逐项对应**：`validateImprovementCorrespondence` 校验每条改善方向的 `related_finding_ref` 能落到某条发现（`#N` 或 `N` 均可），对不上记 `orphan`/`malformed`；**`related_finding_ref` 是组装期校验字段、不落库**（MD-11 无指向 MD-08 的外键列，对应关系由 `reason_why` 正文承载）——曾误以为 MD-11 有 `related_finding` 列，探针报 `no such column` 后改正。
- **`saveResearchReport` 五路前置守卫（顺序即优先级）**：边界扫描 → 改善方向对应性 → 七要素齐备 → 研究存在 → 未出过报告（`priorFindings/priorActions` 非空即 `already_reported`，**不覆盖既有报告**，对齐 MD-07「追问不覆盖原研究」）。全过才动笔；四类拒绝（`boundary_violation`/`action_finding_mismatch`/`incomplete`/`already_reported`）**都 `written=false` 且零写库**。
- **写面 `result-store.js` 零 import**（只依赖注入的 `db`）；`run()` 失败**必须抛错**——`createResearchFinding`/`createImprovementAction` 初版漏了 `if (res.success === false) throw`，导致 `TC-D-M4-001/002/005` 三个 L3 反例「未抛错」而用例失败；补上后由库级 FK/UK 兜底并显式上抛。
- **取号**：`nextFindingId`/`nextActionId`/`nextFindingEvidenceLinkId` 取**全量读面自算「库内最大 +1」**，**不写 `SELECT MAX`**（`Math.max` 是编号计算、不是 SQL）。实测 `F-007`/`AC-005`/`LK-FE-009`。
- **A-1 LLM 门禁**：F-21 本体**零外部调用、不调 LLM**——七要素组装、边界扫描、对应性校验全是确定性规则；语义判断（若有）留作可注入判据。`TC-A-M4-005` 标注 ⚠️待确认(§7-T06)，故判据做成确定性规则。
- **踩坑（新，本点）**：
  1. **静态零 SQL 断言扫全文**：`/\b(SELECT)\b/` 类断言会被**注释里的字面英文词**与 `Math.max` 命中——断言要写成「不写 `SELECT MAX`」这类**精确到语句形态**的判据；散文里说「库内最大 +1」。
  2. **`node:sqlite` 适配层的 `first()` 语义必须对齐 D1**：D1 的 `first()` 返回**行对象或 null**（不是 `{success,result}` 信封）、`all()` 返回 `{results}`、`run()` 返回 `{success,meta}`。适配层把 `first()` 写成信封 → `if (existing)` 恒真 → 静默走进「幂等」分支，排查成本高。
  3. **前置守卫的判定顺序会改变返回码**：`saveResearchReport` 把「研究存在」放在「七要素齐备」之后，故**残缺入参 + 不存在研究号**拿到的是 `incomplete(200)` 而非 `404`——冒烟脚本要用**七要素齐备的入参**才能打到 404 分支。
  4. **回归脚本抓尾行判 FAIL 的老坑复现**：`2>&1 | tail -1` 会抓到 `ExperimentalWarning`；判定行用 `2>/dev/null | grep "VERIFY PASS"`。
- **⚠️ 上游文件自身失实（已就地修正并说明）**：`server/README.md` 第 14 行「模块进度」在 F-20 收口时**漏改**——仍写「F-20~F-22 待建」，而第 24 行状态位已翻 F-20 已建。本次随 F-21 一并修正为「F-20/F-21 已建 + F-22 待建」，并补上 F-20 段落。
- **验证**：**25 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-21 / F-23~F-26）；`ref-check` **0 命中 PASS**；实体转义 0（5 个改动文件 + 3 个新增文件全查）；`node --check server/api/index.js` OK；`ci.yml` YAML OK（validate 步 32 项）。**HTTP 冒烟 12 项全绿**：boundary 干净 200 / 命中禁词 409（命中 `e1_goal_statement:预算`、`e1_goal_statement:排期`）／correspondence 200／research-report 研究不存在（七要素齐）404／**落库 201**（`F-007,F-008` + `LK-FE-009,LK-FE-010`）／**重复出报告 200 `already_reported`**／GET 回查 200（`evidence_link_count` 等 12 键）／GET 不存在 404／next-id 200／**组装·扫描·拒绝路径零写入**／**落库只增 MD-08/MD-11/LNK-02、`research` 与 `task` 不变**（`research 3→3`、`task 8→8`、`research_finding 6→8`、`improvement_action 4→5`、`finding_evidence 8→10`）／重复出报告不写行。**双向登记**：`agent-orchestrator/README`（硬红线三写面 + 文件清单 + F-21 用例表 8 组 + §9 口径 9 条 + 路由 6 行 + 反向清单 + 种子基线）、`server/README`（模块进度 + api 行 + 模块表 + **修正第 14 行失实**）、`AGENTS`（server/ 行）、`ci.yml`（功能点列表 + validate 加 F-21 用例步）。
- **下一步**：**F-22**（M4 收尾；承接追问与版本管理 / M4 收口口径）。未决 **Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷** 三项仍待用户裁决。

## 阶段4 · M4 F-22 继续追问承接（M4 收尾）· 2026-09-20

- **交付**：`server/agent-orchestrator/followup-intake.js`（F-22 本体，**零写语句 / 零裸 SQL / 零外部调用 / 不调 LLM**）+ `test-f22.mjs`（**136 断言全绿**）；`api/index.js` 接入 4 条路由（`POST /api/followup-evidence-reuse`、`/api/followup-version`、`/api/followup-intake`、`GET /api/followup-intake/{task_id}`）；**M4（F-18~F-22）五点全部收口**。
- **import 恰 6 条**（全为既有读面）：`./result.js`（F-21 `loadResearchResultContext` 原结果七要素）、`./role.js`（F-18 `FORBIDDEN_PRODUCTION_PATTERNS`+`scanProductionActions` 禁词唯一口径）、`./research-start.js`（F-19 `RESEARCH_CHECK_SEQUENCE`+`resolveComparisonConditions`）、`./verification.js`（F-15 `parseStamp` 抽时点）、`../shared-context/index.js`（M2 `getEvidence`/`getResearch`/`getResearchLineage`/`listEvidenceByFinding`/`listResearch`）、`../task-runner/step-plan.js`（M1 `getTask` 读任务版本与父任务）。
- **F-22 新范式（本点确立）**：
  1. **承接本体零写、只读、不调 LLM**——与 F-19~F-21「各管一段写面」不同，F-22 是**纯读面编排器**：承接已有研究上的新问题 → 检查依据是否仍适用 → 版本口径 → 交下一轮入口，全程不落一行（`test-f22` ③ 以 7 张表行数 + `research`/`task`/`research_finding` 逐行不变实测）。
  2. **跨模块只读面首次落地**——本模块**首次经 `../task-runner` 读任务态**（此前只有 `../api/index.js` 消费目录内模块）；该依赖是阶段4 dev-plan「把真 Agent 接进 M1 调度回路」的落点，**只读不写、不新增写面**。
  3. **依据复用三态（未决 ≠ 沿用）**——`reuse`/`supplement`/`undetermined`；未声明变更默认沿用（`{}` → 全部沿用），声明了变化但无法判定是否仍适用 → `undetermined` 且 `llm_gated=true`，**既不沿用也不补查**（不硬猜）。三维度 `CHANGE_DIMENSIONS`＝时间范围 / 查询条件 / 适用范围；四态 `SCOPE_RELATIONS`（相同/更窄/更宽/未声明）；三档 `REUSE_SEVERITIES`。
  4. **保守方向优先**——更窄适用范围**沿用**（`scope_narrowed_still_applicable`）；更宽 / 未声明关系 / 无法解析**一律补查**；`parseCoverage` 从 `EXT-02.info_time_point` 抽「取数时点 + 覆盖区间」，**抽不到即 `null` 不猜**、相应依据保守计入补查。
  5. **原结果逐字节保留 + 不否定**——`intakeFollowup` 承接前后对原研究取快照**逐字节比对**（自检 `original_preserved` 恒 true）；`negates_original_conclusion` **恒 false**（对齐 MD-07 表注「追问不覆盖原研究」，与 F-21 ⑦「未出过报告」守卫互为正反面）。
  6. **版本钉版**——原任务与原研究**始终钉在启动时版本**（实测本轮追问升 v4 而原研究仍 v3、逐字未变）；声明范围变更而未升版 → 提示 `new_version_required`，**建版本行归 M1 F-01**（本步不建）。
  7. **下一轮入口 S-B1**——`chain=F-19>F-20>F-21`、`check_sequence_keys` **派生自 F-19**、比较条件**复用 F-19 口径**；追问链经 M2 `getResearchLineage` 呈现（`ancestors`/`descendants`）。
  8. **追问越界只提示不拦截**——禁词口径**复用 F-18**（不重定义），且**不拦截提问**（追问是用户输入、非产出，与 F-21 对产出正文的 409 不同）。
  9. **下游派发可注入**——未注入即不触发（`reason='not_injected'`，不重复发 Queue 消息），注入则恰调用一次。
  10. **入口守卫五路前置拒**——缺 `task_id` / 任务不存在 / 非 `hva_followup` 任务 / 原研究不存在 / 既无研究行又未给原研究编号 → 无法解析。
- **夹具两路**：① 种子原型真实剧本（`T-1023` `hva_followup` + 显式 `R-007`，**不造任何行**）；② M1 F-05 真实调用链（F-03→F-04→F-11→F-21 造 `done` 原研究 → F-05 `createFollowupTask` 造追问任务与带 `parent_research_no` 的研究壳）。
- **踩坑（新，本点）**：
  1. **文档卡的「用法」行会被静态 import 扫描命中**：`test-f22` 的 import 计数断言扫到文件头注释里的 `from "./x.js"` 示例 → 先 `stripComments(code)` 再抽 import。
  2. **夹具未跑 F-20 时 CB=0**：候选行为断言写成「说明性断言」（依赖上游未跑则不硬断言数字），避免把环境差异当缺陷。
  3. **HTTP 层 400 关键词是硬约定**：`errorResponse` 只按「不在|不存在|未解析」等关键词判 400，故 `intakeFollowup` 的三处守卫文案必须**含 400 关键词**（「须为 hva_followup 任务」「time_range 须为 {...} 且 from/to 均可解析」「applicability_relation 须为 {...} 之一」），否则误报 500。
- **验证**：**26 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-22 / F-23~F-26）；`ref-check` **0 命中 PASS**（11 条软偏差均为既有叙事性引用、无新增）；实体转义 0（6 个改动/新增文件全查）；`node --check` 三个文件 OK；`ci.yml` YAML OK（validate 步 **33 项**）；`followup-intake.js` 静态红线复核 0 命中（零写/零 SELECT/零 fetch/零 http 字面/零 node:sqlite/零 tool-executor）、import 恰 6 条。**HTTP 冒烟 12 项全绿**：reuse 200/非法 400、version 200、intake 200、非追问任务 400、任务不存在 404、原研究不存在 404、GET 200/404、未注册 404、9 张表行数 + `research`/`task`/`followup_message` 逐行不变、零写入。**双向登记**：`agent-orchestrator/README`（模块硬红线补跨模块只读面 + 文件清单 2 行 + F-22 用例表 8 组 + §10 口径 10 条 + 路由 4 行 + 反向清单 3 段 + 种子基线）、`server/README`（模块进度 + api 行 + 模块表）、`AGENTS`（server/ 行）、`ci.yml`（validate 加 F-22 用例步）。
- **下一步**：**M4 收口**。未决 **Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷** 三项仍待用户裁决；后续阶段5 / F-27 起待用户「继续」。

## 阶段5 · M6 F-27 目标配置页（前端首个功能点 · 零构建前后端分离）· 2026-09-20

- **交付**：`frontend/` 前端骨架落地（**阶段5 首个功能点**）——`pages/goal.html`（F-27 页面本体）+ `assets/api.js`（**唯一数据出口**，含两条运行期同源守卫）+ `assets/app.js`（跨页共享口径：外壳/导航/目标解析/版本与运行链派生/未读）+ `assets/base.css`（自原型沿用）+ `index.html`（外壳，非功能点）+ `test-f27.mjs`（**130 断言全绿**，10 组）。**未改 `server/api/index.js`**——F-27 只消费既有 F-01 全套路由，不重复实现上游写面。
- **前端纪律（本点确立，全部做成可静态扫描或可运行期断言的）**：
  1. **唯一网络出口**：全站只有 `assets/api.js` 出现 `fetch`（静态断言「命中恰 1 个文件」）；`app.js` 自身不发请求、一律经 `window.API`。
  2. **同源运行期守卫**：路径必须 `/api/` 开头、不得含 `://`，违反**直接抛错不发请求**（把纪律做成闸，不靠注释）；`BASE` 必须为空串。
  3. **业务数据只在服务端**：前端 `localStorage` 只存会话态（`applied` / `navUnread`），业务数据一律不落前端存储（运行期断言实测存储键）。
  4. **不复制第二个口径**：六要素键与序号取自服务端 `GOAL_FIELDS`，用例逐条比对键序。
  5. **看页面不写库**：进入页面**只读**（GET-only 断言），口径检查改为「保存新版本后自动跑一次 + 显式按钮」。
- **踩坑（新，本点，三条都值得记）**：
  1. **契约方向必须逐点对齐服务端返回结构**：`saveGoalVersion` **直接返回版本行**（不是 `{ version }`，`registerGoal` 才是 `{ goal, version }`，`fillGoalGap` 是 `{ gap, version, merged_field }`，`applyGoalVersion` 是 `{ applied_version_no, ... }`，`runGoalCheckTask` 是 `{ task, check, gaps, all_gaps }`）。页面写成 `ver.version.version_no` → 抛错**发生在渲染之前**，于是「数据库已改、界面没更新、后置动作没跑」——一次读属性错误伪装成三类独立缺陷（提示缺失 / 版本历史不刷新 / 待补项为 0）。
  2. **保存流程里「后置动作自己落提示」会吃掉前一条提示**：保存成功后自动跑口径检查，检查的 `note()` 立刻覆盖「已保存为 v4 + 影响范围」，用户等于看不到保存结果。做法：后置动作支持 `silent` 并**回报文案**，由 `save()` 合成**一条**最终提示。
  3. **静态扫描断言要按「语句形态」，不是裸关键字**：`\b(SELECT|DELETE|...)\b` 会命中 CSS 元素选择器 `select`、HTML `<select>` 标签、JS `delete` 运算符；`/http/i` 会命中错误文案里的 `HTTP 500` 字样。改成「引号内的语句形态」（`SELECT ... FROM` / `DELETE FROM` …）与「带 scheme 的地址」（`https?://`），既保判据又不误伤。
- **自伤防护（沿用并强化）**：静态扫描一律先 `stripAllComments`；执行器自身文件从受检集合排除（`test-f27.mjs`）；文档卡里出现被扫词是正常的。
- **上游缺口（登记未擅改）**：① 材料（MD-03）只读展示——`registerGoalMaterial` 未接入 `server/api` 可调用面，页面**不自造端点**；② `checkGoalGaps` 对未补充规则重跑会再落同数待补项（F-01 既有口径，用例**如实锁住 3 → 6**，不当作本点缺陷）；③ 静态资源同源绑定方式（tech-stack §7.3 TS-20）未定，**`wrangler.toml` 未加 `[assets]`**，本点不改部署配置。
- **验证**：**28 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-22 / F-23~F-26 / F-27 / ref-check 骨架）；`ref-check` **0 命中 PASS**（软偏差 20 → 16：AGENTS.md 行内路径改根相对写法消音 4 条，新增 5 条为 F-28~F-32 待建页的登记引用）；实体转义 0（7 个前端文件全查）；`node --check` ×3 OK；`ci.yml` YAML OK（**validate 步 35 项**）；`env -u NODE_PATH` 直跑 F-27 PASS（**模拟 CI**，证明仓库根装 jsdom 即可，不需要额外环境变量）。
- **双向登记**：`frontend/README.md`（新建：上游 6 段 + 交付口径硬约定表 + 目录表 + **§4 已知缺口 4 条** + 跑用例 + 反向清单）、`AGENTS.md`（`frontend/` 行状态位 `待建` → `🟡 部分已建`，含两条登记缺口；**不动编号体系故不 bump 宪法版本**，与 v2 先例一致）、`ci.yml`（功能点列表补 F-27 + validate 加「装 jsdom」与「跑 F-27」两步）。
- **下一步**：**F-28 机会列表与详情**。未决三项（**Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷**）仍待用户裁决；材料登记面接入 `server/api` 归 F-01 侧定。

## 阶段5 · M6 F-28 机会列表与详情页（前端第二个功能点）· 2026-09-20

- **交付**：`pages/opportunities.html`（F-28 页面本体，原型「列表 + 详情」两页合并的左右布局一页）+ `test-f28.mjs`（**159 断言全绿**，11 组）+ `assets/api.js` 扩 5 个既有端点封装 + `assets/app.js` 增 `OPP_FIELDS`（机会六要素口径副本）。**未改 `server/api/index.js`**——F-28 只消费既有 M2 F-09/F-10 读面与状态写面，不自造端点。
- **消费的既有路由（全部既有，零新增）**：`GET /api/opportunities?goal_id=`（列表）、`GET /api/opportunities/{id}`（读模型：本体 + `assessment` 二态 + 状态链 + 关系 + 证据关联）、`GET /api/evidence-trace/{id}`（**证据 → EXT-01 查询记录 → CFG-01 来源 + 四要素齐全性与 `traceable`**）、`PATCH /api/opportunity-status`（状态变更 + PD-05 留痕）、`GET /api/research?opportunity_id=`（已提交研究 → 研究编号）、`GET /api/source-tool-briefing`（来源名称映射 + 可用/降级）。
- **前端纪律（F-27 五条之上新增三条，同样做成运行期断言）**：
  1. **证据回查必须走回查路由**：前端不自己拼「证据 + 查询记录」——静态断言 `api.js` 含 `/api/evidence-trace/`，运行期断言点开证据触发的是该路由。
  2. **口径副本第二个实例**：`U.OPP_FIELDS` 的键序与中文标签**逐条等于**服务端 `OPPORTUNITY_SIX_ELEMENTS` / `OPPORTUNITY_SIX_ELEMENT_LABELS`（`test-f28` ② 组逐字比对）。顺带确认 `unknown_item` **不在六要素内**（它是二态判定列）。
  3. **写请求白名单**：`test-f28` ⑨ 组断言写请求**只**落在 `/api/opportunity-status`，且**不出现** `/api/query|query-records|query-recovery|tool-permission|agent-delegations`（硬红线「前端不发起任何生产写接口调用」的运行期判据）。
- **本点确立的两个模式**：
  1. **「门禁态」与「空态」刻意分开**：目标从未跑过机会发现且无产出 → 门禁（整块收起 + 指回目标配置）；跑过但本轮无产出 → 空态（并**点明 M3-F-16「没有足够依据也是合法产出」**，不外推为「没有机会」）。两者用两套夹具真实走通（新登记目标 → 门禁；再 `POST /api/discovery-tasks` → 放行 + 空态）。
  2. **「应用层判缺」用库级放行做反例**：`evidence` 四要素列皆 `NOT NULL` 但**无 CHECK**，故纯空白串 `'   '` 能插进去（实测确认）。用例先证「库级放行」，再证「应用层判四要素不齐 → 不可作为有效依据、且**不会**被误标为可回查」，把「四要素缺一不可回查」锁成**双断言**。
- **踩坑（新，本点，两条）**：
  1. **同目录静态扫描用例按「自身文件名」排除执行器 → 同目录每新增一个执行器就假失败**：`test-f27.mjs` ③ 组原本 `if (/test-f27\.mjs$/.test(f)) continue`；本点新增 `test-f28.mjs`（内含 `INSERT INTO`、`.prepare(`、`http://localhost`、`fetch(`）后被误扫，F-27 **一次红 6 条**。修法：改成按**执行器通配**排除 `/^test-f\d+\.mjs$/`，把「受检对象＝前端运行期产物」这层意图写进谓词与注释。**凡「扫一个目录里除我之外的所有文件」的用例，排除规则都不能写死自己的文件名。**
  2. **选择器工具别把元素当 `dom` 传**：`q(dom, sel)` 原实现取 `dom.window.document`，误传 `Element` 时抛 `Cannot read properties of undefined (reading 'document')`。改成 `rootOf(x) = x && x.window ? x.window.document : x`，JSDOM / Document / Element 皆可作 scope。
- **上游缺口（登记未擅改，已写进 `frontend/README.md` §4 第 5~8 条）**：① 状态处置**落库而非会话态**（原型用 `data.js` 会话覆盖；真实实现走 `PATCH /api/opportunity-status`；PD-05 `change_reason` NOT NULL → 页面给**固定语义**处置说明，不让前端编造业务口径）；② 证据链**点开才回查**；③ **机会状态字典值域无只读接口**（`dict:OPP_STATUS`），状态徽标文案沿用原型文案表，属**展示副本而非业务口径**；④ **机会页首屏取数 13 次**（外壳 3 + 本页 10：列表 1 + 逐条读模型 6 + 已提交研究 2 + 来源说明 1），因 `server/api` 无「机会批量读模型」端点，本点不越界自造。
- **验证**：**29 执行器全绿**（F-01~F-06 / F-07~F-12 / F-13~F-22 / F-23~F-26 / F-27 / **F-28** / ref-check 骨架，失败 0）；`ref-check` **0 悬空 / 0 孤儿 / 0 重号 PASS**（软偏差稳在 **16 条**，本点**未新增**）；实体转义 0（8 个改动/新增文件全查）；`node --check` ×4 OK；`ci.yml` YAML OK（**validate 步 36 项**）；`env -u NODE_PATH` 直跑 F-27/F-28 双双 PASS（**模拟 CI**）；`prototype/mock/verify.js` 仍 PASS。运行期实测：Q3 六条机会（候选 3 / 已提交 2 / 暂不研究 1）、对象筛选 品类 1 条、证据链条数＝LNK-01 行数、处置后 PD-05 `0→1→2` 行、机会行数 8→8（只改状态不删记录）。
- **双向登记**：`frontend/README.md`（§1 用例补 `TC-I-M6-002`、§2 硬约定补「不自造端点 / 两份口径各有唯一上游 / 证据回查走 evidence-trace」、§3 目录 F-28 行 `待建` → `✅ 已建` + 新增 `test-f28.mjs` 行、§4 增 4 条缺口、§5 跑用例补 F-28、§6 反向清单补执行器与「两份口径的唯一上游」）、`AGENTS.md`（`frontend/` 行状态位补 F-28 落地与四项登记缺口）、`ci.yml`（功能点列表补 F-28 + validate 加「跑 F-28」一步）、`frontend/index.html`（F-28 页 `built: true`，进度 2/6）。
## 阶段5 · M6 F-29 研究建议提交页（前端第三个功能点 · 人工节点）· 2026-09-20

- **交付**：`pages/propose.html`（F-29 页面本体，**404 行**）+ `test-f29.mjs`（**154 断言全绿**，12 组）+ `assets/api.js` 扩 5 个既有端点封装（`checkResearchProposal` / `submitProposal` / `createHvaResearchTask` / `listProposals` / `getProposalRecord`）+ `assets/app.js` 的 `gateOf` 扩到 F-29。**未改 `server/api/index.js`、未改 `server/task-runner/*`**——全部消费既有 F-03/F-04 路由，零新增端点。
- **本点是「编排落点」而非「新建能力」**：后端 F-03（建议落 MD-12 + 幂等键 + 版本绑定 + 机会状态迁移）与 F-04（建 hva_research 任务 + PD-06 上下文 + CFG-03 权限）都已在阶段3 完成。F-29 的职责是把两者**按人工节点的语义串起来**，并把 oracle 的两条要点做成可断言：① 提交后触发 F-04；② 重复提交幂等。
- **幂等的落点（本点最关键的一个设计决定）**：服务端已保证「同幂等键不新建行」，但**触发 F-04 是调用方的责任**（F-03 明确不建任务）。若页面在重复提交时仍调 F-04，会撞 F-04 的守卫（「建议已触发任务 X，不得重复启动相同任务」→ 409）。故页面**只在 `created=true` 后调用一次** F-04；幂等命中时只读既有 `triggered_task_id` 并如实提示。用例 ⑨ 组三条断言同时锁住：建议行不变、任务行不变、**且不再出现 `/api/hva-research-tasks` 请求**（不靠后端兜错）。
- **「事实行优于状态列」（本点确立的模式）**：种子（及历史数据）里存在**状态为 `submitted` 却没有对应 MD-12 建议行**的机会（`OPP-010` / `OPP-012`，实测确认）。页面若按状态列判断就会谎报「该机会已提交过研究建议」。改为以**事实行**（`listProposals` 有无行）为准，并按 M1-F-17 的同一口径（人工节点完成判据＝有无真实建议行）如实分三种呈现：有建议行 → 「已提交过研究建议（共 N 条）」；无建议行但状态 `submitted` → 「状态为已提交研究，但本平台暂无登记在案的建议行」；`deferred` → 「已标记暂不研究，记录保留」。用例 ⑤ 组以此为主断言（**反例优先**）。
- **「前端不复制第二份校验口径」（本点确立的模式）**：原型在页面里用本地正则算「需补充的要点」；真实实现改为调 `POST /api/research-proposal-checks`（服务端 `checkResearchQuestion`）。落地手法：① 页面只呈现服务端回的 `missing[]` 与 `hint`；② 用例静态断言「页面**不出现**关键词备选式（`人群|用户|新客|客群` 这类**含 `|` 的正则字面**）与 `15` 阈值，也**不硬编码**那两句提示语」；③ 加**「不误报」反例**（问题「复购」已含行为词 → 不得点名缺行为；再换「客群」→ 应点名缺行为且不误报缺人群）。注意判据是**正则字面**而不是单个词——页面 placeholder 里有「搜索 / 加购 / 人群」等词（**原型钉死，必须保留**），但它们不构成第二份匹配规则。
- **两个既有执行器的谓词修复（非放宽断言）**：`test-f28.mjs` ③ 组的排除谓词是 `/test-f2[0-9]\.mjs$/`，**匹配不到未来的 `test-f30.mjs`**（`f30` 不匹配 `f2[0-9]`）→ 未来每加一个 F-3x 执行器都会把 F-28 打红一次。统一成 `/^test-f\d+\.mjs$/.test(path.basename(f))`（与 F-27/F-29 一致）。**受检对象＝前端运行期产物**这层判据一字未动。
- **踩坑（新，本点，两条）**：
  1. **`**` 加粗语法写进了会渲染的字符串**：`renderGate()` 里写了 `挂在**真实机会**上`——`**` 在 HTML 里原样显示。文档卡 / JS 注释里用 `**` 没问题（惯例如此），但**任何会进 `innerHTML` 的字符串**必须用 `<b></b>`。自检：`node -e` 抽内联脚本后 `html.replace(/<!--...-->/g,'').match(/\*\*/g)` 数一下，非 0 就逐个看是否在注释里。
  2. **断言选词要按服务端真源推**：「复购」命中行为词表（`/行为|找品|搜索|加购|访问|复购|购买|下单/`），故用「复购」测「缺行为」必然失败。写断言前先按服务端正则自己推一遍该输入命不命中，别凭语感。
- **验证**：**30 执行器全绿**（F-01~F-29 + ref-check 骨架，失败 0；F-27 130 / F-28 159 / **F-29 154**）；`ref-check` **0 悬空 / 0 孤儿 / 0 重号 PASS**（软偏差 **16 → 15 条**，本点未新增）；实体转义 0（9 个改动/新增文件全查）；`node --check` ×5 OK；`ci.yml` YAML OK（**validate 步 36 → 37 项**）；`env -u NODE_PATH` 直跑 F-27/F-28/F-29 三个全 PASS（**模拟 CI**）；`prototype/mock/verify.js` 仍 PASS。运行期实测：MD-12 `0 → 1` 行（`PROP-001`）、task `7 → 8`（`T-1024`，`task_type=hva_research`）、**任务 `started_at` 恒等于建议 `submitted_at`**、PD-06 上下文 3 行、LNK-04 两行（`proposal/trigger` + `opportunity/output`）、PD-05 `0 → 1`、重复提交后**建议行与任务行均不变**、换问题后视为新研究（`1 → 2` 行）。
- **双向登记**：`frontend/README.md`（§1 用例补 `TC-I-M6-003`、§3 目录 F-29 行 `待建` → `✅ 已建` + 新增 `test-f29.mjs` 行、§4 增缺口 9~12（本点两条有意偏差 + 事实行口径 + 取数 6 次）、§5 跑用例补 F-29、§6 反向清单补执行器与内部引用图）、`AGENTS.md`（`frontend/` 行补 F-29 落地与「六项已登记未擅改」）、`ci.yml`（功能点列表补 F-29 + validate 加「跑 F-29」一步）、`frontend/index.html`（F-29 页 `built: true`，进度 3/6）。
- **下一步**：**F-30 研究结果页**（呈现结果七要素 + 每项关键发现挂证据来源与适用范围 + 候选 HVA 支持/不支持并列 + **已查明与仍受限内容视觉可分**）。未决三项仍待用户裁决：**Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷**。

## 阶段5 · M6 F-30 研究结果页（前端第四个功能点）· 2026-09-20

- **交付**：`pages/result.html`（F-30 页面本体）+ `test-f30.mjs`（**88 断言全绿**，7 组）+ `assets/api.js` 扩 2 个既有端点封装（`listAllResearch` / `getResearchResult`）+ `assets/app.js` 沿用 F-27/F-28 共享口径（无需新增键）。**未改 `server/api/index.js`、未改 `server/agent-orchestrator/*`**——F-30 只消费既有 F-21 `loadResearchResultContext`（`GET /api/research-result/{no}`）与 F-28 已落地的 `GET /api/evidence-trace/{id}` 与 `GET /api/research`（全量），零新增端点、零新增写面。
- **七要素唯一真源（沿用 F-21 `RESULT_ELEMENTS`）**：`e1_goal_statement`（业务目标）/ `e2_scope_method`（研究范围，按 `｜；` 拆 `.spec-line`）/ `e3_evidence_findings[{order_no,finding_id,finding_text,support_flag,limit_note,evidence_refs[]}]`（关键发现逐条挂证据）/ `e4_population_diff`（人群差异）/ `e5_candidate_hva{candidates[{candidate_id,behavior_name,behavior_status,supported[],unsupported[]}],supported_count,unsupported_count}`（候选 HVA 支持/不支持并列）/ `e6_limits`（其他解释与限制）/ `e7_improvement_actions[{order_no,action_id,target_for,problem_what,reason_why}]`（改善方向）。`out_of_scope_note` 为外层字段（不覆盖范围声明），与 e6 合并呈现「仍受限」视觉区。`boundary`（复用 F-18 `FORBIDDEN_PRODUCTION_PATTERNS` 禁词布尔）只断布尔、不锁具体禁词字面。
- **前端纪律（在 F-27 五条 + F-28 三条之上，沿用不做新罚则）**：① 唯一出口仍是 `api.js`（运行期守卫同 F-27，路径必须 `/api/` 开头、不得含 `://`、`BASE=""`）；② 证据回查复用 F-28 已落地的 `/api/evidence-trace/` 路由（不自己拼证据 + 查询记录）；③ 口径不复制第二份——七要素条目名与 `RESULT_ELEMENTS` 逐条比对、候选 HVA 两态键名（`supported`/`unsupported`）取自服务端；④ 看页面只读（GET-only 断言），选研究切换器只 `listAllResearch()` 建下拉，不写库。
- **「已查明 vs 仍受限 视觉可分」的落地法（本点核心 oracle · TC-I-M6-004）**：复用原型 `base.css` 既有两类样式——③ 每条「关键发现」套 `.res-established`（**绿左线**，语义＝已查明、有证据支撑）；⑥ `e6_limits` 套 `.doc-limits.res-limited`（**琥珀底**，语义＝仍受限、非结论）；两区在版面上分块并用不同底色/左边线，做到「一眼区分哪些是已查明的发现、哪些是仍受限的解释」。候选 HVA 区用 `.grid-2` 两列并列（`alert-ok` 支持列 / `alert-warn` 不支持列）。
- **双研究对照模式（本点确立，锁住「结论随切换重渲染 + 未支持亦是完整结果」）**：用种子里**两个对照研究**真实走通——`R-007`（`supported_count=1`，有 1 条支持的候选 HVA `CB-001`「首单后 7 日内二次找品」+ 3 支持 / 3 不支持 point）+ `R-006`（`supported_count=0`，候选 `CB-002`「首单购买家庭装」整体未支持）。断言锁：默认进 R-007 七要素逐字（e1=「业务目标：提升京东超市新客 30 天复购率（GOAL-2026Q3-01 v3）」、F-001=「搜索进入与推荐位进入的新客…分布基本一致，可比基础成立」、CB-001/BP-002「有无该行为的人群复购差异达 11.4pp」、BP-004「CDP 无用户级明细，行为发生的精确时序未逐人核对」、e6=「观察到『行为与较好表现同时出现』不等于因果」、AC-001~003 target_for）；切换器选 R-006 → 结论条变「未找到足够依据支持候选行为」+ 2 条发现 + 1 条改善方向（`AC-004`「若继续验证，需先构造消费力可比的人群分组」），证明「未支持也是完整结果、随切换重渲染」。
- **证据点开回查（复用 F-28 交互范式，非新能力）**：③ 里每条发现的证据折叠 `summary` 点开才回查 `GET /api/evidence-trace/{id}`（以 `EV-1038` 关联 `F-001` 为真实反例），断言点开后 `calls` 含该路由且回查结果四要素字段齐全；点开前不发起请求（运行期断言 `calls` 长度）。
- **踩坑（本点，已修，值得记）**：**① BRD 标题与功能描述跨行匹配失败**——断言原写「`#### F-30` 所在行含『呈现结果七要素』」，但 BRD 里「呈现结果七要素」在标题**下一行**，单行匹配 miss → 1 条失败。修法：拆成「`/#### F-30 研究结果页/` 全文件命中」+「`/呈现结果七要素/` 全文件命中」两断言。**凡「标题 + 描述」跨行的内容断言，一律按全文正则而非单行 `find`**。② 复用 F-29 已修的排除谓词（`/^test-f\d+\.mjs$/`）→ F-30 自动从受检集合排除，未再踩 F-28 旧坑。
- **验证**：**31 执行器全绿**（F-01~F-30 + ref-check 骨架，失败 0；F-27 130 / F-28 159 / F-29 154 / **F-30 88**）；`ref-check` **0 悬空 / 0 孤儿 / 0 重号 PASS**（软偏差 **15 → 15 条**，本点未新增）；实体转义 0（7 个改动/新增文件全查）；`node --check` ×4 OK；`ci.yml` YAML OK（**validate 步 37 → 38 项**：功能点列表补 F-30 + 加「跑 F-30」一步）；`env -u NODE_PATH` 直跑 F-27/F-28/F-29/F-30 四个全 PASS（**模拟 CI**）；`prototype/mock/verify.js` 仍 PASS。运行期实测：R-007 七要素全渲染 + 绿左线 6 条发现 + 琥珀底 `e6` + 两列候选 HVA（1 支持 / 3+3 point）；R-006 切换后结论条与发现数随数据重渲染；证据点开回查 1 次且四要素齐全；前端全部经 `/api/` 仅 GET、零写、localStorage 不含业务数据；63 运行期文件零引用 frontend、直调接口仍 200、无 `.json` 快照。
- **双向登记**：`frontend/README.md`（§1 用例补 `TC-I-M6-004`、§3 目录 F-30 行 `待建` → `✅ 已建` + 新增 `test-f30.mjs` 行（7 组 88 断言）、§4 增缺口 13（与本原型的有意偏差 ①：证据点开才回查，同 F-28 偏差 ②）、§5 跑用例补 F-30、§6 反向清单补执行器与 `test-f30.mjs → pages/result.html + assets/* + ../server/api/index.js + ../server/agent-orchestrator/result.js + ../db/`）、`AGENTS.md`（`frontend/` 行补 F-30 落地：③ 关键发现「已查明」绿左线 / ⑤ 候选 HVA 支持不支持两列并列 / ⑥「仍受限」琥珀底视觉可分；**F-30~F-32 三页待建 → F-31~F-32 两页待建**）、`ci.yml`（功能点列表补 F-30 + validate 加「跑 F-30」一步）、`frontend/index.html`（F-30 页 `built: true`，进度 4/6）。
- **下一步**：**F-31 / F-32**（M6 前端剩余两页）。未决三项仍待用户裁决：**Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷**。

## 阶段5 · M6 F-31 追问对话页（前端第五个功能点 · M1 F-05 写面只读封装）· 2026-09-20

- **交付**：`pages/followup.html`（F-31 页面本体）+ `test-f31.mjs`（**85 断言全绿**，7 组）+ `assets/api.js` 扩 1 个既有端点封装（`createFollowupTask` → `POST /api/followup-tasks`）。**未改 `server/api/index.js`、未改 `server/task-runner/followup.js`**——F-31 只消费既有 M1 F-05 `createFollowupTask` 写面，不自造端点。
- **消费的既有路由（全部既有，零新增）**：`POST /api/followup-tasks`（建 `hva_followup` 任务 + 新 MD-07 研究壳，F-05 写面）、`GET /api/research`（全量研究列表，建切换器）、`GET /api/research-result/{no}`（原研究七要素，取 `research_question`/`evidence_link_count`/`goal_version_no` 呈现「已注入上下文」）、`GET /api/source-tool-briefing`（可调度工具：来源名称 + 授权 `ok`／降级 `degraded` 徽标，真实字段 `source_name`/`availability_status`/`capability_can`）。
- **「不编造 Agent 结论」纪律落地（本点核心 oracle · TC-I-M6-005）**：Agent 应答**只回显 F-05 真实返回**（新任务号／新研究号／`version_changed`／`tool_permissions` 数量），不伪造「补查返回的真实结果」；落实前后端分离——结论由后端 Agent 产生，前端不臆造。
- **默认选 R-006 的真实依据（锁住「建新任务关联原研究」）**：种子 `R-006` 的 `start_task_id='T-1021'`（T-1021 为真实 hva_research 任务，可建追问）；`R-007` 的 `start_task_id=NULL`（种子内不一致），按 F-05 硬前置无法在其上建追问。页面默认选 R-006，**不擅自改种子**令 R-007 可建；断言锁：发送后 `task.task_type='hva_followup'`、`parent_task_id='T-1021'`、`research.parent_research_no='R-006'`、`original_research.research_no='R-006'`、LNK-04、task_step、PD-06 4 行、PD-07+1、原研究逐字节不变。
- **踩坑（本点，已修，两条，值得记）**：① **工具区字段名取错**——初版页面用原型 mock 字段 `x.name`/`x.status`/`x.can`，但 `getSourceToolBriefing` 真实返回是 `x.source_name`/`x.availability_status`/`x.capability_can` → 工具区空白／断言失配。修法：按真实字段渲染（含 `ok = availability_status === "ok"`）。② **`ReferenceError: target is not defined`**——`test-f31.mjs` group ③ 里 `const target` 块级作用域，group ④ 引用它做「原研究逐字节不变」比对时崩溃；修法：模块级 `let target;`，group ③ 改为赋值 `target = ...`。
- **验证**：**32 执行器全绿**（F-01~F-31 + ref-check 骨架，失败 0；F-27 130 / F-28 159 / F-29 154 / F-30 88 / **F-31 85**）；`ref-check` **0 孤儿 / 0 悬空 / 0 重号 PASS**（软偏差 16 → 16 条，本点未新增）；实体转义 0（8 个改动/新增文件全查）；`ci.yml` YAML OK（**validate 步 38 → 39 项**：功能点列表补 F-31 + 加「跑 F-31」一步）；`env -u NODE_PATH` 直跑 F-27/F-28/F-29/F-30/F-31 五个全 PASS（**模拟 CI**）。运行期实测：默认 R-006 五字段逐字渲染 + 工具徽标（CDP/HJE/PIM/MKT ok、ACT degraded）+ chips≥4 + 点 chip 填入输入框；发送 → 1 个 POST `/api/followup-tasks` 201、库新建 hva_followup 任务 + 研究壳 + LNK-04 + task_step + PD-06 4 行 + PD-07+1、原研究逐字节不变；空问题发请求前挡住、库不变；前端全部经 `/api/`、仅 GET+1 个 POST、零写、localStorage 只含 applied/navUnread、业务数据不落存储、打开页面零写；63 运行期文件零引用 frontend、直调 4 接口 200/201、无 `.json` 快照。
- **双向登记**：`frontend/README.md`（§1 用例补 `TC-I-M6-005`、§3 目录 F-31 行 `待建` → `✅ 已建` + 新增 `test-f31.mjs` 行（7 组 85 断言）、§4 增缺口 14~16（不编造结论／不自造端点／默认选 R-006）、§5 跑用例补 F-31、§6 反向清单补执行器与 `test-f31.mjs → pages/followup.html + assets/* + ../server/api/index.js + ../server/task-runner/followup.js + ../db/`）、`AGENTS.md`（`frontend/` 行补 F-31 落地：F-05 写面只读封装／已注入上下文／可调度工具真实字段／不编造结论；**F-31~F-32 两页待建 → F-32 一页待建**）、`ci.yml`（功能点列表补 F-31 + validate 加「跑 F-31」一步）、`frontend/index.html`（F-31 页 `built: true`，进度 5/6）。
- **下一步**：**F-32 任务与状态页**（M6 前端第六页，最后一项）。未决三项仍待用户裁决：**Q-16**、**`discovery/agent.md` 本体缺口**、**F-14 `loadDiscoveryContext` 取值缺陷**。

## 阶段5 · M6 F-32 任务与状态页（前端第六 / 最后一个功能点 · M1 F-06 / M5 F-26 读面）· 2026-09-20

- **交付**：`pages/tasks.html`（F-32 页面本体）+ `test-f32.mjs`（**77 断言全绿**，7 组）+ `assets/api.js` 扩 1 个只读端点封装（`listTaskBlocks` → `GET /api/task-blocks`）。**未改 `server/api/index.js`、未改 `server/task-runner/*`**——F-32 只消费既有读面（PD-03 逐次留痕读面 + MD-06/MD-07 + F-21 七要素读面），**纯只读展示页（零写请求）**，是 F-27~F-32 六页里首个**无任何写动作**的页。
- **消费的既有路由（全部既有，零新增写面）**：`GET /api/task-blocks`（PD-03 读面，逐次留痕）、`GET /api/opportunities`（MD-06，机会六要素 + 状态）、`GET /api/research`（MD-07，全量研究列表）、`GET /api/research-result/{no}`（F-21 七要素读面，取 `e5_candidate_hva.unsupported_count`）；任务列表来自目标记录 `rec.tasks`（`app.js` 已载入的 `U.recordOf(GOAL_ID).tasks`），不新增取数面。
- **三类事实分别可见（本点核心 oracle · TC-I-M6-006，种子真值 GOAL-2026Q3-01 锁进断言）**：① **机会未被选中**＝4（MD-06 中 3 candidate + 1 deferred，与 F-28 同一份事实源 `GET /api/opportunities`）；② **任务未完成**＝3（`rec.tasks` 中 running1 + blocked1 + stopped1，共 7 任务）；③ **研究未支持候选行为**＝1（R-006 的 CB-002 `not_supported`，R-007 的 CB-001 `candidate_supported`；`e5_candidate_hva.unsupported_count>0`）。断言额外锁「页内三类数字＝原表 `GET /api/opportunities` + `/api/goals/{id}.tasks` + `/api/research-result/{no}` 重算结果」一致。
- **状态机口径复用后端（不另立判据 · BRD F-06 / M5 F-26）**：`f32Reason(t, block)` 前端只做中文呈现——stopped→「已停止」、blocked 且 `block_reason_code='target_unclear'`→「等待必要信息」、其余 blocked→「等待接口恢复」；与 `server/` 既有 `PD-03`(`call_failed`/`source_unavailable`/`target_unclear`) 写面口径一致；「停止不自动重启」由后端守卫，前端只在 T-1019(`source_unavailable`,stopped) 卡片标注「保留已完成部分，不自动重启」。`stepPlan(t)` 从 `progress_text`（如「2 / 5 步」）解析已完成步数着色；5 个过滤器 chips（全部／运行中／受阻／已停止／已完成）。
- **踩坑（本点，已修，两条，值得记）**：① **返回体结构误当作裸数组**——初版把 `API.listAllResearch()` / `API.listOpportunities(GOAL_ID)`（结构为 `{items:[...]}`，非裸数组）直接赋值给 `studies`/`opps`，导致 `.filter is not a a function`、整页 render 中断（boot-notice「数据加载失败 studies.filter is not a function」）。修法：改为 `(await API.xxx()).items || []`，补齐 `|| []` 防御。② **静态断言正则误红**——断言「`listTaskBlocks` 为 GET 只读封装」用 `listTaskBlocks\s*=\s*\(` 匹配不到，因 api.js 用对象属性简写 `listTaskBlocks: (taskId) => ...`（冒号非等号）；修法：`/listTaskBlocks\s*[:=]\s*\(/`。
- **验证**：**33 执行器全绿**（F-01~F-32 + ref-check 骨架，失败 0；F-27 130 / F-28 159 / F-29 154 / F-30 88 / F-31 85 / **F-32 77**，合计 693 断言）；`ref-check` **0 孤儿 / 0 悬空 / 0 重号 PASS**（软偏差 16 → 16 条，本点未新增）；实体转义 0（6 个改动/新增文件全查：index.html / README.md / AGENTS.md / ci.yml / api.js / tasks.html）；`ci.yml` YAML OK（**validate 步 39 → 40 项**：功能点列表补 F-32 + 加「跑 F-32」一步）；`env -u NODE_PATH` 直跑 F-27~F-32 六个全 PASS（**模拟 CI**）；`prototype/mock/verify.js` 仍 PASS。运行期实测：默认进 GOAL-2026Q3-01、三类事实标签＋数字逐字（①=4/②=3/③=1）、任务卡片含 T-1022/1021/1023/1020/1019/1018、5 过滤器；T-1020(`call_failed`)→等待接口恢复＋真实 `done_part`/`block_note`/`resume_condition`，T-1019(`stopped`)→已停止＋`done_part`＋不自动重启；前端全部经 `/api/`、仅 GET、写动作 0 次、localStorage 实测 `[]`（不含业务数据）、打开页面零写；63 运行期文件零引用 frontend、直调 6 接口（`/api/goals`/`/api/goals/GOAL-2026Q3-01`/`/api/task-blocks`/`/api/opportunities`/`/api/research`/`/api/research-result/R-006`）全 200、无 `.json` 快照。
- **双向登记**：`frontend/README.md`（§1 用例补 `TC-I-M6-006`、§3 目录 F-32 行 `待建` → `✅ 已建` + 新增 `test-f32.mjs` 行（7 组 77 断言）、§4 增缺口 17~19（只读页零写面／不自造端点／默认按当前浏览目标过滤）、§5 跑用例补 F-32、§6 反向清单补执行器与 `test-f32.mjs → pages/tasks.html + assets/* + ../server/api/index.js + ../server/task-runner/goal.js + ../server/shared-context/index.js + ../server/agent-orchestrator/result.js + ../server/task-runner/task-state.js + ../db/`）、`AGENTS.md`（`frontend/` 行补 F-32 落地：三类事实分别可见＋状态机口径与后端共用＋默认按目标过滤＋只经四种只读路由不自造端点；**F-32 一页待建 → 已落地（77 断言全绿）**）、`ci.yml`（功能点列表补 F-32 + validate 加「跑 F-32」一步）、`frontend/index.html`（F-32 页 `built: true`，**进度 5/6 → 6/6**，M6 六页面全部已建）。
- **M6 收官**：F-27~F-32 六页面全部落地，前端零构建静态资源 + 前后端分离纪律收口；`frontend/index.html` 进度 6/6。提交 **`36fa2d5`** 于分支 **`F-32-tasks-page`**（自 `F-31-followup-page` 另起，ff-only 就绪；须 F-31 合入 main 后方可顺序 ff-merge）。三项待裁决缺陷见下节，已于本日收口并全部合入 main。

## 三项待裁决缺陷：实地坐实 + 用户裁决 + 修复 + 全部合入 main · 2026-09-20

- **触发**：用户指令「缺陷的地方现在全部找我确认」→ 不凭记忆，三处逐项实地核对（读真源文件 + 种子行 + 分支 diff）后交用户裁决；AskUserQuestion 三项全选「修复」方向。
- **实地坐实（非凭记忆）**：① Q-16（`db/seed/0001_mock.sql` 的 `opportunity_evidence.linked_at` 10 行全写产出任务 ID，`schema.md` §12 登记属实）；② `discovery/agent.md` 仅 17 行建壳（hva 已落五段本体），`agent-runtime/README.md` 目录树标「建壳」属实；③ F-14 `loadDiscoveryContext` 活 bug：`api/index.js:779` 运行时调用，原读 `ctx.goal/background/capabilities/sources` 顶层键，但 `getTaskContext` 实际返回 `sections[]`+`scope`，四字段恒空（`agent-orchestrator/README.md` §⑥ 登记「未擅自改」属实）。
- **三分支独立修复（ff-only 就绪）**：
  - `fix-q16-seed`：Fix① Q-16 采纳方向①——`db/seed/generate_mock.py` `opp_created` 由 `r[11]`(producing_task_id) 改 `r[12]`(created_at)，重生成 `0001_mock.sql` 10 行 `linked_at` 改写真实时点；`schema.md` v1.5→v1.6，§12 Q-16 翻「已决①」，旧版归档 `./.trash/schema.md-v1.5.md`；README 三处引用翻正。
  - `fix-discovery-agent-md`：Fix② `agent-runtime/discovery/agent.md` 把 `## 建壳声明` 替换为 `## 角色指令`（职责/输入/工作方式/输出/结束条件）+ `## Skill 清单`（S-A1~S-A4 表），文档卡与反向清单同步；README 目录树/状态翻正。
  - `fix-f14-loadcontext`：Fix③ `server/agent-orchestrator/discovery.js` `loadDiscoveryContext` 改从 `ctx.sections` 按 `context_type_code` 取值（对齐 F-19 `research-start.js:355`）；`test-f14.mjs` 加 ⑥ 组真实 D1 断言。
- **全部合入 main（ff-only 线性，已执行）**：`main` 原停在 F-29（30608b4）；4 条线（F-30/F-31/F-32 链 + 3 修复分支）皆从 F-29 分叉。执行：先 `merge --ff-only F-32-tasks-page`（main→a8855b2），再依次把每个修复分支 `rebase` 到当前 main tip 后 `merge --ff-only`。最终 `main` = **`9a90a39`**，线性链：`F-29 → F-30 → F-31 → F-32 → fix-q16-seed(81aec48) → fix-discovery-agent-md(7066a4c) → fix-f14-loadcontext(9a90a39)`，**无 merge commit、无冲突**（修复分支与 F-30/31/32 改动文件不相交，README 改动行不重叠）。
- **验证（合并后重跑）**：`test-f14.mjs` **1/0**（38 断言全绿）；`frontend/test-f32.mjs` **1/0**（77 断言全绿）；其余 F-30/F-31 内容未变。实体转义 0；ref-check 全仓 0 命中。
- **纪律**：DDL 一律未动（Q-16 仅改种子单文件）；一缺陷一 PR（原独立分支，合并后清理）；未发现上游文件自身缺陷，无「未擅自改」登记。
- **待用户操作**：本地合入完成，部署流水线由**用户推送 main 时触发**（本环境不自动 push）。原「未决三项」至此全部收口。

## 出关前全量回归 → 揪出并修复 Q-16 引发的种子回归 · 2026-09-20

- **触发**：用户「继续完成剩余的工作」→ 决定先做**全量回归（32 套件）**而非只跑个别套件，结果揪出一个**此前三项缺陷修复时完全没被发现的回归**。教训：局部回归（只跑 F-16/F-17）会漏掉跨表关联影响，**改种子后必须全量回归**。
- **发现的回归（重要）**：Q-16 修复时**重跑 `generate_mock.py` 全量重生成种子**，因**生成器与已验证 SQL 不同步**，连带回退三处已收口修正：
  - `gap_rule` GAP-3/GAP-4 的 `target_field` 由规范化值 `business_scope`/`focus_period` **回退成别名 `scope`/`period`**——**冲掉 Q-13 已决①**，直接触发 `test-f01` 的 `alias_fields.length===0` 断言失败（87 通过 / 1 失败）。
  - `task` T-1023 的 `parent_task_id` 由 `NULL` 变 `T-1021`。
  - `opportunity_relation` LK-OR-001 的 `relation_kind` 由 `superseded` 变 `related_update`——**跨枚举误用**（`related_update` 属 `EVIDENCE_LINK_KIND`，而 `OPP_RELATION` 只有 `same_issue`/`superseded`）。
  - 判定依据：无测试断言 T-1023 父任务值（test-f31/f22 的 parent_task_id 断言针对**新建**追问任务，非种子行）；故以"已验证且 CI 通过"的 pre-Q16 种子为准恢复。
- **修法（分支 `fix-q16-seed-regression`，提交 **`c769a81`**，已 ff-merge 进 main）**：把生成器三处对齐回已验证口径后**重新生成**，使生成器成为唯一真源。**验证：重生成结果与已验证种子（`81aec48^`）仅差 10 行 `opportunity_evidence.linked_at`（即 Q-16 修复本身），其余零差异** → 隐患根除。
- **全量回归结果（修复后）**：**32/32 套件通过，内部手动断言合计 2194 条**；`test-f01` 由「87 通过 / 1 失败」转为 **88 全通过**。
- **运行时冒烟（补单元测试覆盖不到的一层）**：`prototype/mock` 起服务 → `/health` 200；`restricted` 正确映射 `restricted_flag=1 + fail_reason=权限未开通`；`slow` 给 `retry_count=2` 且**成功不算失败**（`result_status=ok`，对应 F-26 契约）；未知路由 404。前端六页零构建静态交付 **HTTP 200 全部**。
- **分支清理**：34 个已合分支全部删除（均为 main 祖先、ff 已合、提交全在 main 内，**零数据丢失**），仓库只剩 `main`。
- **产出 `docs/04-plan/go-live-checklist.md`（出关清单 v1.0，提交 **`ff0d956`**）**：三道门禁——A 代码就绪 ✅ / B 部署上线 ⬜（无远程无凭证，含操作步骤与 PR 标题约定）/ C 外部契约 ⬜（`T-01`~`T-10`、`T-20`~`T-27`、`T-32` + `TS-10`~`TS-13`，含 P0 阻断提示）；已登记进 `docs/04-plan/README.md`。引用自检 **0 命中 PASS**、实体转义 0。
- **当前 main**：**`ff0d956`**（线性：`…F-32 → fix-q16-seed → fix-discovery-agent-md → fix-f14-loadcontext → fix-q16-seed-regression → docs(plan)出关清单`）。
- **仍需用户拍板（按优先级）**：① 远程地址 + Cloudflare 凭证（开门禁 B）；② **`TS-10` Workers AI 模型选型（最大风险，决定 F-20 能否「敢说不足以判断」）**；③ `T-01`/`T-02`/`T-05` 真实契约；④ `T-20`/`T-25` 分类框架；⑤ `TS-11` varchar 长度策略；⑥ `TS-12` 拼音排序列。
- **踩坑复现（已验证有效）**：同一文件 4 处 Edit **并行放在一条消息里，静默丢了 2 条**（回执 success 但文件没变）——GAP-3 与 LK-OR-001 两条被丢。**靠"编辑后必须 Grep 复核"这条纪律才当场发现**，否则重跑生成器得出的"已修复"结论是假的。再次印证：多处改动要么分开消息，要么改完立即复核。

## 全仓未决项实地扫描 + 四项用户裁决落地（2026-09-20 晚）

**扫描方式（不凭记忆）**：`schema.md` §12 Q-xx、`external-deps.md` §7 T-xx、`tech-stack.md` §8 TS-xx 三张清单 + 全仓 `⬜` 散落标记 + ADR 内嵌项。

**扫描结论**：Q-01~Q-16 **全部已决**；§7 T-xx 多为对接方给值（我不能编）；§8 TS-xx 13 条。**真正漏网的在 ADR 里**——`ADR-003` §7 两条 ⬜ 不在任何一张未决清单中，而 ADR 自己写明「必须在下游写入侧实现前关闭」，但 F-16 写入侧**已落地** → 是真空缺（`opportunity.js` 原样透传 `unknown_item`，Agent 写空串就落空串，无任何写入侧限制）。

**四项裁决与落地**（均独立分支从 main 另起、逐条 ff-merge，main 现 `f342412`）：

| 裁决 | 落地 | 提交 |
| --- | --- | --- |
| ① 空串态「确无未知项」**只允许 PM 写入** | `opportunity.js` 新增纯函数 `judgeUnknownItemWrite` + 在 `formOpportunityOrGap` 作**前置守卫**（取号与写库之前判，不留半截状态） | `64ecc9e` |
| ② 产出方两者皆有，**按来源区分** | 用 `MD-06.producing_task_id` 判定（非空＝Agent 任务产出、空＝PM 补录）——**不新增列、不改 DDL**；防 NULL 规避**沿用既有** `pending_supplement`（未评估→六要素不齐→走缺口分支不写库），**不重造第二套口径** | `64ecc9e`（与 ① 合并） |
| ③ §2.5 模型约束**放宽** | 原「必须支持 function calling」→ 改为「① JSON schema 结构化输出；② 敢说不足以判断」。依据：工具不是模型选的（F-19 已确定性编排排查证顺序）、调用不走模型（F-24 自建 MCP 按 `tool_id` 执行，`mcp-client.js` 里「模型」仅以禁令形态出现） | `30bf969`（tech-stack v1.3） |
| ④ TS-11 兜底＝**应用层校验** | 库级 `CHECK` 不写进 `0001`；代价是绕过写入面的路径拦不住，须在各写入面分别落实 | `30bf969`（与 ③ 合并，同文件） |

**①+② 必须合并做**的原因：① 的强制规则依赖 ② 的来源判定，分开会产生不自洽中间态（先禁空串会让 PM 也写不了）。

**同步下游的关键判断**：钉版引用分两类处理——**「现状声明」**（如「已建 v1.2」）改 v1.3；**「历史回填事实」**（如「2026-09-19 回填至 v1.2」）**保留 v1.2**，因为钉旧版是史实，改成新版本反而失真。共清 8 处已失效的「TS-11 仍待裁决」表述（含 tech-stack §3 正文表与 db/README 检查单）。

**另修两处登记瑕疵**（`f342412`）：`Q-16` 状态列标 ⬜ 但正文写「已决①」→ 翻 ✅；`T-32` 行只有 3 列（表头 5 列）且「阻塞什么」被错放进「谁提供」列 → 补齐。**未动** `ADR-003` §7 第 3 条（写入 CFG-05 还是 agent.md 属架构归属，须用户裁决）。

**验证**：全量 32/32 套件、**2210 断言 0 失败**（`test-f16` 由 73→89，新增 ⑩ 组 16 条含端到端「Agent+空串→抛错且不写 MD-06」与「Agent+非空未知项→正常落库不误伤」）；引用自检 0 命中 PASS；实体转义 0。

**⚠️ 发现未提交的 Workers AI 接入试验（未动，待用户处置）**：工作区里 `wrangler.toml`（新增 `[[ai]]` binding）、`server/api/index.js`（新增 `/api/ai-ping`、`/api/ai-tool-test` 路由）、**新文件** `server/agent-orchestrator/llm-client.js`（222 行，`chat`/`chatWithTools`/`chatJSON`，`DEFAULT_MODEL = "@cf/deepseek/deepseek-v4-flash"`，`MODELS` 池含 pro/glm-flash/kimi）。来源不明（非本会话产物），**未测试、未登记、未提交**。它恰好给出了 TS-10 想要的**具体模型 ID 形态**（`@cf/<厂商>/<模型>`），但其中的 `chatWithTools` 走 function calling，与刚裁决的「放宽、不要求 function calling」有张力 → 已向用户报告待定。

**本轮两个新踩坑**：
1. **`grep ... | head -20` 截断导致漏项**——列下游引用文件时被 head 截断，漏掉 `db/README.md` 等 3 个文件里的 5 处「TS-11 仍待裁决」。改为按关键词（TS-11）**不截断全扫**才补齐。**凡"同步下游/全量扫引用"类操作，禁止 head 截断，或先计总数再取全量。**
## Workers AI 接入试验收编（2026-09-20 晚，提交 `562103a`）

用户裁决「规范化收编」后执行。收编对象：工作区三个未提交产物（`wrangler.toml` 的 `[[ai]]`、api 两个探测路由、新增 `llm-client.js` 222 行）。

**收编时修的两处真实缺陷**：
1. `chatWithTools` 的 `stopped_by` 错报——原实现在**每轮循环末尾无条件**赋值 `max_rounds`，导致「执行过工具后正常结束」被错报成 `max_rounds`。修法：**把赋值移出循环**（只有真正用尽轮次才标）。
2. 用尽轮次时 `content` 恒 `null`——原实现只判断「最后一条消息是否为 assistant」，而每轮末尾都会 push 一条 tool 消息，**该判断恒假**。修法：回找最后一条 assistant 消息。

**另消除硬编码**：api 三处 `@cf/...` 模型字面改为引用 `MODELS` 常量（唯一真源），`/api/ai-ping` 改走 `chat()` 封装。

**新增 `test-llm-client.mjs`（56 断言，mock binding 不触真实推理、无需账号）**；登记进 agent-orchestrator README 文件清单 + 用例、server README 模块表、api 文档卡、tech-stack §8 TS-10 补进展（**TS-10 仍未关**，默认值属待验证暂定）。全量 34 套件 **2266 断言 0 失败**。

**⚠️ 收编完成后工作区又冒出新的并发改动**（非本会话产物，与之前 AI 试验同一现象）：`server/agent-orchestrator/discovery.js` **+295 行**（新增 `import { chatWithTools, chatJSON, MODELS } from "./llm-client.js"` 与 `runDiscoveryWithLLM`，并动态引入跨模块**写面** `../tool-executor/index.js#recordQuery`）、`agent-runtime/discovery/agent.md` +14 行。这批改动**改变了 F-14 的硬红线语义**（原本「零写库、不调 LLM」，现在既调 LLM 又能写 EXT-01），且使我刚写的「编排层仍不调 LLM、门禁关闭前无编排文件 import」登记声明**失真实**。实测 test-f14 仍 38 绿、全量 2266 断言 0 失败（未破坏既有用例，因新增的是新函数、写语句在 tool-executor 侧）。**未擅自收编，已向用户报告待裁决。**

2. **`git commit -m "..."` 用双引号时，反引号被 shell 当命令执行**——提交信息里的 `` `*` `` 被展开成当前目录文件列表，第一个 `AGENTS.md` 被当命令执行（`command not found`），提交信息该处被静默替换为空。**修法：`-m` 一律用单引号**（单引号内反引号不展开），改完用 `git log -1 --format=%B` 复核。


